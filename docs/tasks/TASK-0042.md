# TASK-0042

## Fecha
2026-03-11

## Estado
DONE

## Objetivo
Endurecer las reglas de importación en `Subcategorías`, `Recursos` y `APUs` para detectar duplicados antes de confirmar y exigir en `APUs` la combinación obligatoria `descripción + unidad válida del sistema`.

## Alcance
- Añadir motivo de omisión en los previews de importación.
- Detectar duplicados dentro del pegado y contra la base activa antes de confirmar.
- Exigir en `APUs`:
  - descripción obligatoria
  - unidad obligatoria
  - unidad existente y válida en el catálogo de unidades del sistema para categoría `5`
- Alinear backend y frontend del importador de `APUs` con payload estructurado.

## Implementación
- Se añadió `reason` en los previews de:
  - `frontend/src/pages/Subcategorias.jsx`
  - `frontend/src/pages/Recursos.jsx`
  - `frontend/src/pages/APUs.jsx`
- Se incorporó validación previa de duplicados:
  - dentro del propio pegado
  - contra los registros ya cargados en el contexto activo
- Se cambió el import de `APUs` para trabajar con objetos `{ descripcion, unidad }`.
- Se añadieron nuevos schemas en `backend/app/schemas/apu.py`.
- Se endureció `POST /api/v1/apus/import-clipboard` en `backend/app/api/endpoints/apus.py`.
- Se reescribió `APURepository.import_clipboard(...)` en `backend/app/repositories/apu.py` para:
  - validar unidad
  - validar duplicados por `descripcion + unidad`
  - crear códigos secuenciales reales
  - confirmar la transacción correctamente

## Validación
- `python -m py_compile backend/app/schemas/apu.py backend/app/api/endpoints/apus.py backend/app/repositories/apu.py`
- `npm run build`

## Resultado
El flujo de importación ya no requiere revisar errores uno a uno tras confirmar: los duplicados y filas inválidas se detectan antes, y `APUs` queda alineado con la regla de negocio de unidad obligatoria y válida.
