# TASK-0045

## Fecha
2026-03-11

## Estado
DONE

## Objetivo
Revertir la restricción introducida en `TASK-0044` que excluía la categoría `APU` del modal de importación en `Subcategorías`.

## Alcance
- Restaurar `APU` en el selector de categoría destino del modal de importación.
- Eliminar la guarda adicional que bloqueaba la importación hacia categoría `5`.
- Recuperar el comportamiento previo sin afectar el resto de la importación avanzada.

## Implementación
- Se eliminó el filtro `IMPORTABLE_SUBCATEGORIAS` en `frontend/src/pages/Subcategorias.jsx`.
- Se restauró la apertura directa del modal de importación.
- Se volvió a mostrar `APU` como opción de destino en el selector del modal.
- Se retiró la guarda que impedía importar hacia categoría `5`.

## Validación
- `npm run build`

## Resultado
La exclusión funcional introducida en `TASK-0044` quedó anulada y el modal de importación de `Subcategorías` vuelve a comportarse como antes de esa restricción.
