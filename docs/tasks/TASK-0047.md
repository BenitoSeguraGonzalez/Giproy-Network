# TASK-0047

## Fecha
2026-03-11

## Estado
DONE

## Objetivo
Corregir el error `ReferenceError: Cannot access 'unidades' before initialization` en la pantalla `APUs`.

## Implementación
- Se reordenó la inicialización del preview de importación en `frontend/src/pages/APUs.jsx` para que `unidades` esté declarada antes de ser usada por `useMemo`.

## Validación
- `npm run build`

## Resultado
La pantalla `APUs` deja de fallar al montar y conserva intacta la lógica del preview de importación.
