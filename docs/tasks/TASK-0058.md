# TASK-0058

Estado
DONE

Tipo
hardening

Prioridad
critical

Descripcion
Blindar el modulo `Presupuestos` y el editor completo para multiempresa: carga del presupuesto, refrescos, lineas, notas, tanteos, Pareto, indirectos, catalogo APU, `EDT` y acciones auxiliares deben respetar de forma uniforme la empresa activa del superadministrador.

Archivos afectados
- frontend/src/pages/Presupuestos.jsx
- frontend/src/components/presupuestos/PresupuestoDetail.jsx
- frontend/src/context/PresupuestoContext.jsx
- frontend/src/components/presupuestos/
- frontend/src/api/presupuestos.js
- frontend/src/api/proyectos.js
- frontend/src/api/edt.js
- backend/app/api/endpoints/presupuestos.py

Checklist

[x] analisis
[x] implementacion
[x] pruebas
[x] documentacion

Notas
- Esta TASK corrige la zona donde ya se detecto el fallo visible de `Indirectos`.
- Deben eliminarse las llamadas `api.get/post/delete(...)` directas que hoy esquivan el contexto tenant.
- `presupuestosApi` ya expone operaciones de tanteo (`apply`, `clear`, `revert`) para que el editor no dependa de `axiosConfig` directo.
- `PresupuestoDetail`, `PresupuestoContext`, `Presupuestos`, `CatalogoApuTab`, `TanteoTab`, `LineasPresupuestoTab` y `HerramientasTab` quedaron reencaminados a clientes tenant-aware.
- Al fallar una recarga bajo otro tenant, el editor ya limpia el presupuesto/proyecto activo en lugar de seguir mostrando datos del contexto previo.
