# TASK-0059

Estado
DONE

Tipo
hardening

Prioridad
critical

Descripcion
Cerrar el saneamiento multiempresa en `Precios Unitarios`, con foco especial en `APUs`, para que `Bases`, `Subcategorías`, `Recursos` y `APUs` respeten siempre la empresa activa en CRUD, importacion, exportacion, duplicados, borrado masivo y flujos auxiliares.

Archivos afectados
- frontend/src/pages/Subcategorias.jsx
- frontend/src/pages/Recursos.jsx
- frontend/src/pages/APUs.jsx
- frontend/src/api/subcategoriasItems.js
- frontend/src/api/recursos.js
- frontend/src/api/apus.js
- backend/app/api/endpoints/subcategorias_items.py
- backend/app/api/endpoints/recursos.py
- backend/app/api/endpoints/apus.py

Checklist

[x] analisis
[x] implementacion
[x] pruebas
[x] documentacion

Notas
- `APUs.jsx` sigue siendo el punto mas delicado por mezcla de wrappers API y llamadas directas.
- El objetivo es que ningun flujo de catalogo tecnico pueda operar fuera de la empresa seleccionada.
- `APUs.jsx` ya quedó reencaminado a `apusApi`, `recursosApi`, `subcategoriasItemsApi` y `basesTrabajoApi`, eliminando las llamadas directas con query string manual.
- `Recursos.jsx` ya exporta con `empresa_id` consistente y `BasesTrabajo.jsx` dejó explícito el bypass sin tenant para el catálogo global de países.
- En el dominio `Precios Unitarios`, la única llamada directa remanente en los archivos revisados corresponde a `/paises/`, que es catálogo global y está marcada con `withoutTenant()`.
