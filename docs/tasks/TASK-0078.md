# TASK-0078

## Fecha
2026-03-12

## Estado
DONE

## Objetivo
Unificar la banda informativa de formato esperado en los modales de importación de `Subcategorías`, `Recursos` y `APUs`.

## Implementación
- Se creó `frontend/src/components/precios-unitarios/ImportFormatHint.jsx` como componente reutilizable.
- Se sustituyó el bloque ad-hoc existente en `frontend/src/pages/Recursos.jsx`.
- Se añadió el mismo patrón visual en:
  - `frontend/src/pages/Subcategorias.jsx`
  - `frontend/src/pages/APUs.jsx`
- El copy quedó ajustado por módulo:
  - `Subcategorías`: línea completa como descripción si solo hay una columna.
  - `Recursos`: columnas esperadas y fallback de reconstrucción.
  - `APUs`: `descripción + unidad` con unidad válida obligatoria.

## Validación
- `npm run build`

## Resultado
Los tres flujos de importación masiva de `Precios Unitarios` muestran ahora una banda de ayuda homogénea, alineada visualmente y adaptada a la lógica real de cada parser.
