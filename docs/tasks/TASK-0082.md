# TASK-0082

## Fecha
2026-03-12

## Estado
DONE

## Objetivo
Implementar la shell base de `Administración Global`: entrada de menú, rutas protegidas, layout y portada inicial para `Superadministrador`.

## Alcance
- Nuevo ítem de navegación lateral `Administración Global`.
- Visibilidad exclusiva para `Superadministrador`.
- Ruta base `/admin-global`.
- Pantalla inicial tipo panel técnico con cards de acceso a submódulos.
- Protección de acceso por rol y manejo correcto de acceso directo por URL.

## Restricción
Esta TASK solo crea la shell del módulo. No debe mover todavía funcionalidades híbridas ni alterar permisos existentes de `Administrador`.

## Implementación
- Se creó `frontend/src/pages/AdminGlobal.jsx` como portada inicial del módulo.
- Se añadió la ruta protegida `/admin-global` en `frontend/src/routes/AppRouter.jsx`.
- Se integró acceso visual exclusivo para `Superadministrador` en:
  - `frontend/src/layouts/AppLayout.jsx`
  - `frontend/src/pages/Dashboard.jsx`
- Se marcó `/admin-global` como ruta segura sin requerimiento de empresa activa en `frontend/src/api/tenant.js`.
- La portada deja explícito que las funciones híbridas no se mueven aún y que su clasificación queda para `TASK-0084`.

## Validación
- `npm run build`

## Resultado
`Administración Global` queda operativo como shell de navegación exclusiva para `Superadministrador`, sin tocar todavía `Settings` ni romper permisos híbridos existentes.
