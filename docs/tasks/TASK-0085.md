# TASK-0085

## Fecha
2026-03-12

## Estado
DONE

## Objetivo
Preparar el submódulo de gobernanza dentro de `Administración Global`, agrupando gestión avanzada de empresas, usuarios globales y políticas transversales.

## Alcance
- Definir secciones:
  - `Empresas`
  - `Usuarios globales`
  - `Políticas`
- Revisar qué acciones ya existen y cuáles deben centralizarse.
- Diseñar layout de tablas, filtros y acciones administrativas.
- Respetar el aislamiento multiempresa ya implantado.

## Dependencia
Depende de `TASK-0081` y `TASK-0082`.

## Resultado esperado
Queda definida y preparada la gobernanza central del sistema sin mezclarla con módulos operativos de negocio.

## Implementación realizada
- Se implementó la pantalla `frontend/src/pages/AdminGlobalGobernanza.jsx`.
- `Administración Global` ya enlaza a `Gobernanza` como submódulo preparado.
- La vista agrupa tres áreas:
  - `Empresas`
  - `Usuarios globales`
  - `Políticas`
- `Empresas` y `Usuarios globales` se muestran con resumen operativo y enlaces a sus flujos actuales, evitando mover todavía acciones híbridas destructivas fuera de `Settings`.
- `Políticas` deja explícitas las reglas de gobierno activas del sistema.
