# TASK-0086

## Fecha
2026-03-12

## Estado
DONE

## Objetivo
Diseñar el submódulo de `Auditoría` dentro de `Administración Global` para consolidar trazabilidad operativa y lectura de eventos del sistema.

## Alcance
- Definir alcance inicial de auditoría:
  - eventos administrativos
  - cambios relevantes
  - actividad global por empresa / módulo / fecha
- Diseñar filtros y formato de consulta.
- Evaluar fuentes de datos reutilizables y backlog necesario para eventos no trazados aún.

## Dependencia
Depende de `TASK-0081` y `TASK-0082`.

## Resultado esperado
Queda una base clara para exponer auditoría operativa sin improvisar una vista monolítica ni dispersa.

## Implementación realizada
- Se añadió el endpoint backend `admin-audit` con:
  - `GET /admin-audit/summary`
  - `GET /admin-audit/recent-events`
- Se implementó la pantalla `frontend/src/pages/AdminGlobalAuditoria.jsx`.
- La auditoría consolida fuentes reales existentes:
  - `auth_debug.log`
  - `fatal_errors.log`
  - estado administrativo global
  - backlog explícito de trazabilidad pendiente
- `Administración Global` ya enlaza el submódulo `Auditoría`.
