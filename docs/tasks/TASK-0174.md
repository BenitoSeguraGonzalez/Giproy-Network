# TASK-0174

Estado: DONE
Tipo: feature
Prioridad: high

Descripción:
Centralizar la gestión de precios de recursos para evitar ediciones directas en el editor de APU. Los precios deben ser de solo lectura en el APU y actualizarse únicamente a través del modal de edición de recursos, asegurando que los cambios se propaguen correctamente a todos los módulos (APU, Presupuestos, etc.).

Archivos afectados:
- frontend/src/pages/APUs.jsx (Modificado)
- backend/app/services/recurso.py (Verificado)

Checklist:
[x] Hacer que el campo `precio` en las líneas de recursos de APU sea `readOnly`.
[x] Eliminar manejadores de eventos (`onBlur`, `onKeyDown`, `onChange`) del campo de precio en APU.
[x] Actualizar estilos CSS para indicar estado de solo lectura.
[x] Verificar que el backend ya cuenta con la lógica de cascada (`_trigger_cascade_for_recurso`).
[x] Validar la propagación de cambios desde el modal de recursos.
