# TASK-0181: Estudio Profundo de Errores Críticos y Corrupción de Datos

Estado
DONE

Tipo
fix

Prioridad
high

## Descripción del Problema
Se han detectado fallos sistémicos que afectan la operatividad de la plataforma:
1. **Error 500 en `/usuarios/me`**: Impide la autenticación correcta y causa errores 401 en cascada.
2. **Falla en Búsqueda OmniClass**: Los maestros no devuelven resultados o el frontend no los visualiza.
3. **Corrupción de APUs**: Presencia de líneas "--- GENERAL" sin vinculación, persistentes tras intentos de corrección.

## Objetivos
- [x] Diagnosticar y corregir el error 500 en el backend (TypeError y EmpresaResponse).
- [ ] Verificar la integridad de la base de datos para registros de `usuarios` y `empresas`.
- [x] Resolver la invisibilidad de resultados en `SearchableSelect` para OmniClass (tbl_21 vs tbl_22).
- [x] Limpiar y prevenir la creación de líneas APU corruptas (Fix: joinedload en repo).
- [x] Corregir visualización de cálculos $NaN en editor APU (Fix: numeric parsing en frontend).

## Plan de Acción
1. [x] Backend Diagnostics: Ejecutar script de prueba para capturar el error de `/me`.
2. [x] OmniClass API Audit: Verificar que `/api/v1/maestros/omniclass/search` devuelva datos correctamente.
3. [x] Frontend Component Review: Revisar `SearchableSelect` y `APUs.jsx` para asegurar que las correcciones se aplican.
4. [x] Data Sanitization: Script oficial de auditoría/saneamiento ampliado para detectar y eliminar `apu_lineas` huérfanas (`recurso_id IS NULL` y `apu_hijo_id IS NULL`).

## Resultado
- Se consolidó el saneamiento de líneas APU huérfanas dentro de `backend/scripts/audit_data_integrity.py` y `backend/scripts/sanitize_data_integrity.py`, evitando depender de scripts sueltos con credenciales embebidas.
- La auditoría ejecutada sobre la base local confirmó `0` incidencias en `orphan_apu_lines`, por lo que no fue necesario borrar datos reales en esta ejecución.
- La verificación técnica por compilación de los scripts afectados fue exitosa.

## Pruebas ejecutadas
- `python -m py_compile backend/scripts/audit_data_integrity.py backend/scripts/sanitize_data_integrity.py`
- Ejecución de auditoría de integridad con salida JSON, confirmando `orphan_apu_lines.count = 0`
