# TASK-0194

Estado
DONE

Tipo
fix

Prioridad
high

Descripción
Ejecutar saneamiento conservador sobre los `4` códigos APU divergentes de `Santiago Bermeo` entre la base maestra `32` y la base de proyecto `34`. El criterio fijado fue alinear la base de proyecto con la base maestra solo cuando el APU destino no tuviera líneas, referencias hijas ni uso en presupuesto.

Archivos afectados
- backend/scripts/sanitize_santiago_divergent_apu_codes.py
- docs/tasks/TASK-0194.md
- docs/CHANGELOG.md
- docs/HANDOFF.md

Checklist

[x] análisis
[x] implementación
[x] pruebas
[x] documentación

Resultado
- Se verificó que los códigos `5-011-0001`, `5-021-0001`, `5-027-0001` y `5-029-0001` estaban divergentes entre la base maestra `32` y la base de proyecto `34`.
- Todos los registros destino en base `34` cumplían el criterio conservador: `0` líneas APU, `0` referencias como APU hijo y `0` usos en presupuesto.
- Se creó y ejecutó `sanitize_santiago_divergent_apu_codes.py` para alinear en base `34` la descripción, normalización, unidad y OmniClass con la base maestra `32`.
- Tras el saneamiento, `Santiago Bermeo` queda sin divergencias activas de `mismo código / distinta descripción` entre su base maestra y su base de proyecto para esos casos.

Validación de licencias
- Sin impacto en licencias.
- No cambia cuotas, permisos ni middleware.

Impacto UI
- Corrige la consistencia del catálogo APU entre base maestra y base de proyecto.
- Evita que reaparezcan APUs con el mismo código pero distinta semántica en el flujo operativo.

Pruebas ejecutadas
- `python backend/scripts/sanitize_santiago_divergent_apu_codes.py`
- `python -m py_compile backend/scripts/sanitize_santiago_divergent_apu_codes.py`
- Consulta SQL/ORM posterior sobre los `4` códigos saneados
- `python backend/scripts/audit_data_integrity.py`
