# TASK-0231 - Corrección de mapeo OmniClass en frontend

## Estado
COMPLETED

## Objetivo
Corregir el mapeo entre categorías funcionales y tablas OmniClass en `Recursos`, `Subcategorías`, `APUs` y otros consumidores.

## Trabajo previsto
- Centralizar el mapeo `categoría -> tabla OmniClass`.
- Eliminar divergencias entre label visible y tabla realmente consultada.
- Revisar especialmente `Recursos`, donde `Materiales` está consultando una tabla distinta de la que la UI anuncia.
- Verificar consumidores secundarios del dato OmniClass en presupuesto/reportes.

## Resultado esperado
Todos los selectores OmniClass consultan la tabla correcta de forma consistente.

## Resultado
- Se centralizó el mapeo OmniClass en `frontend/src/utils/omniclass.js`.
- `Recursos` dejó de mezclar label `Tabla 23` con consultas reales a `Tabla 22`.
- `Subcategorías` y `APUs` reutilizan ya la misma lógica central.
