# TASK-0232 - UX de selector OmniClass con precarga

## Estado
COMPLETED

## Objetivo
Evitar dropdowns OmniClass aparentemente vacíos al abrir el selector.

## Trabajo previsto
- Definir patrón de precarga inicial al abrir selector.
- Reutilizar `GET /maestros/omniclass/tabla/{tabla_id}` o ampliar backend si hace falta.
- Mantener búsqueda incremental posterior sin perder rendimiento.
- Aplicar comportamiento común a `Recursos`, `Subcategorías` y `APUs`.

## Resultado esperado
Al desplegar el selector OmniClass, el usuario ve opciones base relevantes incluso antes de teclear.

## Resultado
- `SearchableSelect` admite ahora `onOpen`.
- `Recursos`, `Subcategorías` y `APUs` precargan OmniClass vía `GET /maestros/omniclass/tabla/{tabla}` al abrir el selector.
