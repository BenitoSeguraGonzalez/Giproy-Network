# TASK-0237 - Backend OmniClass bilingüe

## Estado
COMPLETED

## Objetivo
Habilitar lectura y búsqueda OmniClass sobre título original y título en español.

## Trabajo previsto
- Extender schema/endpoint OmniClass para exponer `titulo_es`.
- Permitir búsqueda por `codigo`, `titulo` y `titulo_es`.
- Definir estrategia de fallback si `titulo_es` es nulo.

## Resultado esperado
El backend OmniClass soporta consumo bilingüe de forma estable.

## Resultado
- El schema expone `titulo_es`.
- La búsqueda OmniClass consulta ahora `codigo`, `titulo` y `titulo_es`.
- Los resultados salen ordenados por `codigo`.
