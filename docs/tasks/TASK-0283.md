# TASK-0283 - Eliminación de Vista Navegable Informativa en EDT/EDO

Estado
COMPLETED

Fecha
2026-03-20

Objetivo
Eliminar la banda `Vista navegable` de la vista gráfica de `EDT` y `EDO` por no aportar valor operativo proporcional al espacio vertical consumido.

Implementación
- Eliminación del bloque informativo `Vista navegable` en `frontend/src/components/projects/HierarchyGraphView.jsx`.
- Limpieza del estado UI persistido asociado a esa banda.
- Limpieza de imports y props ya no utilizados.

Validación
- `npm run build`

Resultado
- La vista gráfica libera más altura útil para el lienzo y mantiene solo controles y contexto con valor operativo directo.
