# TASK-0286 - Integración de Subvalores de Presupuesto en EDT

Estado
COMPLETED

Fecha
2026-03-20

Objetivo
Mostrar en `EDT` los subvalores económicos por nodo cuando existan líneas de presupuesto con APUs asociadas, reutilizando la lógica ya resuelta en `EDT Valorada` y `Presupuesto`.

Implementación prevista
- Reutilizar o extraer la lógica de agregación actualmente presente en `frontend/src/components/presupuestos/EdtValoradaModal.jsx`.
- Integrar en `frontend/src/components/projects/Edt.jsx`:
  - directo por nodo
  - valorado por nodo
  - porcentaje relativo cuando aplique
  - desagregación por subniveles (`subvalores`) dentro del árbol EDT
- Mantener lectura clara en escritorio y portátil.

Dependencias
- `frontend/src/components/presupuestos/EdtValoradaModal.jsx`
- `frontend/src/components/presupuestos/LineasPresupuestoTab.jsx`
- `frontend/src/components/projects/Edt.jsx`

Validación
- Coincidencia numérica entre `EDT` integrado y `EDT Valorada` actual antes de retirarla.
- Compatibilidad con presupuesto operativo único por revisión.

Resultado
- Se creó `frontend/src/utils/edtValuation.js` como capa compartida de métricas y formato.
- `frontend/src/components/projects/Edt.jsx` ahora reutiliza esa lógica para mostrar subvalores por nodo y por rama.
- La vista soporta lectura en escritorio y modo portátil sin crear una segunda superficie paralela.
