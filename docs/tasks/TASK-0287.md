# TASK-0287 - Retiro de EDT Valorada en Presupuesto

Estado
COMPLETED

Fecha
2026-03-20

Objetivo
Eliminar de `Presupuesto` la opción y el modal de `EDT Valorada` una vez que la información valorada quede integrada directamente en `EDT`.

Implementación prevista
- Retirar botón y estado asociado en `frontend/src/components/presupuestos/PresupuestoDetail.jsx`.
- Retirar navegación específica a `EdtValoradaModal`.
- Evaluar si `frontend/src/components/presupuestos/EdtValoradaModal.jsx` pasa a estado legacy, refactorizable o eliminable tras la integración.

Validación
- `Presupuesto` no debe mostrar accesos redundantes.
- La navegación a `EDT` desde presupuesto debe seguir siendo clara.

Resultado
- Se retiró el botón y el estado asociado a `EDT Valorada` en `frontend/src/components/presupuestos/PresupuestoDetail.jsx`.
- `frontend/src/components/presupuestos/EdtValoradaModal.jsx` quedó como pieza legacy reutilizable de lógica, pero ya no como acceso operativo desde `Presupuesto`.
