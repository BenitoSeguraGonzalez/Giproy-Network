# TASK-0292 - Resumen Económico Desplegable en EDT Gráfico

Estado
COMPLETED

Fecha
2026-03-20

Objetivo
Mover el resumen económico de `EDT` en `Modo Gráfico` a una ventana o panel desplegable dentro del área de trabajo, evitando ocupar espacio fijo en el header.

Implementación prevista
- Eliminar las métricas económicas del header del módulo cuando `viewMode === graph`.
- Añadir un disparador compacto dentro del gráfico para abrir/cerrar el resumen.
- Ubicar la información dentro del área de trabajo, no en la cabecera estructural.

Validación
- El lienzo gana altura visible.
- El resumen sigue siendo accesible sin cambiar de módulo.

Resultado
- `frontend/src/components/projects/HierarchyGraphView.jsx` ahora recibe el resumen económico de `EDT` como panel desplegable dentro del área de trabajo.
- El header del gráfico queda libre de esa carga fija.
