# TASK-0309 - Decimales de Presentación en Reportes EDT y EDO

Estado
COMPLETED

Fecha
2026-03-21

Objetivo
Normalizar los decimales visibles en reportes `EDT` y `EDO`, alineándolos con la política de empresa.

Alcance
- listado/diccionario/valorada EDT
- salida numérica sensible de EDO, si aplica
- preview PDF y export Excel

Validaciones obligatorias
- presupuestos asignados, subtotales y porcentajes con `decimales_moneda`
- niveles/cantidades técnicas con `decimales_calculos` cuando corresponda

Resultado esperado
- `EDT/EDO` reportan números con la misma política visual del resto del sistema.

Resultado ejecutado
- El Excel de `EDT` ya presenta `PRESUPUESTO` / `SUBTOTAL_CUENTA` con `decimales_moneda` y niveles técnicos con `decimales_calculos`.
- `EDO` quedó auditado: no existe hoy exportación activa `Excel/PDF` desde `reporting.py` con salida numérica sensible que requiera ajuste adicional.
