# TASK-0312 - Revisión de Decimales en Stakeholders y Cronograma

Estado
COMPLETED

Fecha
2026-03-21

Objetivo
Determinar si `Stakeholders` y `Cronograma` tienen salidas numéricas sensibles que deban normalizarse bajo la política transversal.

Alcance
- auditoría de plantillas
- auditoría de preview/export
- implementación solo si hay exposición numérica relevante

Validaciones obligatorias
- si no existe dato numérico sensible, la task debe cerrarse como auditoría sin cambios
- si existe, debe alinearse con `decimales_moneda` o `decimales_calculos` según corresponda

Resultado esperado
- `Stakeholders` y `Cronograma` quedan explícitamente clasificados como reportes con o sin requerimiento de normalización decimal.

Resultado ejecutado
- `Stakeholders` quedó auditado como reporte sin salida numérica sensible en `reporting.py`; no requiere ajuste decimal.
- `Cronograma` quedó auditado como plantilla con placeholders numéricos, pero sin ruta activa en `reporting.py`; no se aplicó implementación hasta que exista exportación integrada en el contrato común.
