# TASK-0376 - Comunidad Section Governance on Main Surface

Estado: COMPLETED

Tipo: Frontend + Backend + QA + Documentation

Prioridad: Alta

Área: Comunidad

Objetivo
Corregir la rotura del modal de publicación y llevar la creación/edición de secciones y temas a la propia superficie principal de `Comunidad`, con permisos diferenciados entre público y privado.

Alcance
- Corregir el `ReferenceError` de `handleCreatePost`.
- Eliminar la tarjeta vacía de `Seleccione un tema...` del lienzo principal.
- Permitir creación de categorías y temas por cualquier usuario autenticado en `Público` y `Mi empresa`.
- Mantener edición/desactivación así:
  - `Público`: solo `superadministrador`
  - `Mi empresa`: `administrador` del contexto activo y `superadministrador`
- Exponer controles de crear/editar secciones y temas desde la propia superficie principal.

Restricciones
- Validaciones de permisos siempre en backend.
- Sin eliminar código existente de moderación.
- La gestión principal debe seguir funcionando tanto en público como en privado.

Validación
- `python -m py_compile backend/app/api/endpoints/community.py`
- `npm run build`

Resultado
- La UI principal de `Comunidad` ya puede crear y administrar estructura sin depender de una vista administrativa separada.
- El error de publicación en el modal desaparece.
- Las reglas de edición por ámbito quedan alineadas entre backend y frontend.
