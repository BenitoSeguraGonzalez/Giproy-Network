# TASK-0399 - APUs: corrección de referencia inválida en modal de recurso

## Objetivo
Corregir el `ReferenceError` en `APUs` causado por una referencia heredada a `selectedCat` dentro del modal compartido de edición de recurso.

## Alcance
- Frontend: sustituir la referencia inválida por el contexto real del recurso en edición.
- QA: validar compilación de `frontend`.

## Agentes
- Frontend
- QA
- Documentación

## Validaciones
- `npm run build`

## Resultado
- El editor/creador de APUs ya no cae con `selectedCat is not defined`.
