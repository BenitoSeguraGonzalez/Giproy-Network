# TASK-0426 - Ocultar costo directo en presentación de EDT

## Objetivo
Retirar `Costo Directo` de la presentación visual de `EDT`, manteniendo intactos los cálculos internos necesarios para valoración y presupuesto.

## Alcance
- Quitar `Dir.` del resumen visible de `EDT`.
- Quitar `Dir.` de los chips económicos del gráfico `EDT`.
- Quitar `Dir.` del panel económico flotante del gráfico `EDT`.

## Agentes
- Arquitectura: valida coherencia de la lectura económica visible.
- Frontend: elimina el dato de la presentación sin tocar la lógica.
- QA: verifica que `Val.` e `Ind.` sigan operativos.
- Documentación: registra el cambio visual.

## Validaciones
- `EDT` ya no debe mostrar `Costo Directo`.
- `Val.` e `Ind.` deben seguir visibles donde corresponde.
- Los cálculos internos de valoración no deben romperse.
