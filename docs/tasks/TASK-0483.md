# TASK-0483 - Ajuste posicional del indicador de propiedad en Proyectos

Estado: COMPLETED

## Objetivo
Relocalizar el indicador de propiedad (punto verde/ámbar) para que se posicione al inicio de la fila que contiene el nombre del proyecto, tanto en la cabecera de detalle como en la vista de lista.

## Alcance
- **Frontend (Proyectos.jsx)**:
  - Mover `MarketplaceOriginBadgeSet` en la cabecera de detalle (`selectedProject`) para que esté antes del `h1`.
  - Eliminar el margen superior (`mt-4`) del contenedor previo del indicador.
  - Mover `MarketplaceOriginBadgeSet` en la tabla de resultados para que esté antes del nombre del proyecto.

## Validación
- Visual: El punto debe aparecer en la misma línea que el nombre del proyecto, a su izquierda.
- Funcional: El tooltip y el color deben seguir respondiendo correctamente al origen del proyecto (Propio/Adquirido).
