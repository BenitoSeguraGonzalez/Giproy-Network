# TASK-0533

## Objetivo
Restaurar y homologar la edición directa de recursos normales desde la lista de composición en `APUs` y `ApuBudgetEditor`.

## Alcance
- `frontend/src/pages/APUs.jsx`
- `frontend/src/components/presupuestos/ApuBudgetEditor.jsx`
- `frontend/src/components/precios-unitarios/ResourceEditorModal.jsx`

## Implementación
1. Mantener el flujo existente en `APUs`.
2. Rehabilitar el mismo flujo en `ApuBudgetEditor`.
3. Reutilizar `ResourceEditorModal` para evitar dos UX distintas.
4. Refrescar línea y precio funcional tras guardar, sin salir del editor actual.

## Validación
- Desde ambos editores se puede abrir edición de recurso normal.
- El modal comparte semántica y comportamiento.
- La línea editada refleja precio/unidad actualizados al volver.
