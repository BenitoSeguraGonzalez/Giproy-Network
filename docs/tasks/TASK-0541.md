# TASK-0541

## Objetivo
Corregir el filtro del catálogo de `APUs` para que busque globalmente en toda la base activa y actualizar la CTA `Nuevo APU` a un lenguaje visual más actual y coherente con el módulo.

## Alcance
- `frontend/src/pages/APUs.jsx`

## Implementación
1. Sustituir el filtrado local por un pool global usando el catálogo completo de APUs ya cargado para el editor.
2. Permitir búsqueda por descripción, código, unidad y subcategoría asociada.
3. Ajustar el placeholder y los textos de resultados para dejar claro que la búsqueda es global.
4. Replantear la CTA `Nuevo APU` con una presentación más ligera y actual, alineada con la línea visual reciente del módulo.

## Validación
- El filtro encuentra APUs fuera de la subcategoría actualmente seleccionada.
- El encabezado refleja `Resultados globales` cuando hay texto de búsqueda.
- La CTA `Nuevo APU` mantiene funcionalidad y mejora su encaje visual.
- `npm run build` en `frontend` compila sin errores.
