# TASK-0564.md

## Objetivo
Sesión Giproy Clásico enfocada en trazabilidad operativa y promoción del slice activo.

## Modo
GIPROY CLASICO

## Estado
Completado.

## Fecha de creación
2026-03-27 08:04:23

## Alcance inicial
- Analizar el incidente de retorno no deseado al login reportado por la empresa `Santiago Bermeo`.
- Promover el trabajo maduro a una TASK específica del slice clásico.
- Validar no interferencia con la capa BIM.
- Mantener abierta la línea de sesión si persisten interrupciones del flujo distintas al logout visible.

## Validación
- La sesión activa debe quedar trazada con una TASK específica del incidente.
- No se deben introducir dependencias ni activaciones BIM.
- Si el usuario deja de ir al login pero sigue sufriendo recargas/remontajes automáticos, el incidente no debe considerarse cerrado.

## Cierre
- La línea clásica de esta sesión quedó absorbida por tasks específicas ya materializadas o cerradas documentalmente.
- No queda deuda funcional clásica abierta dentro de esta task paraguas.
- No hubo intervención ni contaminación sobre el frente BIM, que mantiene su propio carril abierto.

## Evolución de la sesión
- El frente de autenticación clásica quedó promovido a `TASK-0565` y `TASK-0566` para separar endurecimiento de sesión y eliminación de recargas/remontajes.
- El inciso de saneamiento de sincronización entre `Base Maestra` y `Base de Proyecto` en `Santiago Bermeo` quedó promovido a `TASK-0784` y `TASK-0785`, con reparación real de la base afectada y endurecimiento del flujo `sync-missing`.
- El endurecimiento adicional del flujo de sincronización entre bases quedó promovido a `TASK-0786` y `TASK-0787`, incorporando preview operativo, carriles explícitos de reparación y pruebas de contrato.
- El siguiente ajuste operativo de `APUs` quedó promovido a `TASK-0788` y `TASK-0789` para añadir filtro por estado válido, contador `visibles/totales` y deselección reversible de subcategoría en el listado.
- El siguiente inciso de `Proyectos` quedó promovido a `TASK-0790` y `TASK-0791` para restaurar sincronización real entre árbol y gráfico en `EDO` y blindar `EDT` con el mismo contrato de foco activo compartido.
- El siguiente ajuste operativo de `Proyecto > Presupuesto` quedó promovido a `TASK-0792` y `TASK-0793` para reducir fricción en el alta de APUs y permitir movimiento grupal de líneas con confirmación.
- El siguiente ajuste operativo de `Proyecto > Presupuesto` quedó promovido a `TASK-0794` y `TASK-0795` para pegar cantidades desde portapapeles con validación estricta y confirmación usando la gramática modal compartida.
- El siguiente ajuste operativo de `Proyecto > Presupuesto` quedó promovido a `TASK-0796` y `TASK-0797` para sustituir la multiselección libre por un modo selección contextual, con checkboxes reversibles y ámbito acotado por capítulo EDT.
- El siguiente ajuste operativo de `Proyecto > Presupuesto` quedó promovido a `TASK-0798` y `TASK-0799` para separar explícitamente los lanzadores de `selección` y `tanteo`, usando `Ctrl/Cmd`, `Shift` y un botón superior específico de tanteo.
- El siguiente ajuste operativo de `Proyecto > Presupuesto` queda promovido a `TASK-0800` y `TASK-0801` para resolver conflictos entre modos, cerrar tanteo al priorizar selección, cerrar selección al priorizar tanteo y unificar `Escape` como salida neutra del workbench.
- El siguiente slice maduro de la sesión se promovió a `TASK-0567` para completar el gestor de items del Marketplace clásico multi vendedor.
- El frente siguiente quedó promovido a `TASK-0568` para abrir la gestión SaaS de superadministradores y corregir la UX inicial de tienda para ese rol.
- El siguiente bloque quedó promovido a `TASK-0569` para endurecer ventas exclusivas de `Sistema` y añadir gestor de categorías con alcance por tipo de vendedor.
- El frente siguiente quedó promovido a `TASK-0570` para exponer el panel de superadministradores desde `Administración Global` y retirar la cabecera documental residual de esa portada.
- El siguiente slice maduro quedó promovido a `TASK-0571` para separar la administración de ventas Marketplace entre `Empresa` y `Sistema` con lectura agregada por tenant y UX explícita por rol.
- La incidencia de sesión de `Santiago Bermeo` quedó reabierta como `TASK-0572` al persistir la salida no deseada a login con evidencia compatible con una carrera entre refresh y manejo global de `401`.
- El siguiente ajuste clásico quedó promovido a `TASK-0573` para convertir la duplicación de APUs en un flujo editable inmediato y con descarte seguro del borrador copiado.
- El siguiente ajuste operativo quedó promovido a `TASK-0574` para ejecutar Alembic y cerrar el bootstrap inicial de categorías exclusivas de `Sistema` en Marketplace clásico.
- El siguiente slice funcional quedó promovido a `TASK-0575` para completar el ciclo de vida de publicaciones Marketplace con clonación y retiro definitivo desde gestión vendedor.
- El siguiente frente UX quedó promovido a `TASK-0576` para añadir filtros operativos de lectura en seller y administración del Marketplace clásico.
- El siguiente frente operativo quedó promovido a `TASK-0577` para sembrar el catálogo inicial de productos exclusivos de `Sistema` en Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0578` para reforzar la presentación comercial de productos `Sistema` en la portada y el detalle de Tienda.
- El siguiente frente UX quedó promovido a `TASK-0579` para mejorar la trazabilidad operativa de ventas y pedidos en seller y administración Marketplace clásica.
- El siguiente frente operativo quedó promovido a `TASK-0580` para añadir una consola administrativa de pedidos globales en Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0581` para añadir detalle administrativo expandible de pedidos dentro del dashboard global de Marketplace.
- El siguiente frente UX quedó promovido a `TASK-0582` para reforzar el dashboard admin de Marketplace con métricas operativas reales.
- El siguiente frente UX quedó promovido a `TASK-0583` para añadir insights comerciales de mix y compradores activos en la administración Marketplace.
- El siguiente frente UX quedó promovido a `TASK-0584` para añadir alertas operativas y lectura por canal comercial en la administración Marketplace.
- El siguiente frente UX quedó promovido a `TASK-0585` para añadir una bandeja prioritaria de moderación en la administración Marketplace.
- El siguiente frente UX quedó promovido a `TASK-0586` para añadir un centro de mando de moderación en la administración Marketplace.
- El siguiente frente UX quedó promovido a `TASK-0587` para añadir una vista previa administrativa de publicaciones pendientes en la administración Marketplace.
- El siguiente frente UX quedó promovido a `TASK-0588` para añadir observaciones administrativas persistentes a la moderación Marketplace.
- El siguiente frente UX quedó promovido a `TASK-0589` para mostrar observaciones administrativas dentro del panel vendedor Marketplace.
- El siguiente frente operativo quedó promovido a `TASK-0590` para ejecutar la migración Alembic de `admin_notes` en Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0591` para añadir una bandeja seller de publicaciones observadas en Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0592` para reforzar la corrección guiada de publicaciones observadas dentro del flujo vendedor Marketplace.
- El siguiente frente UX quedó promovido a `TASK-0593` para consolidar un seguimiento seller de publicaciones reenviadas a revisión en Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0594` para dar cierre operativo al workflow seller de moderación con métricas y señalización resumida en Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0595` para reforzar la recirculación seller desde estados de moderación hacia acciones comerciales dentro de Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0596` para reforzar la gestión operativa de productos aprobados dentro del flujo seller de Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0597` para reforzar la gestión seller de productos pausados o inactivos dentro del Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0598` para reforzar la lectura seller de productos pendientes nuevos dentro del Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0599` para reforzar la capa ejecutiva seller del pipeline comercial Marketplace con señales más compactas por estado.
- El siguiente frente UX quedó promovido a `TASK-0600` para consolidar la portada seller de Marketplace clásico como cockpit comercial completo.
- El siguiente frente UX quedó promovido a `TASK-0601` para reforzar la portada buyer de Marketplace clásico con una lectura comercial equivalente.
- El siguiente frente UX quedó promovido a `TASK-0602` para reforzar la cohesión comercial de la portada principal de Tienda clásica.
- El siguiente frente UX quedó promovido a `TASK-0603` para revisar el detalle de producto de Marketplace clásico con la misma lógica cockpit comercial.
- El siguiente frente UX quedó promovido a `TASK-0604` para reforzar la experiencia operativa de pedidos Marketplace clásico tras la compra.
- El siguiente frente UX quedó promovido a `TASK-0605` para reforzar la lectura operativa del historial de pedidos dentro del dashboard buyer de Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0606` para reforzar la lectura operativa de la biblioteca adquirida dentro del dashboard buyer de Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0607` para consolidar el cockpit buyer del Marketplace clásico tras reforzar pedidos y biblioteca.
- El siguiente frente UX quedó promovido a `TASK-0608` para pulir estados vacíos y bordes de uso dentro del cockpit buyer del Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0609` para pulir el acabado visual y la continuidad fina del cockpit buyer del Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0610` para revisar la madurez operativa final del cockpit buyer del Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0611` para decidir el cierre o continuidad menor del frente buyer en Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0612` para revisar el cierre transversal y endurecimiento final del Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0613` para revisar y cerrar asimetrías residuales del Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0614` para decidir el checkpoint de cierre global del Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0615` para validar el cierre transversal final y decidir el punto de estabilización operativa del Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0616` para definir el criterio final de estabilización y cierre del Marketplace clásico.
- El siguiente frente quedó promovido a `TASK-0617` para mover Marketplace clásico a backlog incremental tras su estabilización operativa.
- El siguiente frente quedó promovido a `TASK-0618` para continuar mejoras incrementales de operación comercial en Marketplace clásico.
- El siguiente frente quedó promovido a `TASK-0619` para seguir afinando continuidades operativas incrementales del Marketplace clásico.
- El siguiente frente quedó promovido a `TASK-0620` para seguir afinando micro-recuperaciones UX incrementales del Marketplace clásico.
- El siguiente frente quedó promovido a `TASK-0621` para continuar mejoras incrementales de accionabilidad fina en Marketplace clásico.
- El siguiente frente quedó promovido a `TASK-0622` para continuar micro-mejoras incrementales de navegación y foco en Marketplace clásico.
- El siguiente frente quedó promovido a `TASK-0623` para continuar micro-mejoras incrementales de continuidad contextual en Marketplace clásico.
- El siguiente frente quedó promovido a `TASK-0624` para continuar micro-mejoras incrementales de continuidad operativa en Marketplace clásico.
- El siguiente frente quedó promovido a `TASK-0625` para abrir la fase de pulido gráfico y funciones adicionales tras el cierre estructural de Marketplace clásico.
- El siguiente frente operativo quedó promovido a `TASK-0626` para persistir el estado guardado de `RENDIMIENTO GLOBAL` dentro del editor clásico de APUs.
- El siguiente frente UX quedó promovido a `TASK-0627` para formalizar el plan integral de remodelación visual de Tienda clásica y organizar sus slices de implementación.
- El siguiente frente UX quedó promovido a `TASK-0628` para ejecutar la base visual compartida y el rediseño de portada pública y detalle de producto de Marketplace clásico.
- El siguiente frente UX quedó promovido a `TASK-0629` para aplicar la nueva gramática visual de Tienda al buyer dashboard y a la experiencia postcompra.
- El siguiente frente UX quedó promovido a `TASK-0630` para continuar la remodelación integral sobre seller dashboard y backoffice comercial.
- El siguiente frente UX queda preparado en `TASK-0631` para continuar la remodelación integral sobre administración marketplace y gobierno visual.
- El siguiente frente UX queda promovido a `TASK-0632` para cerrar el pulido transversal responsive, consistencia y remates visuales de Tienda clásica.
- La remodelación visual integral de Tienda clásica queda cerrada con `TASK-0632`, quedando lo siguiente ya en refinamiento incremental o funciones adicionales.
- El siguiente slice correctivo queda promovido a `TASK-0774` y `TASK-0775` para restaurar la impersonación operativa de `superadministración` por empresa activa en Tienda y reactivar la excepción beta de `Santiago Bermeo` hacia `Panel Administrador`.
- El siguiente frente UX queda promovido a `TASK-0776` y `TASK-0777` para alinear `Mis compras` y `Mis ventas` con la gramática visual ya consolidada en `Tienda > Panel Administrador`.
- El siguiente ajuste de continuidad queda promovido a `TASK-0778` para cerrar el comportamiento funcional de `Recientes` y unificarlo con la interacción rápida del storefront clásico.
- El siguiente frente funcional queda promovido a `TASK-0779`, `TASK-0780`, `TASK-0781`, `TASK-0782` y `TASK-0783` para convertir `Mis compras` y `Mis ventas` en superficies reales de trabajo basadas en listados y modales, manteniendo el look & feel del marketplace clásico.
- El saneamiento ya avanza hacia el patrón correcto: buyer prioriza bandejas + modales y seller empieza a apagar la duplicación de listados largos bajo el panel para acercarse al comportamiento real del `Panel Administrador`.
- El siguiente frente UX queda promovido a `TASK-0633` para endurecer estados de carga y continuidad visual dentro del Marketplace clásico ya remodelado.
- El siguiente frente UX queda promovido a `TASK-0634` para añadir favoritos persistidos y continuidad comercial sobre el catálogo clásico de Tienda.
- El siguiente frente UX queda promovido a `TASK-0635` para añadir comparación de productos y reforzar la decisión comercial dentro del Marketplace clásico.
- El siguiente frente UX queda promovido a `TASK-0636` para añadir productos vistos recientemente y reforzar la continuidad comercial dentro del Marketplace clásico.
- El siguiente frente UX queda promovido a `TASK-0637` para reorientar Tienda clásica hacia una arquitectura storefront tipo hardware store.
- El siguiente frente UX queda promovido a `TASK-0638` para ejecutar el primer slice real de esa reorientación sobre la home pública de Marketplace clásico.
- El siguiente frente UX queda promovido a `TASK-0644` para estructurar la zona exclusiva de superadministradores como centro integral de control clásico.
- El siguiente frente UX queda promovido a `TASK-0645` para añadir torres operativas y acción rápida dentro de la zona exclusiva de superadministración.
- El siguiente frente UX queda promovido a `TASK-0646` para añadir playbooks prioritarios dentro de la consola global de superadministración.
- El siguiente frente UX queda promovido a `TASK-0647` para corregir la ubicación funcional entre `Administración Global` y `Tienda` respecto a capacidades exclusivas de `superadministrador`.
- El siguiente frente UX queda promovido a `TASK-0648` para resetear y sanear el frontend de `Tienda` desde su storefront público, documentando además el inventario funcional real del módulo.
- El siguiente frente UX queda promovido a `TASK-0649` para ejecutar el primer slice de esa migración, rehaciendo la página inicial de ventas de `Marketplace` bajo una arquitectura de catálogo + sidebar comercial.
- El siguiente frente UX queda promovido a `TASK-0650` para corregir la densidad responsive real de la nueva storefront de `Tienda` en escritorio, especialmente para `1920x1080`.
- El siguiente frente UX queda promovido a `TASK-0651` para alinear la storefront de `Tienda` a un header contextual tipo módulo GiProy.
- El siguiente frente UX queda promovido a `TASK-0652` para retirar de la storefront el bloque superior redundante de operación exclusiva de `Sistema`.
- El siguiente frente UX queda promovido a `TASK-0653` para restaurar el scroll natural de la storefront de `Tienda` dentro del layout clásico.
- El siguiente frente UX queda promovido a `TASK-0654` para convertir la storefront pública de `Tienda` en un listado sin fin con carga visual progresiva y precache del siguiente tramo.
- El siguiente frente UX queda promovido a `TASK-0655` para fijar la banda de accesos del storefront por rol y abrir placeholders iniciales de compras, ventas y panel administrador con excepción beta controlada.
- El siguiente frente UX queda promovido a `TASK-0656` para alinear la escala de los botones superiores del storefront al tamaño real de los botones del sistema.
- El siguiente frente UX queda promovido a `TASK-0657` para alinear la banda superior del storefront al tamaño y patrón visual de los selectores de filtros.
- El siguiente frente UX queda promovido a `TASK-0658` para retirar de la home pública los paneles placeholder visibles y dejar solo la banda superior de accesos por rol.
- El siguiente frente operativo queda promovido a `TASK-0659` para ampliar el catálogo de muestra de `Sistema` en Tienda a un set de al menos `20` items idempotentes.
- El siguiente frente UX queda promovido a `TASK-0660` para llevar la storefront a una densidad profesional 4x3 en escritorio amplio, rebajando tipografía y gigantismo visual.
- El siguiente frente UX queda promovido a `TASK-0661` para fijar el header y el subheader de búsqueda/ordenación de la storefront durante todo el scroll.
- El siguiente frente UX queda promovido a `TASK-0662` para redefinir explícitamente la búsqueda superior de Tienda como filtro rápido de catálogo.
- El siguiente frente UX queda promovido a `TASK-0663` para castellanizar los rótulos públicos residuales de la storefront de Tienda.
- El siguiente frente operativo queda promovido a `TASK-0697` para endurecer el creador/editor administrativo y permitir solo categorías fijas vigentes dentro de Tienda.
- El siguiente frente UX queda promovido a `TASK-0664` para retirar de la sidebar el bloque redundante de control comercial de Sistema.
- El siguiente frente UX queda promovido a `TASK-0665` para incorporar carrito en el header del storefront, validación de facturación y confirmación final de compra.
- El siguiente frente UX queda promovido a `TASK-0666` para mejorar el selector de precio con rango mínimo, máximo y reset sobre el mismo slider.
- El siguiente frente UX queda promovido a `TASK-0667` para adaptar el carrito y checkout del storefront a productos unitarios y productos vendibles por paquete, dejando además preparada la estructura comercial futura de producto.
- El siguiente frente UX queda promovido a `TASK-0668` para alinear precio y acción en las cards del storefront y añadir indicación discreta de IVA incluido.
- El siguiente frente UX queda promovido a `TASK-0669` para retirar el panel visual del scroll progresivo y hacer que la storefront renderice completo el catálogo pequeño actual.
- El siguiente frente UX queda promovido a `TASK-0670` para reestructurar las cards del storefront por zonas fijas y conseguir alineación real independientemente de la longitud del texto.
- El ajuste de alineación real de cards quedó materializado en `TASK-0670`, fijando zonas internas rígidas para metadato, título y pie comercial dentro del storefront.
- El siguiente frente funcional queda promovido a `TASK-0671` para abrir la primera función real del `Panel Administrador` de Tienda mediante gestión completa de categorías.
- El siguiente frente funcional queda promovido a `TASK-0672` para dejar `Productos` preparado como segundo bloque del `Panel Administrador`, todavía en modo placeholder controlado.
- El siguiente frente funcional queda promovido a `TASK-0673` para convertir `Productos` en un listado global inicial de todos los artículos del marketplace dentro del `Panel Administrador`.
- El siguiente frente funcional queda promovido a `TASK-0674` para convertir el listado global de `Productos` en una gestión operativa inicial con edición, activación/desactivación y borrado.
- El siguiente frente funcional queda promovido a `TASK-0675` para añadir creación de productos dentro del `Panel Administrador` con selección previa obligatoria de tipo.
- El saneamiento visual del `Panel Administrador` quedó materializado en `TASK-0676`, realineando la superficie al patrón de `Ajuste SaaS` con solo dos funciones activas: `Categorías` y `Productos`.
- El ajuste fino del `Panel Administrador` quedó continuado en `TASK-0677`, sustituyendo la navegación lateral por paneles tipo submódulo, con header fijo y desplazamiento por rueda sobre las opciones.
- El creador/editor específico de `Portal de compras públicas` quedó materializado en `TASK-0678`, con modal propio, metadatos comunes, metadatos exclusivos y base preparada para importador posterior.
- La corrección funcional del `Panel Administrador` quedó materializada en `TASK-0679`, moviendo `Categorías` y `Productos` a submódulos modales reales lanzados desde sus paneles.
- El saneamiento final del patrón modal del `Panel Administrador` quedó materializado en `TASK-0680`, dejando apertura exclusiva por submódulo y eliminando estado residual de panel interno.
- La realineación visual de los lanzadores del `Panel Administrador` quedó materializada en `TASK-0681`, sustituyendo el carril horizontal por paneles en retícula acordes al patrón de `Ajuste SaaS`.
- La corrección del submódulo `Productos` quedó materializada en `TASK-0682`, sustituyendo la superficie improvisada por un listado real con creación desde icono desplegable y acciones por fila.
- La compactación visual de la entrada del `Panel Administrador` quedó materializada en `TASK-0683`, corrigiendo el gigantismo de header y tarjetas de submódulo.
- La compactación real de los modales del submódulo `Productos` quedó materializada en `TASK-0684`, rebajando la escala del editor genérico y del creador/editor de `Portal de compras públicas`.
- El siguiente frente funcional queda promovido a `TASK-0685` para planificar la modalidad híbrida de `Portal de compras públicas`, integrando importador, entrega postcompra y política inicial del proyecto adquirido.
- El plan de `TASK-0685` quedó endurecido para dejar explícita la absorción total de la lógica del importador externo y su retirada como herramienta operativa separada.
- El primer slice integrado de esas mejoras quedó materializado en `TASK-0691`, incorporando versión técnica, validación fuerte, visor de previo y entrega inicial como proyecto adquirido.
- El segundo slice integrado de esas mejoras quedó materializado en `TASK-0692`, incorporando análisis técnico desde archivo base, persistencia del importador y exportación Excel descargable desde el pedido.
- El tercer slice integrado de esas mejoras quedó materializado en `TASK-0693`, sembrando presupuesto base y EDT mínima dentro del proyecto adquirido de `Portal de compras públicas`.
- El cuarto slice integrado de esas mejoras quedó materializado en `TASK-0694`, enriqueciendo el análisis técnico del importador con lectura por capítulos y mejor previo visual para administración.
- La política común de publicación temporal quedó materializada en `TASK-0695`, haciendo obligatorias las fechas de inicio y fin de publicación para todos los artículos y alineando `Portal de compras públicas` a esa misma regla.
- El catálogo demo de Tienda quedó reajustado en `TASK-0696`, dejando todos los artículos de prueba con inicio de publicación en ayer, fin variable por artículo y certificación explícita de la política temporal efectiva.
- El siguiente frente UX queda promovido a `TASK-0698` para mover la activación de `Portal de compras públicas` al header del modal mediante slider, junto a `Código` y `Creador`.
- El siguiente frente funcional queda promovido a `TASK-0699` para hacer derivado y no editable el precio comercial de `Portal de compras públicas` a partir del `Precio de licitación`.
- El siguiente frente funcional queda promovido a `TASK-0700` para permitir borradores persistibles de `Portal de compras públicas`, bloqueando la activación hasta completar todos los descriptores requeridos.
- El siguiente frente UX queda promovido a `TASK-0701` para mostrar los tramos automáticos de `Precio de licitación` mediante la ayuda contextual propia del sistema dentro del modal de `Portal de compras públicas`.
- El siguiente frente UX queda promovido a `TASK-0702` para guiar el intento de activación bloqueada hacia el primer campo faltante dentro del modal de `Portal de compras públicas`.
- El siguiente frente UX queda promovido a `TASK-0703` para universalizar la señal de borrador incompleto y aplicarla en el listado global mediante estado híbrido `Inactivo` + microindicador ámbar.
- El siguiente frente UX queda promovido a `TASK-0704` para exponer `Borrador` como estado visible y filtro dedicado dentro del listado global de productos.
- El siguiente frente UX queda promovido a `TASK-0705` para alinear semánticamente `Activo / Borrador / Inactivo` con la regla real de publicación del storefront.
- El siguiente frente operativo queda promovido a `TASK-0706` para forzar autoaprobación de toda edición hecha por `superadministrador` durante la fase inicial del marketplace.
- El siguiente frente UX queda promovido a `TASK-0707` para blindar el cierre del modal de `Portal de compras públicas` y eliminar salidas abruptas sin confirmación.
- El siguiente frente UX queda promovido a `TASK-0708` para convertir la confirmación de cierre del portal en una decisión de tres salidas con retorno explícito al editor.
- El siguiente frente funcional queda promovido a `TASK-0709` para normalizar correctamente el pegado de montos en `Precio de licitación`.
- El siguiente frente UX queda promovido a `TASK-0710` para aplicar trim automático a cualquier texto pegado en los formularios del creador/editor.
- El siguiente frente funcional queda promovido a `TASK-0712` para excluir `Precio de licitación` del trim global y dejar ese pegado bajo la normalización monetaria específica del portal.
- El siguiente frente funcional queda promovido a `TASK-0713` para separar en backend la política de guardar borrador incompleto de la política de activación/publicación completa.
- El siguiente frente funcional queda promovido a `TASK-0714` para normalizar el pegado con timestamp en las fechas del portal y compactar sus etiquetas visibles para evitar descuadres.
- El siguiente frente UX queda promovido a `TASK-0715` para refinar la semántica del estado administrativo y tratar como `Borrador` a los productos vigentes no activados.
- El siguiente frente UX queda promovido a `TASK-0716` para unificar `Código de licitación` con la referencia técnica del importador y endurecer su pegado.
- El siguiente frente UX queda promovido a `TASK-0717` para sustituir `proyecto` por `GIPROY` en las etiquetas visibles de modalidad de entrega del portal.
- El siguiente frente UX queda promovido a `TASK-0718` para evitar el recorte visual del panel explicativo de tramos de precio dentro del modal del portal.
- El siguiente frente UX queda promovido a `TASK-0719` para retirar paneles informativos redundantes del editor clásico de portal.
- El siguiente frente funcional queda promovido a `TASK-0720` para permitir varios archivos PDF y/o Excel en la importación del portal y aclarar esa UX.
- El frente de planificación arquitectónica queda promovido a `TASK-0721` para consolidar el plan maestro de integración genérica del `Portal de compras públicas` y absorber los cuatro escenarios documentales reales.
- El siguiente slice de diseño queda promovido a `TASK-0722` para clasificar documentos por evidencias en lugar de por plantilla fija.
- El siguiente slice de diseño queda promovido a `TASK-0723` para definir el `analysis_bundle` canónico y la fusión genérica de fuentes.
- El siguiente slice de diseño queda promovido a `TASK-0724` para preparar la extracción tolerante a variaciones sobre `pdf`, `xls` y `xlsx`.
- El siguiente slice de UX queda promovido a `TASK-0725` para rediseñar el bloque de importación con composición de bundle y corrección manual asistida.
- El siguiente slice de política queda promovido a `TASK-0726` para fijar niveles de completitud documental y degradación funcional.
- El siguiente slice técnico queda promovido a `TASK-0727` para definir la generación automática de APUs `Pendiente` a partir de `solo presupuesto`.
- El siguiente slice técnico queda promovido a `TASK-0728` para soportar formalmente `APUs anidados` como insumos referenciados.
- El siguiente slice de salida queda promovido a `TASK-0729` para separar los contratos de generación `GIPROY` y `Excel`, incluyendo `Cod. Referencial`, localización y geolocalización automática en `GIPROY`.
- El siguiente slice de trazabilidad queda promovido a `TASK-0730` para marcar proyectos y bases de trabajo generados desde compras públicas.
- El siguiente slice de licenciamiento queda promovido a `TASK-0731` para imponer la validación de propiedad empresarial en generación, apertura y uso.
- La primera materialización del nuevo plan queda ya cubierta por `TASK-0722`, `TASK-0723` y `TASK-0727`, incorporando clasificación por evidencias, `analysis_bundle` canónico visible y bootstrap de APUs `Pendiente` para `solo presupuesto`.
- Se corrige además una regresión de edición en el modal clásico del portal: `openPortalEditModal` deja de invocar el setter residual `setPortalImportFile` y vuelve a limpiar correctamente `portalImportFiles`, restaurando la apertura del editor.
- El siguiente tramo del importador queda ya materializado en `TASK-0724` y parcialmente en `TASK-0725`, añadiendo extracción técnica inicial de APUs/recursos y su exposición en el preview administrativo del portal.
- `TASK-0725` queda ampliada además con corrección manual asistida del rol por archivo, de modo que el superadministrador pueda reclasificar fuentes y relanzar el análisis sin depender por completo de la detección automática.
- `TASK-0728` queda ya iniciada con resolución básica de `APUs anidados` dentro del bundle y exposición visible de enlaces anidados detectados.
- `TASK-0729` queda ya materializada en la salida clásica del portal: `GIPROY` recibe `Cod. Referencial`, localización y geolocalización automática en `Datos de proyecto`, y la preview del editor anticipa ya ese sembrado sin alterar la salida `Excel`.
- `TASK-0730` y `TASK-0731` quedan ya iniciadas en backend clásico: los activos entregados por compra quedan marcados con procedencia y ownership, los proyectos comprados refuerzan su traza en `plantillas_config` y la apertura de proyectos/bases ya revalida la empresa propietaria en los endpoints sensibles.
- `TASK-0724`, `TASK-0725` y `TASK-0726` quedan ampliadas con conciliación incremental por firmas en el bundle multiarchivo: el importador ya deduplica rubros/APUs/recursos/VAE entre fuentes solapadas y la UX clásica expone cuántos elementos fueron conciliados.
- `TASK-0726` queda reforzada además con cobertura semántica presupuesto/APUs: el bundle informa ya cuánto presupuesto está respaldado por APUs reales o pendientes y qué rubros siguen sin correspondencia técnica visible.
- `TASK-0723` y `TASK-0725` quedan reforzadas con reimportación incremental: al volver a analizar nuevas fuentes, el portal reaprovecha el `import_analysis` existente como base y lo enriquece en lugar de descartarlo.
- `TASK-0727` y `TASK-0728` quedan ahora reforzadas con promoción bootstrap->real: cuando llegan APUs reales compatibles, el bundle absorbe esos APUs sobre la identidad de los APUs pendientes generados desde presupuesto y expone cuántos fueron promovidos.
- `TASK-0728` queda afinada además con anidaciones pendientes: el bundle ya diferencia entre enlaces anidados resueltos y referencias APU todavía no enlazadas, y la UX clásica las hace visibles.
- `TASK-0724` queda afinada además en `VAE / desagregación`: el extractor ya diferencia cabeceras, detalles, porcentajes, formularios y resúmenes para mejorar la lectura de formatos heterogéneos.
- `TASK-0724`, `TASK-0726` y `TASK-0728` quedan afinadas además con conciliación semántica ligera: presupuesto, APUs y referencias anidadas ya toleran mejor variantes de descripción y la UX clásica expone cuántas coincidencias se resolvieron por esa vía.
- `TASK-0728` queda endurecida además frente a falsos positivos: cuando una referencia anidada encaja con varios APUs por semántica, el bundle la marca como ambigua en vez de enlazarla de forma arbitraria y la UX clásica la hace visible.
- `TASK-0724` queda afinada además para documentos hispanos reales: la conciliación semántica pliega tildes y variantes latinas antes de comparar, reduciendo falsos negativos entre descripciones equivalentes.
- `TASK-0726` queda cerrada además con autodiagnóstico del bundle: cobertura parcial, anidaciones ambiguas, referencias pendientes y ausencia de recursos suficientes generan warnings ejecutivos sin depender de lectura manual de métricas.
- El siguiente frente UX queda promovido a `TASK-0732` para separar estructuralmente `Configuración híbrida`, `Fuente documental` y `Resultado técnico` dentro del editor clásico del portal.
- El siguiente frente UX queda promovido a `TASK-0733` para rediseñar `Archivos técnicos de entrada` como flujo claro de carga multiarchivo.
- El siguiente frente UX queda promovido a `TASK-0734` para sustituir el estado residual actual por un estado vacío limpio y una lista operativa de archivos cargados.
- El siguiente frente UX queda promovido a `TASK-0735` para separar visualmente la entrada documental del análisis técnico profundo.
- `TASK-0732`, `TASK-0733`, `TASK-0734` y `TASK-0735` quedan ya materializadas en la UI clásica: el editor separa configuración, fuente documental y resultado técnico, reorganiza la carga multiarchivo y sustituye el estado residual por una lista operativa de fuentes.
- `TASK-0735` queda afinada además con compactación visual de `Resultado técnico`: la lectura ejecutiva pasa al primer plano y las métricas secundarias se agrupan en `Lectura estructural` y `Calidad del bundle`, tanto en editor como en vista previa.
- `TASK-0732` queda afinada además por limpieza de superficie: se retiran del bloque `Configuración híbrida` los indicadores/controles derivados que no aportaban una acción real en esta fase, incluyendo `Origen técnico`.
- `TASK-0732` queda afinada además con compactación del bloque superior: menor densidad tipográfica en el encabezado y disposición en una sola fila de `Código / referencia técnica` y `Modalidad de entrega`.
- `TASK-0732` queda afinada además en proporción y peso visual: los edits del bloque superior rebajan su peso tipográfico a `font-medium`, `Código / referencia técnica` gana más ancho y `Modalidad de entrega` se estrecha para reflejar su naturaleza temporal.
- `TASK-0732` queda afinada además sustituyendo el desplegable nativo de `Modalidad de entrega` por un menú propio del modal para recuperar coherencia visual con el sistema clásico.
- `TASK-0733` queda afinada además por limpieza operativa de `Fuente documental`: se retiran labels explicativos y cartela de estado, `Archivos cargados` pasa al centro del flujo y `Analizar` baja debajo del listado.
- `TASK-0733` queda afinada además corrigiendo la carga múltiple real: nuevas selecciones se acumulan sin duplicarse, el selector visual se reduce a un actuador con icono y se eliminan las cápsulas `PDF/XLS/XLSX` que ya no aportaban valor.
- `TASK-0733` queda endurecida además con una solución completa del picker: el importador abandona el patrón frágil `label + input` y pasa a `input` oculto con `ref`, click explícito, merge centralizado y re-selección segura.
- `TASK-0733` queda blindada además contra caída de la UI al analizar: `import_analysis` se normaliza antes de entrar en estado para filtrar entradas nulas y asegurar shape mínimo en `source_files`, `chapter_breakdown`, `generated_apus` y `analysis_bundle`.
- `TASK-0733` queda blindada además contra `422` estructurados: el detalle del backend se convierte a texto antes de entrar en `appAlert`, evitando que React vuelva a intentar renderizar objetos como hijos.
- `TASK-0733` queda cerrada además por compatibilidad de contrato multipart: el backend acepta ya tanto `files` como `file`, eliminando el desacople que estaba disparando `body > file: Field required`.
- `TASK-0733` queda cerrada además desde frontend con un contrato estable: el cliente publica cada archivo solo en `file`, que ya es compatible con el backend actual y evita duplicar payloads durante el preview.
- `TASK-0733` queda blindada además contra cuelgues operativos: el preview del importador usa timeout cliente ampliado y, si el backend no responde, el estado `Analizando` se libera con un mensaje explícito.
- `TASK-0733` queda afinada además en diagnóstico: cuando el backend devuelve `message/details` en un `500`, la UI lo expone como texto legible en vez de colapsar en un mensaje genérico.
- `TASK-0732` queda afinada además para `1920x1080`: labels de formulario, títulos de bloque, pills y botones del editor de portal rebajan peso y tracking para evitar una lectura demasiado dura en escritorio amplio.
- El siguiente frente UX queda promovido a `TASK-0736` para convertir la ventana de confirmación de importación en una confirmación operativa y no en un dashboard técnico.
- El siguiente frente UX queda promovido a `TASK-0737` para hacer visible el estado de persistencia del análisis dentro de la confirmación.
- El siguiente frente UX queda promovido a `TASK-0738` para simplificar el preview a un resumen ejecutivo de negocio y decisión.
- El siguiente frente UX queda promovido a `TASK-0739` para mover el diagnóstico técnico profundo a un detalle colapsable.
- `TASK-0736`, `TASK-0737`, `TASK-0738` y `TASK-0739` quedan ya materializadas en la ventana clásica de confirmación: estado claro de importación, persistencia explícita, resumen ejecutivo útil y detalle técnico secundario.
- El siguiente frente funcional queda promovido a `TASK-0740` para conectar la modalidad `Excel + GIPROY` con una descarga previa real en la confirmación y con la plantilla oficial `Formato de salida-entrega.xlsx`.
- `TASK-0740` queda ya materializada: la confirmación del portal permite descargar el Excel de salida cuando la modalidad lo exige y el backend lo genera a partir de la plantilla oficial de entrega.
- El siguiente frente UX queda promovido a `TASK-0741` para reducir `Resultado técnico` a un resumen mínimo y útil dentro del editor del portal.
- El siguiente frente de protección operativa queda promovido a `TASK-0742` para confirmar reemplazos de fuente documental cuando ya existe análisis asociado.
- El siguiente frente de cierre de flujo queda promovido a `TASK-0743` para retirar `Revisar previo` y sustituirlo por `Guardar Excel` cuando la modalidad lo requiera.
- `TASK-0741`, `TASK-0742` y `TASK-0743` quedan ya materializadas: el editor del portal muestra solo el resumen técnico definido por negocio, protege la recarga de fuentes ya analizadas con el diálogo propio del sistema y expone `Guardar Excel` en lugar de `Revisar previo` cuando corresponde.
- El listado global administrativo vuelve a filtrar por `product_type`: se añade el selector `Tipo` y `Portal de compras públicas` deja de desaparecer falsamente al aplicar ese criterio.
- `TASK-0740` queda además endurecida por compatibilidad entre versiones: `Guardar Excel` puede resolver ya por la ruta nueva `excel-preview` o reutilizando la ruta estable de `preview` con `output_kind=excel`.
- Los productos `Portal de compras públicas` quedan además protegidos contra categorías nulas: el guardado fuerza su categoría fija, y tienda/admin reparan de forma automática los portales ya existentes sin `category_id`.
- El storefront clásico queda además preparado para usar imágenes reales de portada en la cabecera de las cards, conservando el degradado como fallback cuando el producto aún no tenga imagen definida.
- El Excel de salida del `Portal de compras públicas` queda además realineado a la nueva plantilla `APU_Presupuesto_Completo.xlsx`, exportando el bundle completo sin truncado artificial y conservando la lógica formulada entre `PRESUPUESTO`, `DESAGREGACIÓN CONSOLIDADA`, `INDICE_APUs` y todas las hojas `APU_*`.
- El storefront clásico queda además normalizado sobre una `meta común` pública: las cards dejan de repetir título en cuerpo, soportan descuento comercial, distinguen el `Monto de licitación` del precio de venta en portales y abren un modal público de detalle con CTA de carrito.
- La zona común del editor genérico queda además alineada con la del portal: primero se edita la `meta común del producto` y después la `configuración del producto`, evitando que el modelo unificado se quede resuelto solo en el storefront.
- El modal público de detalle queda además endurecido para responsive real: fullscreen en móvil, caja flotante en desktop y hero comercial mejor truncado sin romper la estética del storefront clásico.
- El siguiente frente queda formalizado además como arquitectura de pagos, postcompra y gobierno comercial del marketplace clásico, dejando prevista la futura entrada de administradores como vendedores parciales sin implementar todavía ese slice.
- Ese frente de pagos queda ya consolidado documentalmente en `docs/marketplace-payments-governance-execplan.md` y descompuesto en `TASK-0755` a `TASK-0764`, incluyendo `checkout_draft`, `payment_attempt`, `PaymentReadiness`, restricción de compra a administradores de empresa, carrito persistente y compatibilidad futura con vendedores parciales.
- El mismo frente queda ampliado además con política de devolución selectiva: `TASK-0765`, `TASK-0766` y `TASK-0767` fijan devolución automática de `30 minutos` para `APUs`, `Proyectos` y `Bases Maestras`, sin devolución automática en el resto del catálogo y con excepción manual desde `Ventas Sistema` por `superadministración`.
- El mismo frente queda ampliado además con política de carrito abandonado: `TASK-0768` fija que todo carrito con más de `7 días` sin actividad se considere abandonado y sea limpiado automáticamente sin afectar pedidos ni pagos ya formalizados.
- El mismo frente queda ampliado además con una mejora UX de continuidad comercial: `TASK-0769` fija un recordatorio sutil al iniciar para administradores de empresa cuando exista carrito persistente válido.
- Antes de implementar, el mismo frente queda endurecido además con matriz de estados, jobs temporales, auditoría mínima, política de tiempo/redondeo, feature flags y criterio MVP para no abrir pagos sobre una base frágil.
- Como primer slice codificado del frente, `TASK-0756`, `TASK-0768` y `TASK-0769` quedan materializadas: compra restringida a administradores de empresa, carrito persistente con abandono a `7 días` y recordatorio sutil de carrito al iniciar para compradores autorizados.
- `TASK-0755` queda ya iniciada con núcleo real de pagos: tablas y migración para `checkout_draft`, `payment_attempt` y `payment_event`, más endpoints base de drafts sin desactivar todavía el checkout inmediato clásico.
- `TASK-0757` y `TASK-0758` quedan ya iniciadas con un primer slice visible: el panel admin incorpora `Formas de pago`, backend siembra y valida readiness de métodos base, y el carrito clásico ya puede seleccionar método y preparar `checkout_draft` por fases sin romper el checkout legado.
- `TASK-0761` queda ya iniciada con el primer método real: `Transferencia bancaria` registra pedidos pendientes de validación manual, captura referencia obligatoria y evita la entrega prematura de activos.
- `TASK-0762` y `TASK-0763` quedan ya reforzadas en UX operativa: `Mis compras` muestra mejor método/estado/referencia de pago y `Ventas Sistema` gana filtros, observación inline y más contexto de validación manual.
- `TASK-0764` queda ya iniciada de forma explícita: los pedidos empiezan a exponer estado de pago y `fulfillment_status`, el detalle de pedido deja de asumir entrega automática y el checkout legado registra también trazabilidad mínima de intento/evento.
- `TASK-0765` y `TASK-0767` quedan ya iniciadas con la base de elegibilidad: los pedidos calculan ventana de devolución automática por categoría y esa señal ya se muestra tanto en `Mis compras` como en `Ventas Sistema`.
- `TASK-0766` queda ya iniciada con refund trazable y revocación lógica del acceso: se crea `marketplace_refunds`, el comprador puede ejecutar devolución automática en pedidos elegibles y superadministración puede forzar devoluciones manuales desde `Ventas Sistema`.
- `TASK-0759` queda ya iniciada con el primer proveedor online real: `PayPhone` prepara transacciones desde `checkout_draft`, abre la cajita de pagos en el storefront y confirma `id + clientTransactionId` desde backend antes de activar la compra.
- `TASK-0760` queda ya iniciada con el segundo proveedor online real: `PayPal` prepara checkout desde `checkout_draft`, crea la orden por backend, captura desde backend y activa la compra solo tras `COMPLETED`.
- `TASK-0755` y `TASK-0764` quedan además endurecidas temporalmente: los `checkout_draft` vencidos y los intentos online colgados ya pasan a `expired` antes de volver a preparar, crear o confirmar pagos.
- `TASK-0762` y `TASK-0763` ganan además una capa de conciliación visible: buyer y backoffice ya pueden leer una cronología compacta de eventos de pago y el último hito relevante por pedido.
- Como inciso operativo en `Precios Unitarios`, se crean y cierran `TASK-0770` y `TASK-0771`: `Recursos` ya incorpora buscador lateral de subcategorías alineado con la UX de `APUs`.
- `TASK-0761` gana además expiración operativa: las transferencias pendientes que no se revisan dentro del plazo dejan de seguir activas y pasan a `expired` con trazabilidad.
- `TASK-0755` y `TASK-0763` ganan además un housekeeping visible en backoffice: `Ventas Sistema` ya resume drafts abiertos, pagos online esperando confirmación, transferencias fuera de plazo y ventanas automáticas de devolución, y permite ejecutar el sweep operativo desde la consola.
- `TASK-0766` y `TASK-0767` ganan además trazabilidad visible de reembolso: buyer, detalle de pedido y `Ventas Sistema` ya exponen modo, motivo y fecha del refund, además del actor cuando aplica resolución manual.
- `TASK-0762`, `TASK-0763` y `TASK-0764` ganan además una unificación de lenguaje: buyer, detalle y backoffice ya resuelven método y estado de pago desde la misma semántica visual.
- El frente de pagos/postcompra del marketplace clásico queda además cerrado en esta fase: tasks `TASK-0755` a `TASK-0769` quedan materializadas o cerradas documentalmente, y buyer, detalle y `Ventas Sistema` ya exponen también la resolución operativa residual de cada refund según método de pago.
- Como inciso de saneamiento en `APUs`, `TASK-0772` y `TASK-0773` quedan materializadas para restaurar la persistencia inmediata del orden de recursos y blindar backend para que reportes y consumidores sigan leyendo ese mismo orden sin depender de payloads con `recurso_codigo`.
