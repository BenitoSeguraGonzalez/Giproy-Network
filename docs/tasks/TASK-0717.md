# TASK-0717.md

## Objetivo
Realinear la nomenclatura visible de `Modalidad de entrega` en `Portal de compras públicas`, sustituyendo la palabra `proyecto` por `GIPROY`.

## Implementación
- `frontend/src/pages/MarketplaceAdminDashboard.jsx` actualiza las etiquetas visibles de `PORTAL_DELIVERY_MODE_OPTIONS`.
- `Solo proyecto` pasa a `Solo GIPROY`.
- `Excel + proyecto` pasa a `Excel + GIPROY`.
- Los valores internos `project_only` y `excel_and_project` no cambian.

## Validación
- `npm run build`

## No interferencia BIM
- El ajuste se limita a texto visible del dashboard clásico de marketplace.
- No se activó ni modificó ninguna superficie BIM.
