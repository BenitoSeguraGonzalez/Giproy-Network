# TASK-0718.md

## Objetivo
Evitar que el panel explicativo de los tramos automáticos de `Precio de licitación` quede recortado dentro del modal clásico de `Portal de compras públicas`.

## Implementación
- `frontend/src/pages/MarketplaceAdminDashboard.jsx` reancla el panel flotante de `PortalPricingHelp` para que se abra hacia la izquierda del icono.
- Se añade un `max-width` defensivo para no desbordar pantallas estrechas.
- No se modifica la lógica de cálculo ni la política de precios.

## Validación
- `npm run build`

## No interferencia BIM
- El ajuste se limita al helper visual del portal en la capa clásica.
- No se activó ni modificó ninguna superficie BIM.
