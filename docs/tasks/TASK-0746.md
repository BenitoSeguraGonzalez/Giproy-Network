# TASK-0746 - Fórmulas vivas del Excel del portal sobre la nueva plantilla

## Objetivo
Mantener la lógica formulada del Excel del portal en lugar de sustituirla por valores fijos.

## Alcance
- `PRESUPUESTO` formulado contra `INDICE_APUs`.
- `DESAGREGACIÓN CONSOLIDADA` formulada contra `INDICE_APUs` y `APU_*`.
- `INDICE_APUs` formulado contra cada hoja `APU_*`.
- `APU_*` formuladas contra catálogos, parámetros y umbrales.

## Estado
Completado - 2026-03-30

## Resultado
- El libro mantiene referencias y fórmulas entre hojas como en la plantilla de negocio.
- La exportación queda utilizable como archivo técnico vivo y no como dump estático.
