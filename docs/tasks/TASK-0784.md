# TASK-0784

## Objetivo
Endurecer la sincronización `Base Maestra -> Base de Proyecto` para reparar APUs heredados vacíos o incompletos sin pisar contenido local válido.

## Estado
Completado.

## Alcance
- Revisar `sync-missing` y detectar por qué podía dejar APUs sin líneas pese a existir en destino.
- Añadir reparación conservadora de APUs heredados vacíos/incompletos.
- Exponer en la respuesta cuántos APUs heredados fueron reparados.
- Ajustar el feedback de UI en `BasesTrabajo`.

## Resultado
- `sync-missing` ya no se limita a crear faltantes por código.
- Los APUs heredados con estructura vacía pueden reconstruirse desde la Base Maestra.
- La UI informa también cuántos APUs heredados fueron reparados.
