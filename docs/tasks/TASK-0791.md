# TASK-0791

## Título
Endurecer EDT y vista gráfica jerárquica con foco activo compartido

## Objetivo
Evitar la regresión espejo en `EDT` y en el componente compartido `HierarchyGraphView`, alineando ambos al contrato `activeNodeId + selectedIds`.

## Estado
Completado.

## Alcance
- Aplicar el mismo patrón de foco activo en `EDT`.
- Permitir a `HierarchyGraphView` priorizar el foco activo sobre la selección múltiple.

## Resultado
- `EDO` y `EDT` comparten el mismo comportamiento estable.
- La vista gráfica mantiene coherencia aunque exista selección masiva en árbol.
