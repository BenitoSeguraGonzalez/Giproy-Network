# TASK-0807

## Título
Actualizar UI global de estado de licencia

## Objetivo
Alinear banner global, panel de licencias y consumo frontend con el snapshot nuevo de backend.

## Estado
Completado.

## Resultado
- El banner global ya representa `solo lectura` y `próxima licencia`.
- `Settings` muestra estado operativo, días de gracia y próxima licencia.
- `AdminGlobalLicencias` ya expone próxima licencia y lectura de `solo lectura` sin romper el panel existente.
