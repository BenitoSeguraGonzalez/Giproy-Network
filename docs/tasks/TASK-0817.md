# TASK-0817 - Exponer categoría base del APU en payload de reportes

## Estado
Completado

## Objetivo
Añadir al motor de reportes el dato de categoría base del APU para que pueda consumirse sin heurísticas en visor y PDF.

## Alcance
- Resolver la categoría base desde la subcategoría del APU.
- Incluir `categoria_base` en el payload de reportes APU.
- Mantener compatibilidad con el flujo actual de `Presupuesto + APUs`.

## Resultado
- Los items APU de reportes ya incluyen `categoria_base` en backend.
