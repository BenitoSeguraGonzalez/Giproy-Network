# TASK-0837 - Enriquecer API Y Persistencia De Cronograma De Trabajo

## Estado
Completado

## Objetivo
Mantener persistencia compatible con el JSON existente, añadiendo configuración global y overrides por línea.

## Resultado
- `schedule_data` sigue siendo compatible con el modelo previo.
- La configuración global se persiste sin romper clonación ni datos legacy.
- El endpoint `cronogramas-trabajo` ya devuelve un contrato operativo útil para frontend.
