# TASK-0849 - Optimizar Ruta Backend De Altas Y Pegado Masivo En Presupuesto

## Estado
Completado

## Objetivo
Reducir el coste de servidor al añadir APUs y al pegar cantidades desde portapapeles en `Presupuesto`.

## Resultado
- El alta de APU ya inserta en una sola operación, soporta `after_linea_id` y fusiona con línea existente cuando corresponde.
- El pegado masivo usa una ruta `bulk` real y evita recalculados completos por cada cantidad.
- `refresh_presupuesto_prices(...)` deja atrás consultas `N+1` y soporta ejecución más controlada.
- La medición directa del tramo backend del alta quedó alrededor de `0.06s`.
