# TASK-0854 - Documentar Capacidades Reales De Exportación MS Project Con Pywin32

## Estado
Completado

## Objetivo
Registrar en la documentación operativa que el carril `.mpp` de `Cronograma de Trabajo` ya depende de `pywin32 + Microsoft Project`, y no de una promesa abstracta de entorno premium.

## Resultado
- `CHANGELOG.md` y `project_state.json` recogen el nuevo estado del carril `.mpp`.
- Se documenta el cambio de PowerShell embebido a `pywin32`.
- Queda claro el contrato: Windows + Project + plantilla + `win32com` habilitan `.mpp`; en otro caso se usa XML.
