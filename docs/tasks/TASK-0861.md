# TASK-0861 - Corregir Pareto global para que analice solo partidas/APUs

## Estado
Completada

## Objetivo
Evitar que el modo hoy llamado `GLOBAL` mezcle capítulos con partidas, dejando el análisis de Pareto exclusivamente a nivel de líneas APU del presupuesto.

## Resultado
- `get_presupuesto_pareto(..., view="global")` ahora usa solo filas con `apu_id`.
- El total, ranking y acumulado de ese modo ya se calculan contra el universo de partidas/APUs.
