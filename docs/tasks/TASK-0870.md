# TASK-0870

## Título
Alinear el motor de reporting con el nuevo contrato de presentación de APUs

## Resultado
- `reporting.py` ahora alimenta `#TEXTOTOTAL` en hojas APU con el estilo esperado del ejemplo general.
- El presupuesto también usa el cuerpo del texto total sin duplicar el prefijo `Son:`.
- Toda generación Excel con presentación de APUs reutiliza las plantillas reconstruidas.
- El motor categorizado restaura cabeceras y filas placeholder por categoría antes de rellenar datos, evitando el patrón de salida `una línea bien / una línea mal`.
- El footer del APU se resincroniza al final para que `Costo directo`, `Indirectos`, `Total` y `Son:` queden estables tras mover bloques.
