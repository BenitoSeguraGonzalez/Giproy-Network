# TASK-0876 - Dejar smoke test de configuración de impresión

## Objetivo
Cubrir con una verificación ejecutable la ausencia de `INTERPRO` y la cobertura correcta del área de impresión.

## Estado
Completada

## Resultado
- Nuevo smoke test `backend/scripts/smoke_test_reporting_print_setup.py`
- Verifica headers/footers limpios, `fitToWidth = 1`, `fitToHeight = 0`, `fitToPage = true` y `print_area` cubriendo columnas y filas usadas.
