# TASK-0877 - Detectar mezcla de bases en líneas de presupuesto

## Objetivo
Encontrar presupuestos que estén apuntando a APUs de una base distinta a la base activa del proyecto.

## Estado
Completada

## Resultado
- Se añadió `find_mixed_presupuesto_apu_lines(...)` en `backend/app/services/presupuesto.py`.
- Se creó el smoke test `backend/scripts/smoke_test_presupuesto_apu_scope.py`.
- Se confirmó y localizó el caso real en `Santiago Bermeo` (`presupuesto_id = 13`, líneas `270` y `271`).
