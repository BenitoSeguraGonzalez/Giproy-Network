# TASK-0878 - Reparar y remapear líneas mezcladas al APU canónico de la base del proyecto

## Objetivo
Reescribir las líneas históricas contaminadas para que apunten al APU equivalente de la base activa del proyecto.

## Estado
Completada

## Resultado
- Se añadió `repair_presupuesto_apu_scope(...)` en `backend/app/services/presupuesto.py`.
- Se creó el script `backend/scripts/repair_presupuesto_apu_scope.py`.
- Se ejecutó la reparación real en `Santiago Bermeo`, remapeando:
  - línea `270`: `apu 86 (base 32)` -> `apu 349 (base 34)`
  - línea `271`: `apu 87 (base 32)` -> `apu 350 (base 34)`
- Tras el saneamiento, el smoke test quedó en `0` mezclas.
