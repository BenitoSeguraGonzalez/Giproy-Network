# TASK-0880 - Blindar la base de datos para impedir mezcla de bases

## Objetivo
Hacer inviolable la independencia entre bases de trabajo desde PostgreSQL, incluso si algún flujo futuro falla en la aplicación.

## Estado
Completada

## Resultado
- Nueva migración `backend/alembic/versions/c2d4e6f8a0b1_enforce_base_scope_integrity.py`.
- Se agregan triggers PostgreSQL para:
  - `presupuesto_detalles`: APU, EDT y parent deben pertenecer al contexto correcto.
  - `apu_lineas`: recurso y APU hijo deben pertenecer a la misma base/empresa del APU padre.
- Se añadió el script operativo `backend/scripts/apply_base_scope_guardrails.py` y se aplicó sobre la base activa.
