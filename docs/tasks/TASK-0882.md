# TASK-0882 - Unificar resolución de unidades APU en frontend

## Objetivo
Usar una sola lógica para mapear `unidad_id`, descripción corta y descripción completa en los editores de APU.

## Estado
Completada

## Resultado
- Nuevo helper `frontend/src/utils/unitOptions.js`.
- `APUs.jsx` y `ApuBudgetEditor.jsx` resuelven unidades mediante helpers comunes.
- La creación de nuevas unidades APU ya selecciona correctamente `unidad_id` y descripción visible.
