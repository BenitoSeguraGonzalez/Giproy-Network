# TASK-0888 - Apertura Automática Del Editor APU Desde Proyecto

## Objetivo
Hacer que el módulo `APUs` abra directamente el editor/creador cuando se entra desde `Proyecto`.

## Estado
Completada

## Resultado
- `APUs.jsx` detecta la entrada contextual desde proyecto.
- Si no se pidió un `apu_id` concreto, abre el creador APU directamente.
