# TASK-0896 - Añadir Apertura Automática De MPP En Exportación De Cronograma

## Objetivo
Permitir que el backend abra Microsoft Project automáticamente con el `.mpp` recién generado.

## Estado
Completada

## Resultado
- `CronogramaTrabajoService.export_ms_project_mpp(...)` ya soporta `open_after_export`.
- Cuando se solicita, el `.mpp` se genera en una ruta temporal persistente y Project queda visible con el archivo abierto.
