# TASK-0898 - Alinear Scripts De Arranque Backend Con La Venv Del Proyecto

## Objetivo
Evitar que el backend arranque con un Python global que no vea `pywin32/win32com`.

## Estado
Completada

## Resultado
- `backend/activaSistema.bat` y `iniciar_sistema.bat` ya resuelven y usan la `.venv` del proyecto.
