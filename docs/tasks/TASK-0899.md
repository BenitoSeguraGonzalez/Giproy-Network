# TASK-0899 - Hacer Que El Servicio Windows Use La Venv Correcta Del Backend

## Objetivo
Eliminar divergencias entre la ejecución manual del backend y el servicio instalado.

## Estado
Completada

## Resultado
- `instalar_servicio.bat` ya genera `giproy_service_start.bat` usando el `python.exe` de la `.venv`.
- El servicio deja de depender de `uvicorn` global y hereda correctamente paquetes como `pywin32`.
