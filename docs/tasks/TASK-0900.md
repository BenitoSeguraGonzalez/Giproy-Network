# TASK-0900 - Alinear Launcher.bat Con La Venv Real Del Proyecto

## Objetivo
Evitar que el Control Center arranque con un Python global cuando la `.venv` válida del proyecto está en la raíz.

## Estado
Completada

## Resultado
- `Launcher.bat` ya busca también `.venv` y `venv` en la raíz del proyecto.
