# TASK-0901 - Alinear Servicios Del Control Center Con La Venv Del Proyecto

## Objetivo
Hacer que el backend gestionado por el launcher use la misma `.venv` que el proyecto real.

## Estado
Completada

## Resultado
- `tools/launcher/launcher_app/ops.py` ya prioriza `.venv`/`venv` de `backend` y de la raíz del proyecto antes de caer a Python global.
