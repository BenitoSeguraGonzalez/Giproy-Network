# TASK-0904 - Crear GiProy Project Bridge local en PowerShell

## Objetivo
Crear un helper local para Windows que detecte Microsoft Project y pueda abrir cronogramas desde XML en la sesión interactiva del usuario.

## Cierre
- `bridge_server.ps1` expone `health`, `status` y `open-msproject-from-xml`.
