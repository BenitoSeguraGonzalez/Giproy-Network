# TASK-0910 - Exponer PDF desde Excel en el visor común de reportes

## Objetivo
Añadir una tercera opción visible de exportación en el visor común de reportes para elegir `PDF desde Excel`.

## Cierre
- `CommonReportPreviewModal` muestra un CTA adicional.
- `Presupuesto`, `APUs`, `EDT`, `VAE` y `Fórmula Polinómica` lo conectan al backend.
- el resto del flujo de preview/export permanece estable.
