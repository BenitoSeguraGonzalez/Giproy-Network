# TASK-0936 - Reportes Y QA De Planificacion Gantt

## Modo
GIPROY CLASICO

## Estado
En progreso - Slice 1 QA

## Objetivo
Cerrar la madurez del Gantt con reportes y pruebas de planificación.

## Alcance
- Reporte Gantt.
- Reporte de dependencias.
- Reporte de ruta crítica.
- Reporte de carga/esfuerzo.
- Tests de dependencias, cascada, calendario, ciclos, baseline y export/import.

## Criterio De Aceptación
- Existe suite de pruebas que cubre reglas MS Project básicas.
- Reportes reutilizan el contrato común de reporting.
- No hay acoplamiento BIM.

## Implementación Slice 1
- Se añade suite enfocada `test_cronograma_trabajo_ms_project.py`.
- La suite cubre:
  - Parseo de duración ISO MSPDI (`PT...`, `P...DT...`).
  - Conversión de `LinkLag` desde décimas de minuto a días laborables según jornada.
  - Tipos de dependencia MS Project `FF/FS/SS/SF`.
  - Exportación XML con identificador estable `Text1 = line-{presupuesto_linea_id}`.
  - Exportación de dependencia rica `SS + lag`.
  - Reimportación controlada del XML para actualizar fecha, duración, avance, dependencia y metadata sin modificar presupuesto/APU.

## Pendiente
- Reporte Gantt.
- Reporte de dependencias.
- Reporte de ruta crítica.
- Reporte de carga/esfuerzo.
- Ampliar QA con casos de ciclos, baseline y calendario real multi-semana.

## Punto De Reentrada
Cuando el usuario pida "seguir con integracion de gantt", continuar desde `TASK-0936` con el siguiente sub-slice recomendado:
- Implementar reportes visibles del Gantt clásico sobre el contrato común de reporting.
- Orden sugerido:
  - `cronograma_trabajo_gantt` como preview/export base.
  - `cronograma_trabajo_dependencias` para relaciones `FS/SS/FF/SF + lag`.
  - `cronograma_trabajo_ruta_critica` para críticas, hitos y holguras.
  - `cronograma_trabajo_carga` para esfuerzo/cuadrilla/rendimiento.
- Mantener sin cambios BIM, sin activar UX BIM y sin crear dependencias hacia BIM.
- No reabrir `TASK-0935`: export/import XML MS Project ya quedó implementado y validado en `0.144.91`; `TASK-0936 Slice 1` ya cubre QA roundtrip backend en `0.144.92`.

## Validación Slice 1
- `PYTHONPATH=backend python -m pytest -q backend/app/tests/test_cronograma_trabajo_ms_project.py`
