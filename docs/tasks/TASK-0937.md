# TASK-0937 - Confirmacion Visible En Editor APU De Presupuesto

## Modo
GIPROY CLASICO

## Estado
Completada

## Objetivo
Corregir la capa visual del dialogo de confirmacion al cerrar el editor APU abierto desde una linea de presupuesto, para que quede por encima del drawer de trabajo y no bloquee la interaccion.

## Alcance
- Mantener el ajuste limitado a la capa clasica de Presupuesto/APU.
- No tocar backend.
- No cambiar la UX BIM ni introducir dependencias BIM.
- Evitar un cambio global agresivo de z-index para todos los dialogos.

## Implementacion
- `AppDialogProvider` acepta un `zIndex` opcional por dialogo.
- `ApuEditorModal` eleva la confirmacion de cierre sobre el drawer del editor.
- `ApuBudgetEditor` eleva la confirmacion de cierre del editor interno sobre el drawer del editor.

## Validacion
- `npx eslint src/components/ui/AppDialogProvider.jsx src/components/presupuestos/ApuEditorModal.jsx`: OK.
- `npx eslint src/components/presupuestos/ApuBudgetEditor.jsx --quiet`: falla por deuda previa no relacionada de variables sin uso en el editor APU.
- `python -m json.tool docs/project_state.json`: OK.
- `python -m json.tool docs/runtime/WORK_MODE_STATE.json`: OK.
- `npm run build`: OK.
- Búsqueda de acoplamientos BIM en archivos tocados: sin acoplamiento nuevo; coincidencias limitadas a documentación de no interferencia o estado histórico.
