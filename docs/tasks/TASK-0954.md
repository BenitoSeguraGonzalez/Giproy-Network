# TASK-0954 - QA E Importación De Referencias MS Project Y Excel

## Modo
GIPROY CLASICO

## Estado
Completada - Slice 1 QA

## Objetivo
Validar el nuevo flujo integral contra las referencias reales en `docs/adicionales/gantt`.

## Referencias
- `Cronograma valorado.xls`
- `Cronograma de trabajo.mpp`
- `Flujo de caja de project.xlsx`

## Alcance
- Documentar mapeo de periodos, rubros, tareas, recursos y costos.
- Usar XML MS Project como ruta estable de importación/exportación.
- Definir estrategia para `.mpp`: Project Bridge/MS Project o conversión controlada.
- Crear pruebas de periodización, flujo de caja y acumulados con datos equivalentes a las referencias.

## Criterios De Aceptación
- Existen fixtures o casos equivalentes que validan la relación Gantt -> Valorado -> Caja.
- La importación no modifica presupuesto, EDT ni APUs fuera del contrato autorizado.
- Las limitaciones de `.mpp` quedan documentadas con ruta alternativa.

## Implementación Slice 1
- Se añadió la suite `backend/app/tests/test_cronograma_integrated_references.py`.
- La suite clasifica las referencias reales:
  - `Cronograma valorado.xls`: binario legacy OLE.
  - `Cronograma de trabajo.mpp`: binario legacy OLE de Microsoft Project.
  - `Flujo de caja de project.xlsx`: libro Office Open XML legible con `openpyxl`.
- La suite valida que `.mpp` no sea XML y, por tanto, no debe importarse directamente desde backend sin Project Bridge/MS Project o conversión controlada.
- La hoja `Uso de tareas` del flujo de caja se mapea como pares mensuales `Costo` / `Costo acumulado` para `Agosto`, `Septiembre`, `Octubre` y `Noviembre`.
- Se validan partidas hoja como `Replanteo y nivelación` y `Excavación cimientos`, evitando filas resumen duplicadas.
- Se añade un caso equivalente `Gantt -> Valorado -> Caja`: la periodización mensual 2014-08-04 a 2014-12-01 produce 4 periodos y el `cash_flow` derivado reproduce el patrón de la referencia para `Replanteo y nivelación`.

## Estrategia `.mpp`
- `.mpp` queda reconocido como referencia binaria Microsoft Project, no como entrada directa portable.
- La ruta estable sigue siendo XML MSPDI exportado/importado por `cronogramas-trabajo`.
- Para `.mpp`, la estrategia autorizada es Project Bridge/MS Project o conversión explícita a XML antes de entrar al backend.

## Validación
- `PYTHONPATH=backend python -m pytest -q backend/app/tests/test_cronograma_integrated_references.py backend/app/tests/test_numeric_integrity_secondary_modules.py backend/app/tests/test_cronograma_trabajo_ms_project.py`: OK, `15 passed`.
- `python -m py_compile backend/app/tests/test_cronograma_integrated_references.py`: OK.

## No Interferencia BIM
- Sin rutas, modelos, componentes ni UX BIM.
