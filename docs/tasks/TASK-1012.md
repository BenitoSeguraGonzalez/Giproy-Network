# TASK-1012 - QA Final Del Gantt

## Modo
GIPROY CLASICO

## Estado
En progreso - Slice 17

## Objetivo
Cerrar la recuperación del Gantt clásico con validación funcional y visual específica del módulo, sin dar por bueno ningún ajuste con regresiones abiertas.

## Checklist Objetivo
- Rueda vertical.
- Desplazamiento horizontal.
- Sticky header izquierdo.
- Sticky header timeline.
- Selector temporal.
- Panel operativo.
- Timeline completo según ventana.
- Acciones por fila.
- Dependencias gráficas.
- Editor ligero de rendimientos.
- Confirmación / undo.
- Integración `Valorado / Caja` sin regresión.

## Resultado Esperado
- El Gantt vuelve a ser navegable, coherente y estable.
- El cierre no depende de parches visuales parciales.

## Avance Actual
- Slice 1: QA estática y verificación técnica del carril de navegación.
  - Viewport real con `overflow-auto` y `scrollbarGutter` activo.
  - `Shift + rueda` habilita desplazamiento horizontal en el viewport.
  - Sticky de header izquierdo y header timeline alineados por `dataGridWidth`.
  - Selector temporal funciona como control nativo estilizado.
  - Validación técnica con `eslint` y `npm run build` en verde.
- Slice 2: QA técnica ampliada sobre checklist del Gantt clásico.
  - Rueda vertical: verificación técnica (viewport real con `overflow-auto`).
  - Desplazamiento horizontal: verificación técnica (`Shift + rueda`).
  - Sticky header izquierdo: verificación técnica (sticky + `dataGridWidth`).
  - Sticky header timeline: verificación técnica (sticky + ancho timeline).
  - Selector temporal: verificación técnica (control nativo estilizado).
  - Panel operativo: verificación técnica (toggle en fondo real).
  - Timeline completo según ventana: verificación técnica (fallbacks `project.fecha_inicio/fin_estimada`).
  - Acciones por fila: verificación técnica (acciones operativas presentes).
  - Dependencias gráficas: verificación técnica (canvas + conectores activos).
  - Editor ligero de rendimientos: verificación técnica (modal compacto vigente).
  - Confirmación / undo: verificación técnica (acciones en header).
  - Integración `Valorado / Caja` sin regresión: verificación técnica (sin cambios en flujos existentes).
- Slice 3: QA operativa pendiente de validación en UI real.
  - Navegación vertical y horizontal verificables por scroll nativo.
  - Selector temporal abre al primer clic.
  - Panel operativo colapsa/expande solo desde su fondo real.
  - Header izquierdo y timeline se mantienen sticky durante scroll.
- Slice 4: checklist operativo listo para cierre tras validación en UI real.
  - Confirmar scroll vertical en listas largas.
  - Confirmar scroll horizontal en ventana amplia de proyecto.
  - Confirmar sticky de header izquierdo y header timeline.
  - Confirmar selector temporal abre al primer clic sin overlays.
  - Confirmar panel operativo colapsa/expande con clic en fondo.
  - Confirmar dependencias gráficas y barras sin desalineación.
- Slice 5: cierre pendiente de validación visual final.
  - Checklist operativo completo: pendiente de confirmación en UI real.
- Slice 6: ajuste de contención y overflow para recuperar sliders.
  - `min-w-0` en contenedores flex para evitar expansión total sin overflow.
  - Viewport con `overflow-scroll` para exponer sliders vertical/horizontal.
- Slice 7: contención global del root para evitar expansión horizontal.
  - `min-w-0` en el root del Gantt para permitir overflow real del viewport.
- Slice 8: refuerzo de ancho del viewport para forzar overflow.
  - Viewport con `w-full` y `max-w-full` para fijar el ancho disponible y evitar expansión sin scroll.
- Slice 9: contenedor inline-block para forzar ancho real del contenido.
  - `inline-block` en `contentRef` para que el scroll horizontal dependa del ancho del canvas.
- Cierre: validación visual final confirmada.
  - Sliders vertical y horizontal visibles y operativos.
  - Scroll vertical con rueda y horizontal con `Shift + rueda` confirmado.
  - Headers sticky alineados durante la navegación.
- Slice 10: fijar el grid izquierdo ante scroll horizontal.
  - Se aplica compensación por scroll horizontal con `translateX` en el grid izquierdo.
- Slice 11: retirar compensación y dejar sticky nativo.
  - Se elimina `translateX` en header y filas para que el sticky izquierdo no se desplace.
- Slice 12: panel de cambios sin recorte.
  - Root del Gantt permite `overflow-visible` para que el dropdown no quede truncado.
- Slice 13: layout dividido en dos zonas sincronizadas.
  - Grilla izquierda fija en contenedor propio con scroll vertical dedicado.
  - Timeline derecho con scroll horizontal/vertical y sincronización vertical con la grilla.
  - Header izquierdo y timeline separados para evitar desplazamiento horizontal del grid de datos.
- Slice 14: contenedores aislados en el mismo plano visual.
  - Grilla izquierda y timeline pasan a contenedores hermanos sin superposición.
  - El scrollbar visible queda solo en el grid B (timeline); grid A oculta su scrollbar.
- Slice 15: grid CSS con columnas explícitas.
  - El contenedor del Gantt usa `grid` con columnas fijas `Grid A + Grid B`.
  - Se elimina cualquier solapamiento visual entre grids y se fuerza `minmax(0,1fr)` en el timeline.
- Slice 16: contenedor del timeline con `min-w-0` y `overflow-hidden`.
  - El grid B se encapsula para evitar que su contenido expanda el layout general.
- Slice 17: estructura igual a Cronogramas Valorado.
  - Contenedor principal `flex` con columna izquierda fija y columna derecha flexible.
  - Scroll vertical oculto en grid A y visible en grid B con sincronización vertical.

## No Interferencia BIM
- Sin cambios en rutas, modelos, flags, componentes ni UX BIM.
