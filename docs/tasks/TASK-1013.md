# TASK-1013 - Control De Cumplimiento Del Plan De Recuperación Del Gantt

## Modo
GIPROY CLASICO

## Estado
Activa

## Objetivo
Gobernar y auditar el cumplimiento del plan de recuperación del Gantt clásico, asegurando que cada intervención se mantenga dentro del alcance autorizado y preserve la funcionalidad ya adquirida.

## Alcance
- Controlar que el trabajo permanezca exclusivamente en `Proyecto > Cronogramas > Gantt`.
- Verificar que no se toquen otros módulos, estilos globales ni BIM sin autorización expresa.
- Validar que la recuperación siga la secuencia aprobada:
  1. saneamiento
  2. workspace
  3. panel operativo
  4. toolbar
  5. navegación
  6. QA

## Criterios De Cumplimiento
- No perder funcionalidad existente del Gantt.
- No introducir cambios globales del sistema.
- No cerrar una tarea con regresiones activas de navegación o layout.
- Mantener trazabilidad documental y de estado en cada slice.
- Reportar obligatoriamente porcentaje de terminación cada vez que se complete total o parcialmente una TASK.

## Relación Con Otras TASKs
- Supervisa `TASK-1007` a `TASK-1012`.
- Supervisa también `TASK-1014` cuando el ajuste afecte el editor ligero de rendimientos del Gantt clásico.
- Supervisa también `TASK-1015` cuando el ajuste afecte interoperabilidad MS Project del Gantt clásico.
- Supervisa también `TASK-1016` cuando el ajuste retire carriles técnicos no vigentes del cronograma clásico.
- Supervisa también `TASK-1017` cuando el ajuste conecte recálculo económico en tiempo real al editor de recursos del Gantt clásico.
- Supervisa también `TASK-1018` cuando el ajuste reoriente exportación Project hacia XML interoperable sin dependencia de Microsoft Project.
- Supervisa también `TASK-1019` cuando el ajuste redefina la estrategia `.mpp` real usando Aspose.Tasks Java.
- Supervisa también `TASK-1020` cuando el ajuste implemente el runner Java privado para exportación `.mpp`.
- Supervisa también `TASK-1021` cuando el ajuste integre el carril Java en el backend común y el contrato de capacidades del Gantt clásico.
- Supervisa también `TASK-1022` cuando el ajuste cierre QA, seguridad de licencia y UX del carril `.mpp` server-side.
- Debe actualizarse en paralelo a cada avance del plan de recuperación.

## Regla Operativa Obligatoria
- En cada cierre parcial o total de una TASK del plan de recuperación del Gantt se debe informar:
  - porcentaje de la TASK afectada;
  - porcentaje de la Fase 14;
  - porcentaje relativo global.

## No Interferencia BIM
- Sin cambios en rutas, modelos, flags, componentes ni UX BIM.

## Seguimiento De Sesión 2026-04-09
- Incidencia clásica atendida fuera del slice Gantt: `POST /api/v1/login/session-trace` devolvía `500` en el backend común por imports faltantes en `backend/app/api/endpoints/auth.py`.
- Corrección aplicada solo en la capa clásica/común de autenticación para restablecer trazabilidad de sesión sin tocar módulos BIM ni flujos UX BIM.
- Impacto sobre esta TASK de control Gantt: sin avance funcional del plan Gantt; se mantiene el alcance supervisado sin contaminación cruzada.
- Porcentaje TASK-1013: `0%` adicional en esta sesión.
- Porcentaje Fase 14: `0%` adicional en esta sesión.
- Porcentaje relativo global: `0%` adicional en esta sesión.

## Seguimiento Gantt Clásico 2026-04-09
- Slice funcional aplicado en `Proyecto > Cronogramas > Gantt` sobre el editor ligero de rendimientos, manteniendo el trabajo dentro del módulo clásico y sin tocar BIM.
- El cálculo base del tiempo del APU pasa a gobernarse por el mayor rendimiento operativo entre `Equipos y Herramientas` y `Mano de Obra`, con fallback seguro para casos sin esas categorías.
- El editor de recursos marca con color propio el recurso gobernante y actualiza esa marca en tiempo real al editar rendimientos.
- No se introdujeron rutas, flags, componentes ni UX BIM.
- Porcentaje TASK-1013 estimado tras este slice: `12%`.
- Porcentaje Fase 14 estimado tras este slice: `8%`.
- Porcentaje relativo global estimado tras este slice: `1%`.

## Seguimiento De Persistencia De Rendimientos 2026-04-09
- Se abrió `TASK-1014` para restaurar la edición persistente de rendimientos gobernantes desde el editor ligero del Gantt clásico.
- El tanteo operativo pasa a guardarse en metadata del cronograma, manteniendo el APU maestro sin cambios y sin contaminación BIM.
- Porcentaje TASK-1013 estimado tras supervisar este slice adicional: `16%`.
- Porcentaje Fase 14 estimado tras este slice adicional: `11%`.
- Porcentaje relativo global estimado tras este slice adicional: `1.5%`.

## Seguimiento De CTA MS Project 2026-04-09
- Se abrió `TASK-1015` para redefinir el CTA `MS Project` del Gantt clásico como descarga directa de archivo `.mpp`.
- El contrato deja de priorizar `Project Bridge` en ese botón y `XML Project` queda alineado al carril XML real.
- El ajuste no toca BIM ni rompe el backend común de cronograma.
- Porcentaje TASK-1013 estimado tras supervisar este slice adicional: `19%`.
- Porcentaje Fase 14 estimado tras este slice adicional: `13%`.
- Porcentaje relativo global estimado tras este slice adicional: `1.8%`.

## Seguimiento De Retiro De Project Bridge 2026-04-09
- Se abrió `TASK-1016` para retirar `Project Bridge` del cronograma clásico al no estar funcionando de forma fiable.
- El módulo queda consolidado con dos carriles claros: `.mpp` directo cuando el entorno lo soporta y `XML Project` como fallback explícito, incluyendo no Windows.
- El ajuste no toca BIM ni altera contratos fuera del módulo clásico de cronogramas.
- Porcentaje TASK-1013 estimado tras supervisar este slice adicional: `22%`.
- Porcentaje Fase 14 estimado tras este slice adicional: `15%`.
- Porcentaje relativo global estimado tras este slice adicional: `2.1%`.

## Seguimiento De Acople Económico En Tiempo Real 2026-04-09
- Se abrió `TASK-1017` para incorporar `precio unitario` y `precio total` al editor de rendimientos del Gantt clásico, manteniendo el recálculo económico en vivo.
- El modal reutiliza la política operativa común del APU, por lo que duración, rendimiento y costos permanecen alineados dentro del mismo slice clásico.
- El ajuste no toca BIM ni cambia el APU maestro.
- Porcentaje TASK-1013 estimado tras supervisar este slice adicional: `26%`.
- Porcentaje Fase 14 estimado tras este slice adicional: `18%`.
- Porcentaje relativo global estimado tras este slice adicional: `2.5%`.

## Seguimiento De Simplificación Del Modal 2026-04-10
- Se refina `TASK-1017` para retirar la prominencia del borrador operativo en el modal clásico de recursos.
- El flujo visible queda reducido a `Aceptar` y `Cancelar`, con restauración discreta por icono y descarte real al cancelar.
- También se elimina la categoría visible `APUs` del agrupado del modal, manteniendo el vocabulario operativo clásico esperado.
- Porcentaje TASK-1013 estimado tras supervisar este refinamiento: `29%`.
- Porcentaje Fase 14 estimado tras este refinamiento: `20%`.
- Porcentaje relativo global estimado tras este refinamiento: `2.8%`.

## Seguimiento De Reajuste De Exportación Project 2026-04-10
- Se abrió `TASK-1018` para retirar la dependencia residual con Microsoft Project desktop del carril `MS Project`.
- El backend vuelve a presuponer entorno sin COM ni `.mpp` directo, y el frontend alinea el botón `MS Project` a exportación XML compatible generada por GiProy.
- El ajuste se mantiene dentro del cronograma clásico y no toca BIM.
- Porcentaje TASK-1013 estimado tras supervisar este refinamiento: `33%`.
- Porcentaje Fase 14 estimado tras este refinamiento: `23%`.
- Porcentaje relativo global estimado tras este refinamiento: `3.1%`.

## Seguimiento De Plan MPP Server-Side 2026-04-10
- La validacion posterior de licencia y runtime confirma que el carril `.mpp` real no debe apoyarse en Python sino en `Aspose.Tasks for Java 20.2` con licencia privada del servidor.
- Se abre `TASK-1019` como replanteamiento autorizado del carril `.mpp` y se preparan `TASK-1020` a `TASK-1022` para motor Java, integracion backend comun y QA/seguridad.
- El plan queda acotado a la capa clasica de `Proyecto > Cronogramas > Gantt`, manteniendo `XML Project` como fallback estable y sin contaminar BIM.
- Porcentaje TASK-1013 estimado tras supervisar este nuevo frente: `36%`.
- Porcentaje Fase 14 estimado tras este nuevo frente: `25%`.
- Porcentaje relativo global estimado tras este nuevo frente: `3.4%`.

## Seguimiento De Implementacion MPP Java 2026-04-10
- Se completa el runner Java privado del carril `.mpp` y su integracion en `CronogramaTrabajoService`, manteniendo el backend comun como orquestador.
- La exportacion `.mpp` del cronograma clasico vuelve a apoyarse en servidor, sin COM, sin `Project Bridge` y sin exponer la licencia al cliente.
- La capa BIM no fue tocada ni contaminada; el fallback `XML Project` permanece operativo.
- Porcentaje TASK-1013 estimado tras supervisar esta implementacion: `42%`.
- Porcentaje Fase 14 estimado tras este nuevo frente: `31%`.
- Porcentaje relativo global estimado tras este nuevo frente: `4.1%`.

## Seguimiento De QA Automatizada MPP Java 2026-04-10
- Se endurece la cobertura del carril `.mpp` del Gantt clasico con pruebas automatizadas del backend comun: capacidades disponibles, fallback por ausencia de `java` y generacion binaria real.
- La suite `backend/app/tests/test_cronograma_trabajo_ms_project.py` queda en verde con `5 passed`, manteniendo intacto el fallback XML y sin tocar BIM.
- Porcentaje TASK-1013 estimado tras supervisar esta QA: `45%`.
- Porcentaje Fase 14 estimado tras este nuevo frente: `34%`.
- Porcentaje relativo global estimado tras este nuevo frente: `4.4%`.

## Seguimiento De QA API MPP 2026-04-10
- Se supervisa un cierre adicional del carril `.mpp` del Gantt clásico centrado en contrato HTTP, sin ampliar alcance fuera del módulo de cronogramas clásicos.
- La suite automatizada cubre ya exportación `.mpp` a nivel servicio y a nivel endpoint, incluyendo `attachment` correcto y `409` explícito cuando el entorno no soporta `.mpp`.
- La capa BIM permanece intacta: sin cambios en rutas, flags, modelos, componentes ni UX BIM.
- Porcentaje TASK-1013 estimado tras supervisar este nuevo frente: `48%`.
- Porcentaje Fase 14 estimado tras este nuevo frente: `37%`.
- Porcentaje relativo global estimado tras este nuevo frente: `4.8%`.

## Seguimiento De Optimización De Exportación MPP 2026-04-10
- Se supervisa un ajuste de rendimiento sobre el carril `.mpp` del cronograma clásico ante exportaciones que permanecían demasiado tiempo dentro del runner Java/Aspose.
- El cambio mantiene el backend común en la capa clásica y evita por defecto la recalculación global del proyecto dentro de Aspose, conservándola solo como mecanismo opt-in de servidor.
- La capa BIM sigue intacta: sin cambios en rutas, flags, modelos, componentes ni UX BIM.
- Porcentaje TASK-1013 estimado tras supervisar este nuevo frente: `50%`.
- Porcentaje Fase 14 estimado tras este nuevo frente: `39%`.
- Porcentaje relativo global estimado tras este nuevo frente: `5.0%`.

## Seguimiento De Cacheo Del Runner MPP 2026-04-10
- Se supervisa una segunda optimización del carril `.mpp` clásico centrada en eliminar el coste de compilación repetida del runner Java.
- El backend pasa a preferir un runner Java precompilado y reutilizable cuando el servidor dispone de `javac`, con degradación segura al modo fuente para no bloquear instalaciones parciales.
- La capa BIM sigue intacta: sin cambios en rutas, flags, modelos, componentes ni UX BIM.
- Porcentaje TASK-1013 estimado tras supervisar este nuevo frente: `52%`.
- Porcentaje Fase 14 estimado tras este nuevo frente: `41%`.
- Porcentaje relativo global estimado tras este nuevo frente: `5.3%`.

## Seguimiento De Corrección Final Del Carril MPP 2026-04-10
- Se supervisa la corrección del carril `.mpp` clásico tras detectar un atasco real superior a seis minutos sobre el proyecto `Santiago Bermeo`.
- El ajuste mantiene la capa clásica en backend común y reorienta el payload interno del runner Java hacia una construcción `.mpp` más liviana, priorizando tareas y dependencias para evitar cuelgues en Aspose.
- También se endurece la coherencia temporal exportada para tareas cuya ventana real era válida pero cuya duración derivada llegaba como `0`.
- La validación real del flujo clásico se ejecuta sobre `empresa_id=3`, proyecto `SantiagoBermeo-2026-001`, presupuesto `13`, cerrando la exportación `.mpp` en `13.353 s`.
- La capa BIM sigue intacta: sin cambios en rutas, flags, modelos, componentes ni UX BIM.
- Porcentaje TASK-1013 estimado tras supervisar este nuevo frente: `58%`.
- Porcentaje Fase 14 estimado tras este nuevo frente: `43%`.
- Porcentaje relativo global estimado tras este nuevo frente: `5.6%`.

## Seguimiento De Optimización Backend MPP Sin Recursos 2026-04-10
- Se supervisa un recorte adicional del trabajo redundante del backend clásico: el carril `.mpp` deja de construir también en Python los bloques `Resources` y `Assignments` cuando el runner Java no los consume.
- La optimización confirma mejor separación de carriles: `XML Project` conserva la salida rica completa y `.mpp` usa una ruta interna más minimalista para estabilidad.
- La validación real posterior sobre `Santiago Bermeo` queda en `14.866 s`, lo que indica que el cuello remanente está ya del lado Java/Aspose y no en la preparación Python.
- La capa BIM sigue intacta: sin cambios en rutas, flags, modelos, componentes ni UX BIM.
- Porcentaje TASK-1013 estimado tras supervisar este nuevo frente: `60%`.
- Porcentaje Fase 14 estimado tras este nuevo frente: `43%`.
- Porcentaje relativo global estimado tras este nuevo frente: `5.7%`.

## Seguimiento De Caché Real MPP 2026-04-10
- Se supervisa la validación real de la nueva caché del carril `.mpp` clásico sobre `empresa_id=3`, `proyecto_id=7`, `presupuesto_id=13`.
- Tras limpiar `backend/.runtime_mpp_cache`, la primera exportación completa en `12.829 s`; la segunda, ejecutada inmediatamente después sobre el mismo cronograma, completa en `0.007 s`.
- La mejora confirma que el coste duro permanece en el primer disparo Java/Aspose, mientras que las exportaciones repetidas quedan resueltas desde backend común sin reabrir el pipeline pesado.
- La capa BIM sigue intacta: sin cambios en rutas, flags, modelos, componentes ni UX BIM.
- Porcentaje TASK-1013 estimado tras supervisar este nuevo frente: `62%`.
- Porcentaje Fase 14 estimado tras este nuevo frente: `44%`.
- Porcentaje relativo global estimado tras este nuevo frente: `5.8%`.
