# TASK-1015 - Redefinir CTA MS Project A Descarga Directa De Archivo MPP

## Modo
GIPROY CLASICO

## Estado
Activa

## Objetivo
Cambiar el contrato operativo del CTA `MS Project` en `Proyecto > Cronogramas > Gantt` y en el workbench clásico asociado para que genere y descargue el archivo `.mpp` del proyecto, sin priorizar apertura asistida por `Project Bridge`.

## Alcance
- Ajustar el CTA `MS Project` en la capa clásica.
- Mantener `XML Project` como exportación XML real.
- Desactivar apertura automática tras exportación `.mpp`.

## Criterios De Cumplimiento
- `MS Project` descarga `.mpp` cuando el entorno directo lo soporta.
- `MS Project` informa el motivo real cuando `.mpp` no está disponible.
- `XML Project` descarga XML y no `.mpp`.
- No hay interferencia BIM.

## No Interferencia BIM
- Sin cambios en rutas, flags, componentes, modelos ni UX BIM.

## Plan De Ajuste
1. Auditar el flujo actual que prioriza `Project Bridge`.
2. Redefinir el CTA `MS Project` para que represente exclusivamente `.mpp`.
3. Alinear `XML Project` con el carril XML real.
4. Eliminar apertura automática y dejar descarga explícita.
5. Validar y documentar.

## Seguimiento De Sesión 2026-04-09
- Se detectó que el CTA `MS Project` seguía un contrato híbrido entre `Project Bridge` y `.mpp`, lo que no coincide con la expectativa operativa de descargar el archivo del proyecto.
- Se redefine el CTA para que `MS Project` signifique únicamente exportación `.mpp` descargable.
- `XML Project` queda reservado al carril XML.
- Porcentaje TASK-1015 estimado tras este slice: `100%`.
- Porcentaje Fase 14 estimado tras este slice: `13%`.
- Porcentaje relativo global estimado tras este slice: `1.8%`.
