# TASK-1021 - Integrar Exportacion MPP Java En Backend Comun Y Contrato De Capacidades

## Modo
GIPROY CLASICO

## Estado
Completada

## Objetivo
Integrar el runner Java de exportacion `.mpp` en el backend comun del cronograma clasico y alinear el contrato de capacidades reales para `MS Project` y `XML Project`.

## Alcance
- Conectar `CronogramaTrabajoService` con el runner Java.
- Retirar el intento `.mpp` basado en `Aspose.Tasks` Python.
- Ajustar deteccion de capacidades reales del entorno.
- Mantener `XML Project` como fallback estable.

## Criterios De Cumplimiento
- El backend anuncia `.mpp` solo si el carril Java esta operativo.
- El backend conserva XML como salida interoperable estable.
- Los errores operativos se convierten en respuestas controladas.
- No se toca BIM.

## No Interferencia BIM
- Sin cambios en BIM, feature flags BIM, rutas BIM, modelos BIM ni UX BIM.

## Plan De Ajuste
1. Integrar el servicio Python con el runner Java.
2. Rehacer el diagnostico de capacidades y mensajes operativos.
3. Mantener el contrato API compatible para el frente clasico.
4. Verificar fallback XML y no interferencia BIM.
5. Documentar el cambio.

## Seguimiento De Sesion 2026-04-10
- `CronogramaTrabajoService` deja de depender del carril Python para `.mpp` y pasa a detectar licencia, `java`, JAR, runner y plantilla semilla del carril Java.
- `export_ms_project_mpp(...)` ahora invoca el runner Java privado mediante `subprocess`, manteniendo `XML Project` como fallback estable.
- La deteccion de capacidades del backend comun vuelve a anunciar `.mpp` como disponible en esta instalacion, sin tocar rutas ni UX BIM.
- Se elimina `aspose-tasks` Python de `backend/requirements.txt` para evitar mezclar productos/licencias incompatibles.
- Validaciones ejecutadas: `py_compile` del backend, generacion real de `.mpp` con el runner Java y `pytest backend/app/tests/test_cronograma_trabajo_ms_project.py`.
- Porcentaje TASK-1021 estimado tras este slice: `100%`.
- Porcentaje Fase 14 estimado tras este slice: `31%`.
- Porcentaje relativo global estimado tras este slice: `4.1%`.
