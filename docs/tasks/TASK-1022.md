# TASK-1022 - Validar UX Clasica, QA Y Seguridad Del Carril MPP Server-Side

## Modo
GIPROY CLASICO

## Estado
En progreso

## Objetivo
Cerrar la validacion funcional y documental del nuevo carril `.mpp` server-side del cronograma clasico, asegurando seguridad de licencia, UX coherente y no interferencia BIM.

## Alcance
- Validar el CTA `MS Project` del Gantt clasico.
- Confirmar que el cliente solo descarga el binario final.
- Cubrir QA de licencia, plantilla semilla, fallback XML y mensajes operativos.
- Dejar documentada la configuracion privada requerida.

## Criterios De Cumplimiento
- `MS Project` descarga `.mpp` cuando el backend Java esta habilitado.
- `XML Project` sigue operativo como fallback.
- La licencia no se expone al cliente.
- Se valida expresamente que BIM no fue contaminado.

## No Interferencia BIM
- Sin cambios en componentes BIM, rutas BIM, feature flags BIM, modelos BIM ni UX BIM.

## Plan De Ajuste
1. Validar el flujo `.mpp` extremo a extremo en la capa clasica.
2. Confirmar fallback XML y mensajes de error claros.
3. Verificar seguridad basica de licencia y configuracion.
4. Ejecutar QA tecnico y documentar hallazgos.
5. Cerrar trazabilidad en TASK + CHANGELOG.

## Seguimiento De Sesion 2026-04-10
- Queda una primera validacion tecnica ya ejecutada: el cliente no recibe la licencia, el backend anuncia `.mpp` de forma honesta y el runner Java genera un binario real desde servidor.
- Se añade cobertura automatizada del carril Java en `backend/app/tests/test_cronograma_trabajo_ms_project.py`: capacidades `.mpp`, exportacion binaria real y fallback por ausencia de `java`.
- El backend comun queda validado con `5 passed` sobre la suite MS Project del cronograma clasico.
- Resta como cierre formal la validacion extremo a extremo desde la UI clasica y la comprobacion manual del `.mpp` en Microsoft Project o visor equivalente con cronogramas reales.
- Porcentaje TASK-1022 estimado tras este avance: `65%`.
- Porcentaje Fase 14 estimado tras este slice: `34%`.
- Porcentaje relativo global estimado tras este slice: `4.4%`.

## Seguimiento De QA API 2026-04-10
- Se amplía la suite automatizada del cronograma clásico para cubrir el contrato HTTP del endpoint `GET /api/v1/cronogramas-trabajo/{presupuesto_id}/export/ms-project`.
- El slice valida dos carriles reales del backend común: descarga `.mpp` con `attachment` correcto y rechazo `409` cuando la capacidad `.mpp` no está disponible.
- La validación se mantiene completamente en la capa clásica; no se tocan rutas, flags, componentes ni UX BIM.
- Estado automatizado actual del backend común para este slice: `7 passed` en `backend/app/tests/test_cronograma_trabajo_ms_project.py`.
- Resta solo el cierre manual extremo a extremo sobre UI clásica y apertura del `.mpp` con cronogramas reales.
- Porcentaje TASK-1022 estimado tras este avance: `82%`.
- Porcentaje Fase 14 estimado tras este slice: `37%`.
- Porcentaje relativo global estimado tras este slice: `4.8%`.

## Seguimiento De Rendimiento MPP 2026-04-10
- Se detecta una degradación de tiempo en la exportación `.mpp` clásica con el runner Java/Aspose para cronogramas de mayor tamaño.
- El ajuste aplicado evita el `project.recalculate()` global por defecto cuando GiProy ya entrega fechas, duraciones y dependencias resueltas desde backend, dejando esa recalculación solo como opción avanzada de servidor.
- También se vuelve configurable el timeout del runner mediante `ASPOSE_TASKS_MPP_TIMEOUT_SECONDS`, eliminando el límite rígido anterior.
- Validación posterior del carril clásico: `7 passed` en la suite MS Project y `npm run build` del frontend en verde.
- No se tocan rutas, flags, componentes ni UX BIM.
- Porcentaje TASK-1022 estimado tras este avance: `88%`.
- Porcentaje Fase 14 estimado tras este slice: `39%`.
- Porcentaje relativo global estimado tras este slice: `5.0%`.

## Seguimiento De Cacheo Del Runner Java 2026-04-10
- Se identifica un cuello de botella adicional: el runner Java `.mpp` se estaba invocando en modo fuente, con coste potencial de compilación en cada exportación.
- El backend clásico ahora compila y reutiliza el runner `aspose_tasks_mpp_runner` cuando `javac` está disponible, con fallback seguro al modo fuente si el entorno no dispone de compilador.
- La suite automatizada del carril clásico queda en `8 passed` tras cubrir también la preferencia por el runner compilado.
- No se tocan rutas, flags, componentes ni UX BIM.
- Porcentaje TASK-1022 estimado tras este avance: `92%`.
- Porcentaje Fase 14 estimado tras este slice: `41%`.
- Porcentaje relativo global estimado tras este slice: `5.3%`.

## Seguimiento De Estabilización Real MPP 2026-04-10
- Se detecta y corrige la causa real del fallo del carril `.mpp` clásico sobre `Santiago Bermeo`: el runner Java/Aspose se degradaba en asignaciones y después quedaba bloqueado durante el guardado del binario.
- El backend clásico ahora calcula una duración exportable coherente para tareas con ventana temporal válida aunque `duration_hours` llegue en `0`, evitando inconsistencias entre `Start`, `Finish` y `Duration`.
- Para el carril `.mpp` server-side se introduce un XML de ejecución más ligero, centrado en estructura temporal y dependencias; `Resources` y `Assignments` se retiran solo del payload interno usado para construir el `.mpp`, manteniendo intacto el carril XML interoperable completo.
- Validación real ejecutada hoy sobre empresa `Santiago Bermeo` (`empresa_id=3`, proyecto `SantiagoBermeo-2026-001`, presupuesto `13`): generación `.mpp` completada en `13.353 s` con binario resultante de `551936` bytes.
- Validación automatizada actualizada: `10 passed` en `backend/app/tests/test_cronograma_trabajo_ms_project.py` y `py_compile` en verde.
- No se tocan rutas, flags, componentes, modelos ni UX BIM.
- Porcentaje TASK-1022 estimado tras este avance: `97%`.
- Porcentaje Fase 14 estimado tras este slice: `43%`.
- Porcentaje relativo global estimado tras este slice: `5.6%`.

## Seguimiento De Optimización Del XML Runtime 2026-04-10
- Se elimina trabajo redundante del backend clásico en el carril `.mpp`: la ruta `export_ms_project_mpp` deja de construir `Resources` y `Assignments` también en Python cuando el binario final no los necesita.
- `export_ms_project_xml` gana el flag `include_resources`, conservando el XML completo para interoperabilidad MSPDI y usando un XML más liviano solo para la construcción server-side del `.mpp`.
- La validación automatizada sube a `11 passed` y `py_compile` permanece en verde.
- Medición real posterior sobre `Santiago Bermeo` (`empresa_id=3`, proyecto `7`, presupuesto `13`): `14.866 s`, lo que confirma que el cuello dominante remanente está en la fase Java/Aspose y no en la construcción Python de recursos/asignaciones.
- No se tocan rutas, flags, componentes, modelos ni UX BIM.
- Porcentaje TASK-1022 estimado tras este avance: `98%`.
- Porcentaje Fase 14 estimado tras este slice: `43%`.
- Porcentaje relativo global estimado tras este slice: `5.7%`.

## Seguimiento De Caché Real MPP 2026-04-10
- Se valida la caché del exportador `.mpp` clásico con una prueba real sobre `Santiago Bermeo` (`empresa_id=3`, `proyecto_id=7`, `presupuesto_id=13`) después de limpiar `backend/.runtime_mpp_cache`.
- La primera exportación real completa en `12.829 s` y genera `551936` bytes; la segunda exportación consecutiva sobre el mismo cronograma completa en `0.007 s` con el mismo tamaño.
- La mejora confirma que el binario `.mpp` queda reutilizable cuando no cambian cronograma, runner ni plantilla, reduciendo exportaciones repetidas a tiempo prácticamente instantáneo.
- No se tocan rutas, flags, componentes, modelos ni UX BIM.
- Porcentaje TASK-1022 estimado tras este avance: `99%`.
- Porcentaje Fase 14 estimado tras este slice: `44%`.
- Porcentaje relativo global estimado tras este slice: `5.8%`.
