# TASK-1057 - QA Y Documentación Del Workbench Operativo Gantt

## Modo
GIPROY CLASICO

## Estado
DONE

## Objetivo
Verificar que la reorganización del grid, el splitter y el menú contextual del Gantt clásico funcionan sin romper cálculo, scroll, dependencias ni exportaciones.

## Implementacion
- Build frontend.
- Revisión de coherencia visual.
- Documentación en changelog y cierre del task rector.

## Validacion
- `npm run build` OK en `frontend`.
- Validada no interferencia BIM a nivel de archivos tocados.

## Seguimiento De Corrección Runtime 2026-04-13
- Se corrige un fallo runtime del Gantt clásico que impedía abrir el timeline por una referencia de selección agrupada usada sin definición dentro del render de filas.
- También se corrige una segunda causa runtime por orden de inicialización: los listeners globales del timeline ya no leen callbacks de dependencias antes de que existan en el cuerpo del componente.
- Se corrige una tercera regresión de alcance: `createDependencyFromSourceToTarget` vuelve a vivir dentro de `CronogramaGantt`, evitando que el bundle de producción falle por callback no definido al cerrar enlaces entre tareas.
- Se corrige una cuarta causa runtime en React: las vistas fallback del Gantt (`loading`, `sin presupuesto`, `sin cronograma`, `sin filas`) dejan de cortar el cuerpo del componente antes de terminar de registrar hooks.
- El modal clásico `Rendimientos operativos` incorpora un selector visual `APU / Línea presupuesto` para alternar la lectura de cantidades y tiempos de recursos sin modificar cálculo, guardado ni persistencia.
- El resumen superior del modal añade una card explícita `Cantidad línea presupuesto` para dejar visible la referencia presupuestaria usada por la lectura analítica.
- Se ajusta la UX del selector para que la señal del modo `Presupuesto` viva en la card superior `Cantidad presupuesto`, sin cambiar los encabezados de columnas de la tabla.
- Se corrige la proyección visual del modo `Presupuesto`: cantidades y tiempos de recurso ahora usan `cantidad del recurso en el APU × cantidad de presupuesto`, en lugar de mostrar solo la cantidad de presupuesto.
- Se retira la notación dual `proyectada (base APU)` en la celda de cantidad del modo `Presupuesto` para mantener una lectura más limpia; la referencia presupuestaria visible sigue anclada a la card superior.
- Se conserva el valor base del recurso en el tooltip de cantidad cuando el modal está en modo `Presupuesto`, evitando ruido visual permanente en la celda.
- Se corrige una regresión de unidades en el modal: cada recurso vuelve a resolver su unidad propia y la presentación queda normalizada en minúsculas.
- Se elimina además la transformación visual `uppercase` en la columna de unidad del modal, para que la normalización en minúsculas se vea realmente en pantalla.
- Se blinda el último tramo de presentación: la celda y el tooltip de `Unidad` normalizan el valor justo antes de renderizarlo, evitando que una rama residual vuelva a mostrar abreviaturas en mayúsculas.
- Se corrige la proyección económica del bloque `Totales visibles del APU`: el indirecto vuelve a calcularse desde la referencia clásica del APU y arrastra correctamente `Precio Total APU` y `Precio en Presupuesto` en el modal del Gantt.
- Se alinea además la autoridad del porcentaje indirecto con la capa clásica real: el modal prioriza `proyecto/revisión`, luego `presupuesto`, luego `base de trabajo`, y solo deja el APU persistido como fallback defensivo.
- Se corrige el cableado del porcentaje de indirectos del presupuesto activo: el Gantt ahora pasa `selectedBudgetDetail` al modal para leer el `indirectos_porcentaje` real del presupuesto detallado y no depender del resumen corto `selectedBudget`.
- Se ajusta el bloque económico del modal a la fórmula oficial de presupuesto: primero redondea el `precio unitario` funcional y solo después calcula el `precio total` de línea, evitando desvíos como `117,19` frente a `117,12`.
- Validación adicional ejecutada: comprobación explícita del caso `112,62 × 1,04 = 117,12` con el mismo orden de operaciones del backend clásico.
- Revisión completa del Gantt clásico: el panel lateral `Proyección visible en Gantt` y el resumen `Confirmar cambios > Impacto económico visible` dejan de usar costo directo observacional y pasan a usar la misma fórmula funcional del presupuesto activo.
- Se centraliza además una autoridad única del `% indirecto` para esos bloques visibles del Gantt: `presupuesto detallado activo -> presupuesto resumido -> proyecto -> base de trabajo`.
- Se corrige una inconsistencia de unidad en el grid base del Gantt clásico: la duración visible deja de etiquetarse como `Horas` cuando es menor a una jornada y pasa a mantenerse siempre en `Días`, alineada con el modal de análisis; las horas quedan solo como equivalencia contextual en el tooltip.
- Se corrige un `ReferenceError` en producción: la carga defensiva de `base_trabajo` del proyecto dentro de `CronogramaGantt` deja de referenciar un `empId` inexistente en ese componente y vuelve a usar el contrato clásico seguro del cliente API.
- El grid base del Gantt clásico incorpora un selector visual de magnitud de duración directamente sobre el chip `Tipo dur.`; permite alternar `Horas`, `Días` y `Semanas` sin tocar persistencia ni fórmula operativa.
- La magnitud elegida se aplica de forma homogénea a la lectura visible principal del Gantt clásico: columna `Dur.`, chip `Tipo dur.`, contexto de tarea y resúmenes de duración asociados.
- Se compacta además la altura operativa del contenedor principal del Gantt clásico para eliminar espacio muerto en el borde inferior y maximizar el área útil de trabajo sin romper scroll ni panel contextual.
- Se afina el ajuste anterior: la altura del workspace del Gantt clásico deja de estirarse siempre al máximo disponible y pasa a adaptarse al número real de filas visibles, manteniendo tope por viewport.
- Las filas EDT del grid clásico dejan de mostrar `Sin fecha` cuando tienen descendencia programada: ahora heredan la fecha mínima visible de sus hijas y la presentan en formato corto `dd/mm/yyyy`.
- Se corrige una desincronización de lectura entre el grid izquierdo y el timeline derecho del Gantt clásico: ambos carriles comparten ya el mismo modelo de resaltado contextual para fila activa, relacionadas, predecesoras, sucesoras y diferencias confirmadas.
- Se endurece además el menú contextual de barras del Gantt clásico: `Ajuste fino de inicio`, `Gestionar dependencias` y `Añadir sucesora` pasan a ejecutarse después del cierre del popover para evitar que queden absorbidas por el propio overlay.
- Se afina la sincronía visual del Gantt clásico: el realce de fila completa deja de aplicarse a relaciones genéricas de rama y se limita a la fila activa y a relaciones explícitas (predecesoras/sucesoras), evitando falsos descuadres entre carriles.
- Se añade una apertura inmediata del menú contextual directamente desde la barra del Gantt clásico, evitando depender exclusivamente del `double click` y reduciendo la latencia percibida para acceder a acciones rápidas.
- Se corrige un runtime adicional del Gantt clásico: la rama de render del timeline derecho declara ahora su propio `hasExplicitRelationHighlight`, evitando el `ReferenceError` productivo al abrir la vista.
- Se recompila el frontend clásico tras esa corrección y el bundle productivo del Gantt queda regenerado sin errores de build, dejando la reparación incluida en artefacto.
- Se corrige la causa estructural de la desincronización vertical entre grid izquierdo y timeline derecho: la sincronización de altura deja de fijarse sobre la celda interior del timeline y pasa a aplicarse sobre la fila completa, eliminando el `+1px` acumulativo que introducía el borde inferior del wrapper derecho.
- Smoke técnico ejecutado sobre el contrato de sincronía: queda una única referencia `rowTimelineRefs` por fila y la geometría de dependencias vuelve a medir la fila timeline real ya sincronizada.
- Se endurece además el layout del Gantt clásico con un contrato determinista de altura por tipo de fila: agrupaciones EDT y partidas calculables comparten ahora alturas fijas replicadas en ambos carriles, evitando que pequeños cambios de padding/medición vuelvan a acumular deriva vertical.
- Se mejora la interacción de barras del Gantt clásico: el acceso a `Acciones rápidas` deja de depender del `double click` como camino principal y pasa a mostrarse como icono contextual en hover dentro del tramo visible actual de la barra, incluso cuando la barra está parcialmente recortada por el zoom o el scroll horizontal.
- Se añade una captura reactiva del viewport horizontal del timeline para posicionar ese icono según la porción realmente visible de cada barra, manteniendo el look and feel y sin alterar dependencias ni cálculo.
- Se elimina el `double click` de las barras del Gantt clásico para evitar conflictos de interacción; la apertura del menú queda unificada en el icono contextual visible en hover/focus.
- Se separan además los botones de `Acciones rápidas` y `Crear dependencia` para evitar solapes en barras cortas o muy recortadas, reservando espacio de seguridad entre ambos.
- El menú contextual clásico endurece su cierre: ahora responde de forma fiable a `Escape` y a clic fuera del popover usando listeners en captura sobre `document`.
- Se corrige el cableado efectivo de acciones rápidas en el Gantt clásico `split`: `Ajuste fino de inicio` enfoca explícitamente el `datetime-local` real de la fila visible, `Gestionar dependencias` abre el panel inline de dependencias de esa misma fila y `Añadir sucesora` deja una señal visual persistente sobre la barra origen para evitar que parezca una acción muerta.
- Smoke técnico adicional: revisión del flujo completo menú -> estado -> control inline para las tres acciones, más validación de build de producción.
- Se simplifica el menú rápido del Gantt clásico: se retira `Gestionar dependencias` por redundante y `Añadir sucesora` pasa a ofrecer dos caminos en el propio popover, `Visual` o `Por item`.
- La variante `Por item` acepta número de item o código EDT, permite elegir tipo de enlace (`FI/II/FF/IF`) y reutiliza el mismo helper real de creación de dependencias para no duplicar reglas ni validaciones.
- Se corrige el enfoque de UX para creación de sucesoras: `Añadir sucesora` deja de intentar resolver el alta dentro del popover estrecho de `Acciones rápidas` y pasa a reutilizar un modal clásico dedicado con el mismo lenguaje visual del editor de dependencias.
- Ese modal añade el campo `Item o código EDT` para resolver la tarea destino, mantiene edición de `Tipo` y `Lag días`, y conserva una alternativa de `Selección visual` sin salir del flujo clásico del Gantt.
- Smoke técnico ejecutado sobre el nuevo flujo modal: apertura desde barra, cierre por `Escape` o clic fuera, resolución por item/código y compilación productiva sin regresiones de runtime.
- La corrección queda confinada a `frontend/src/components/projects/CronogramaGantt.jsx`, sin tocar backend común, rutas BIM, flags BIM ni UX BIM.
- Se restablece la lectura visual de selección contextual en la rama split del timeline, diferenciando además filas relacionadas, predecesoras y sucesoras.
- Validación ejecutada: `npm run build` OK en `frontend`.
