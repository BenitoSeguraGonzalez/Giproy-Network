# TASK-1058 - Renumeración Visible De Item En Gantt Clásico Incorporando EDT

## Modo
GIPROY CLASICO

## Estado
DONE

## Objetivo
Corregir la columna `Item` del Gantt clásico para que la numeración visible tenga en cuenta la estructura `EDT` y no solo las líneas calculables, manteniendo separados `Item` y `Cod EDT` y sin romper dependencias, referencias operativas ni UX del módulo.

## Alcance
- `frontend/src/components/projects/CronogramaGantt.jsx`
- Numeración visible del grid izquierdo.
- Referencias visibles derivadas de `Item` en menús, tooltips y acciones de dependencias del Gantt clásico.
- Sin backend nuevo.
- Sin BIM.

## Subtasks
- `TASK-1059`: definir el contrato de numeración visible `EDT + líneas`.
- `TASK-1060`: propagar la nueva referencia visible en el Gantt clásico sin romper el `Cod EDT`.
- `TASK-1061`: QA técnica, smoke visual y documentación final.

## Criterios De Cumplimiento
- La columna `Item` refleja la jerarquía visible del Gantt incluyendo ramas `EDT`.
- Las líneas calculables dejan de numerarse como un serial plano global aislado de la estructura.
- `Cod EDT` sigue siendo una columna separada y no se mezcla con `Item`.
- Las referencias operativas que usan `Item` siguen siendo coherentes con la numeración visible.
- La solución queda confinada al carril clásico del Gantt.

## No Interferencia BIM
- Sin rutas BIM.
- Sin feature flags BIM.
- Sin componentes BIM.
- Sin acoplamientos nuevos desde Gantt clásico hacia BIM.

## Implementacion
- Se ajusta el contrato del helper de numeración visible en `frontend/src/components/projects/CronogramaGantt.jsx` para que `Item` se asigne sobre todas las filas visibles del grid, incluyendo ramas `EDT`.
- Las filas calculables conservan la resolución operativa por `Item` para dependencias, evitando que las ramas `EDT` pasen a ser destinos inválidos en acciones del Gantt.
- La columna `Item` del grid izquierdo deja de ocultarse en filas no calculables y pasa a mostrar la referencia visible completa del árbol mostrado.

## Validacion
- `npm run build` OK en `frontend`.
- Verificada no interferencia BIM a nivel de archivos y alcance tocado.
