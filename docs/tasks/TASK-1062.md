# TASK-1062 - Homologación Visible De Codificación Entre Presupuesto, Valorado Y Gantt Clásico

## Modo
GIPROY CLASICO

## Estado
DONE

## Objetivo
Homologar la lectura visible de codificación entre `Presupuesto`, `Cronograma Valorado` y `Cronograma Gantt`, respetando el contrato clásico ya aprobado: `Cod EDT` como referencia estructural, `codigo_item` como referencia de línea/APU y una capa visible coherente para operación humana.

## Alcance
- `frontend/src/components/projects/Cronogramas.jsx`
- `frontend/src/components/presupuestos/LineasPresupuestoTab.jsx`
- Contrato visible de grillas y resúmenes relacionados.
- Documentación `TASK + CHANGELOG`.
- Sin cambios de persistencia.
- Sin backend nuevo en este primer frente.
- Sin BIM.

## Subtasks
- `TASK-1063`: separar visualmente `Item` y `Cod EDT` en `Cronograma Valorado`.
- `TASK-1064`: alinear la lectura visible equivalente en `Presupuesto`.
- `TASK-1065`: QA transversal, smoke visual y cierre documental del frente.

## Criterios De Cumplimiento
- `Cronograma Valorado` deja de usar una única columna ambigua para mezclar `codigo_item` y `codigo EDT`.
- La semántica visible queda alineada con el patrón ya aprobado en `Gantt`.
- `codigo_item` persiste intacto como identificador de línea presupuestaria/APU.
- `Cod EDT` persiste como referencia estructural separada.
- La implementación no rompe cálculo, distribución, reportes visibles ni UX clásica.

## No Interferencia BIM
- Sin rutas BIM.
- Sin flags BIM.
- Sin UX BIM.
- Sin acoplamientos nuevos hacia BIM.

## Cierre
- `TASK-1063` completó la separación visible `Item / Cod EDT` en `Cronograma Valorado`.
- `TASK-1064` extendió la misma semántica visible al árbol operativo de `Presupuesto`.
- `TASK-1065` cerró la homogenización en resúmenes, confirmaciones y apoyos visuales relacionados del carril clásico.
- El frente queda cerrado sin tocar backend, sin cambiar persistencia y sin interferir con BIM.
