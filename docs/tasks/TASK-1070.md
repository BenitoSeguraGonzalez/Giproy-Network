# TASK-1070 - Implementación Por Slices De Sincronización Real Valorado Y Gantt

## Modo
GIPROY CLASICO

## Estado
DONE

## Objetivo
Ejecutar por slices la implementación de la sincronización real entre `Cronograma Valorado` y `Gantt` siguiendo la especificación funcional ya congelada.

## Documento Rector
- `docs/architecture/CRONOGRAMA_VALORADO_GANTT_SYNCHRONIZATION_FUNCTIONAL_SPEC.md`

## Alcance
- Base temporal diaria.
- Reagrupación visible por periodos.
- Mandato del `Cronograma Valorado` sobre subbarras del `Gantt`.
- Persistencia, interacción, reconciliación y estados visibles.
- Preservación del contrato con `Presupuesto/APU`.

## Subtasks
- `TASK-1071` - Contrato temporal base diaria y reagrupación visible.
- `TASK-1072` - Motor de derivación del Valorado desde base diaria.
- `TASK-1073` - Contrato de persistencia de subbarras y sesión operativa.
- `TASK-1074` - Representación inicial de subbarras en Gantt.
- `TASK-1075` - Interacción de división, reagrupación y validación contra cupos.
- `TASK-1076` - Reconciliación, estados visibles, QA y reporting.

## Criterios De Cumplimiento
- Cada slice entra sin romper `Gantt`, `Valorado` ni `Presupuesto/APU`.
- No se introducen mutaciones silenciosas entre capas.
- La UX sigue alineada al Gantt clásico vigente.
- BIM permanece intacto.

## No Interferencia BIM
- Sin cambios en carril BIM.
- Sin UX BIM.
- Sin acoplamientos nuevos hacia BIM.

## Orden Recomendado
1. `TASK-1071`
2. `TASK-1072`
3. `TASK-1073`
4. `TASK-1074`
5. `TASK-1075`
6. `TASK-1076`

## Punto De Reentrada
- Si una sesión futura debe continuar este frente, arrancar aquí y no desde interpretación libre del histórico.

## Progreso
- `TASK-1071` completada: el contrato temporal base diaria y la reagrupación visible ya quedaron implementados en backend y preview frontend.
- `TASK-1072` completada: el `Cronograma Valorado` ya deriva desde base diaria con gobierno explícito para líneas `solo materiales`, cobertura parcial visible, reporting alineado y frontera funcional cerrada entre `Desde Gantt` y modos valorados no derivados.
- `TASK-1073` completada: el backend común ya persiste y rehidrata `gantt_session`, `gantt_subbars`, `manual_temporal_window` y el resumen `gantt_operational`, con QA sobre campos de reconciliación de sesión.
- `TASK-1074` completada: el `Gantt` clásico ya ofrece lectura pasiva madura de subbarras con selección, navegación en fila/contexto, resúmenes agregados, modo operativo explícito y navegación secuencial sin entrar todavía en edición manual.
- `TASK-1075` completada: el `Gantt` clásico ya cubre la interacción principal de subbarras desde `Contexto tarea` con ventana manual, siembra inicial, división, reagrupación, reequilibrado por cupo, origen visible del tramo y `Situación operativa` previa a aceptación.
- `TASK-1076` completada: la previa `Confirmar cronograma`, los estados visibles del `Gantt` y el reporting común ya exponen reconciliación operativa coherente, incluyendo `tramos en borrador`, `Programación manual`, conflictos frente al `Valorado` y QA transversal del carril integrado.
- Frente cerrado: la sincronización real `Valorado <-> Gantt` queda implementada por slices en capa clásica, con contrato preservado frente a `Presupuesto/APU` y sin contaminación BIM.
