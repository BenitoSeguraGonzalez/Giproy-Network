# TASK-1083 - QA Integrada Y Limpieza Documental Del Frente De Tramos Iniciales

## Modo
GIPROY CLASICO

## Estado
DONE

## Objetivo
Cerrar la QA y la documentación del frente de `tramos iniciales valorados`, eliminando contradicciones entre `TODO.md`, tasks legacy y estado real del código.

## Dependencia
- `TASK-1080`
- `TASK-1081`
- `TASK-1082`

## Alcance
- QA backend del endpoint interpadre.
- QA end-to-end del carril `Valorado -> Gantt -> recarga`.
- Limpieza documental de `TASK-1079` y artefactos de mock heredados.

## Criterios De Cumplimiento
- Existe QA de endpoint para `merge-interparent-subbars` cubriendo aceptación y rechazo del contrato.
- La trazabilidad legacy contaminada deja de presentarse como fuente vigente.
- Los artefactos mock obsoletos dejan de inducir un falso estado del frente.
- Se mantiene la no interferencia BIM.

## Progreso
- Se añade QA de endpoint con `TestClient` para `merge-interparent-subbars`, cubriendo caso válido y deriva rechazada por mandato de `tramo inicial`.
- Se retira `backend/app/services/test_cronograma_trabajo.py`, que seguía arrastrando un contrato mock obsoleto.
- `TASK-1079` deja de funcionar como pseudo-fuente de verdad y pasa a archivo `ARCHIVED` con referencia explícita a `TASK-1080` a `TASK-1083`.
- La QA de servicio ya cubre fusiones interpadre encadenadas dentro del mismo `parent_initial_id`, validando que la suma por padre siga cuadrando aunque existan varios subtramos hijos.
- La recarga backend del `Gantt` ya queda protegida por test: cuando una línea conserva `gantt_subbars` persistidas, la derivación automática desde `Valorado` no las sobrescribe al reconstruir filas.
- La QA de endpoint ya cubre también dos fusiones consecutivas sobre la misma línea, verificando que la redistribución valorada sigue siendo coherente entre sesiones sucesivas.
- El frente queda ya saneado a nivel documental: `TASK-1079` archivada, test mock antiguo retirado y checklist rector alineado con el estado real del código.

## No Interferencia BIM
- Sin BIM.
