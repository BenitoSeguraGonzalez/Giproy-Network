# TASK-1092 - QA Y Guardarraíles De Sincronización Para Subbarras Gantt

## Modo
GIPROY CLASICO

## Estado
CLOSED

## Objetivo
Congelar con smoke tests y QA técnica que la selección múltiple, la unión y la división de subbarras respetan el contrato de sincronización con `Cronograma Valorado`.

## Dependencia
- `TASK-1089`
- `TASK-1090`
- `TASK-1091`

## Criterios De Cumplimiento
- Existe smoke test para unión secuencial con ancla temporal en la primera selección.
- Existe smoke test para división por porcentaje con continuidad temporal exacta.
- Uniones o divisiones dentro del mismo `parent_initial_id` no cambian distribución económica del `Valorado`.
- Uniones entre distintos `parent_initial_id` mantienen el flujo explícito de reajuste del `Valorado`.
- `npm run build` queda en verde.

## Progreso
- Queda abierto para ampliar el smoke test existente de subbarras y cerrar el frente con validación técnica reproducible.
- `smoke-cronogramas-gantt-subbars.mjs` sigue en verde tras introducir unión secuencial anclada y división por porcentaje.
- `npm run build` sigue en verde tras la nueva interacción múltiple y el desacople entre selección de línea y `Contexto tarea`.
- El smoke ya congela además guardarraíles de multiselección preservada/degradada, reordenación por línea y reajuste interpadre del `Cronograma Valorado`.

## Validación Ejecutada 2026-04-23
- `npm run build` en `frontend/` OK.
- `node scripts/smoke-cronogramas-gantt-subbars.mjs` en `frontend/` OK.
- `..\\.venv\\Scripts\\python.exe -m pytest app/tests/test_cronograma_trabajo_duration_model.py -q` en `backend/` OK (`16 passed`).

## Porcentaje De Terminación
- Avance anterior de `TASK-1092`: 85%.
- Avance nuevo de `TASK-1092`: 100%.
- Base de estimación: los criterios de smoke para unión anclada, división con continuidad exacta, guardarraíles de selección y validación técnica reproducible quedan cubiertos y verificados otra vez en esta sesión.

## Licencias
- Sin nueva capacidad licenciable.
- Sin cambios en límites, permisos o middleware.

## No Interferencia BIM
- Sin cambios en rutas, componentes o contratos BIM.
- Sin activación de UX BIM.

## Cierre Documental
- `2026-04-24`: se ratifica como slice histórico absorbido al cerrar documentalmente `TASK-1089`; permanece cerrada al 100% y deja de listarse como asociada a un frente activo real.
