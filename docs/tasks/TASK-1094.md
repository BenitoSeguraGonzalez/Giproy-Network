# TASK-1094 - Zoom Visual Inmediato Y Commit Diferido Del Gantt

## Modo
GIPROY CLASICO

## Estado
IN_PROGRESS

## Objetivo
Introducir un carril de zoom de dos fases en `CronogramaGantt`: preview visual inmediata durante la interacción y commit estructural diferido cuando el gesto se estabiliza.

## Dependencia
- `TASK-1093`

## Criterios De Cumplimiento
- El slider de zoom actualiza la vista sin recalcular toda la geometría en cada cambio.
- Los botones `+`, `-`, `100%` y `Todo` siguen funcionando.
- La confirmación del zoom mantiene el ancla del viewport.
- La funcionalidad existente del timeline permanece intacta.

## Progreso
- `CronogramaGantt.jsx` ya introduce preview visual inmediata con commit estructural diferido.
- Los botones `+`, `-`, `100%` y `Todo` siguen entrando por el mismo control, pero el carril pesado del layout ya no se dispara en cada tick del slider.
