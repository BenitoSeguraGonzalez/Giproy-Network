# TASK-1104 - QA Y Documentación Del Drag Gantt Clásico

## Modo
GIPROY CLASICO

## Estado
DONE

## Objetivo
Congelar con pruebas ligeras y documentación el nuevo contrato de drag del `Gantt` clásico.

## Dependencia
- `TASK-1101`

## Criterios De Cumplimiento
- Existe smoke técnico que cubre selección preservada y arrastre múltiple.
- `eslint` sobre los archivos tocados queda en verde.
- `npm run build` queda en verde.
- `CHANGELOG` y `TASKS` reflejan el slice implementado.

## Progreso
- Slice abierto para dejar trazabilidad y guardarraíles de no regresión sobre el frente.
- Se amplía el smoke `smoke-cronogramas-gantt-subbars.mjs` con casos de preservación de selección y drag múltiple.
- `node scripts/smoke-cronogramas-gantt-bootstrap.mjs`, `smoke-cronogramas-gantt-subbars.mjs`, `smoke-cronogramas-gantt-virtualization.mjs`, `smoke-cronogramas-gantt-zoom.mjs` y `npm run build` quedan en verde.
- `eslint` sobre `CronogramaGantt.jsx` sigue arrastrando deuda histórica del archivo monolítico; no se detecta como regresión del slice nuevo.
