# TASK-1109 - Smokes Reales Y Performance Del Gantt Clásico

## Modo
GIPROY CLASICO

## Estado
DONE

## Objetivo
Endurecer el `Gantt` clásico con smoke tests reales sobre la empresa `Santiago Bermeo` y optimizaciones focalizadas de rendimiento en drag, zoom y navegación visible, sin tocar BIM.

## Dependencia
- `TASK-1101`
- `TASK-1105`

## Subtasks
- `TASK-1110` - Smoke real backend para cronogramas/Gantt sobre `Santiago Bermeo`.
- `TASK-1111` - Optimización frontend de interacción visible en `CronogramaGantt`.
- `TASK-1112` - Validación técnica, medición y documentación.

## Criterios De Cumplimiento
- Existe al menos un smoke test real ejecutable contra `Santiago Bermeo` para el carril clásico de cronogramas.
- El smoke real valida contrato operativo de `get_schedule` y `factory reset`.
- El drag'n drop clásico reduce presión de render en interacción continua.
- El slider de zoom reduce presión de render durante la interacción continua.
- Se preserva el comportamiento funcional del Gantt clásico.
- No se toca BIM ni se añaden dependencias hacia BIM.

## Resultado
- Se añade `backend/scripts/smoke_test_cronogramas_gantt_real.py` para validar el carril real de cronogramas/Gantt sobre `Santiago Bermeo`.
- El smoke real mide backend clásico y valida el contrato operativo de `factory reset` con una sola subbarra 100% por línea.
- `CronogramaGantt.jsx` amortigua ahora el `dragPreview` mediante `requestAnimationFrame` y evita comprometer un render React crudo por cada `pointermove`.
- El slider principal de zoom amortigua ahora sus cambios mediante `requestAnimationFrame`, manteniendo commit final explícito y ancla temporal.
- El motor de drag clásico se replantea para que los listeners globales lean estado vigente por refs y no dependan de reinstalaciones continuas durante el render.
- Validación completada con smoke real, smokes frontend y `npm run build`, sin tocar BIM.
