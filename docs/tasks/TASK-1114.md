# TASK-1114 - Creación Inicial Del Cronograma En Modo Gantt

## Modo
GIPROY CLASICO

## Estado
DONE

## Tipo
feature

## Prioridad
critical

## Descripción
Cambiar el contrato backend/frontend de creación inicial para que `Cronograma Valorado` y `CronogramaTrabajo` nazcan en `modo gantt`, con una sola subbarra operativa `100%` por línea calculable y sin depender de `factory reset`.

## Archivos Afectados
- `backend/app/api/endpoints/cronogramas.py`
- `backend/app/services/cronograma_trabajo.py`
- `backend/app/schemas/cronograma.py`
- `frontend/src/components/projects/Cronogramas.jsx`
- `backend/app/tests/*cronograma*`

## Checklist

[x] análisis
[x] implementación
[x] pruebas
[x] documentación

## Resultado
- El alta inicial del cronograma clásico deja de nacer en `homogeneo` y pasa a `gantt`.
- `_ensure_schedule(...)` siembra una sola subbarra `100%` por cada línea calculable con trazabilidad `initial_creation_seed`.
- El frontend clásico adopta `gantt` como fallback funcional para el `Valorado`.
