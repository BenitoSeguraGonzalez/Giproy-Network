# TASK-1191 - Añadir Fin real al grid Gantt y retirar avance

## Estado
DONE

## Modo
GIPROY CLASICO

## Objetivo
Mostrar en el grid izquierdo del Gantt clásico la fecha y hora real de fin de cada tarea, calculada con duración efectiva, modificadores y predecesoras, y retirar la columna `% Av.` del grid porque no forma parte del uso operativo actual del Gantt.

## Alcance
- Capa clásica de Cronograma Gantt.
- Sin cambios en backend ni persistencia de datos.
- Sin activación ni exposición de UX BIM.

## Cambios
- La cabecera del grid sustituye `% Av.` por `Fin`.
- Las dos ramas de render del grid muestran `Fin real calculado` usando `resolveGanttRowFinishDate(...)`.
- Se conserva la trazabilidad interna de `progress_pct` para no romper importaciones, snapshots ni reconciliaciones existentes, pero deja de mostrarse/editase en el grid.

## Validación
- `npm run build`

## No interferencia BIM
Sin cambios en rutas, componentes, APIs ni documentación operativa BIM.
