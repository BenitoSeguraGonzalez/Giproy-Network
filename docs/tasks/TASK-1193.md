# TASK-1193 - Guardar preferencia visual de tareas cortas por usuario y revisión

## Estado
DONE

## Modo
GIPROY CLASICO

## Objetivo
Corregir el alcance de la opción `Tareas cortas = 1 día visual` para que se guarde como preferencia visual por usuario, proyecto y revisión, no como configuración compartida del cronograma.

## Alcance
- Capa clásica de Cronograma Gantt.
- Persistencia local de preferencia UI por usuario/proyecto/revisión.
- Sin cambios en backend ni contratos BIM.

## Cambios
- Se crea una clave local `giproy:gantt:visual-preferences:{usuario}:{proyecto}:{id}:rev-{revision}`.
- El check se guarda inmediatamente para el usuario actual y la revisión activa.
- El `configDraft` runtime recibe la preferencia solo para calcular la geometría visual.
- `Aplicar configuración` sigue guardando únicamente la configuración compartida del cronograma y no persiste esta preferencia visual en backend.

## Validación
- `node scripts/smoke-cronogramas-gantt-subbars.mjs`
- `npm run build`

## No interferencia BIM
Sin cambios en rutas, componentes, APIs ni documentación operativa BIM.
