# TASK-1245 - QA técnica y no regresión de secuenciación gráfica tarea-a-tarea en Gantt clásico

## Modo
GIPROY CLASICO

## Estado
DONE

## Tipo
qa / trazabilidad

## Prioridad
high

## Objetivo
Validar que la secuenciación gráfica tarea-a-tarea del Gantt clásico queda integrada sin romper interacción, shortcuts ni geometría de dependencias.

## Validación ejecutada
- `npm run build` en `frontend/` OK.
- `node scripts/smoke-cronogramas-gantt-subbars.mjs` en `frontend/` OK.
- `node scripts/smoke-cronogramas-gantt-dependencies.mjs` en `frontend/` OK.

## Cobertura validada
- El botón gráfico de secuenciación aparece en `hover/focus` como affordance hermano de `Opciones`.
- El botón se recoloca con fallback geométrico a la derecha visible cuando la superficie del Gantt es pequeña.
- El arrastre visual conserva línea temporal viva hasta `pointerup`.
- La persistencia por defecto crea `FS`.
- El carril textual `Predecesoras` conserva la regla de shortcuts separados por coma.
- El editor gráfico de dependencia permite ajustar tipo/lag o romper la relación.
- No se tocan backend, licencias, multiempresa ni BIM.

## Porcentaje de terminación
- Avance inicial: 0%.
- Avance final: 100%.
