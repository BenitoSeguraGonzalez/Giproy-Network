# TASK-1408 - Adecuacion visual reversible de Datos Generales

## Modo
GIPROY CLASICO

## Estado
DONE

## Tipo
frontend

## Prioridad
alta

## Objetivo
Ampliar `Proyectos > Datos Generales` usando como referencia `tmp/adicionar/formulario_datos_proyecto_giproy.html`, sin eliminar la experiencia actual y sin crear acoplamientos hacia BIM.

## Alias funcional
- `Datos Generales` corresponde al carril clasico implementado como `Datos de Proyecto` en `frontend/src/components/projects/DatosProyecto.jsx`.

## Alcance autorizado
- Crear una variante UI reversible `general-v2` sobre la logica actual.
- Mantener la persistencia existente de `ProyectoDetalle`.
- Reorganizar el cuerpo en dos secciones con desplazamiento independiente:
  - formulario principal aproximado `2/3`
  - imagenes, mapa y documentacion aproximado `1/3`
- Incorporar capitulos colapsables inspirados en el HTML de referencia.
- Reutilizar componentes actuales, look & feel GiProy y reglas de permisos existentes.

## Fuera de alcance
- No ampliar base de datos en este slice.
- No reabrir contratos de reportes cerrados.
- No modificar Gantt, Cronogramas, Presupuesto ni BIM.
- No introducir validaciones de licencia nuevas en frontend.

## Impacto licencias
- Sin impacto funcional.
- No se crean capacidades facturables ni limites nuevos.
- Las validaciones de acceso siguen dependiendo de permisos/backend existentes.

## Impacto UI
- Alto, acotado a `Proyectos > Datos Generales`.
- La UI pasa a un esquema de capitulos colapsables y panel documental independiente.

## No interferencia BIM
- Confirmada como requisito de ejecucion.
- El slice no toca rutas, flags, componentes ni documentacion operativa BIM.

## Porcentaje de terminacion
- Avance inicial: 5%.
- Avance intermedio: 10%.
- Avance final: 100%.
- Base de cierre: variante UI implementada, build frontend en verde, documentacion sincronizada y no interferencia BIM/licencias confirmada.

## Checklist de cierre
- Variante `general-v2` implementada sin romper autosave.
- Campos actuales reubicados en capitulos colapsables.
- Panel derecho con mapa, imagen y zonas documentales preparado.
- Build frontend en verde.
- CHANGELOG, HANDOFF, TODO, WORK_MODE_STATE y project_state sincronizados.

## Validacion
- `npm run build` en `frontend/`.

## Resultado
- El cuerpo de `DatosProyecto.jsx` queda organizado en dos paneles independientes.
- La columna izquierda contiene capitulos colapsables para identificacion, especificaciones, alcance, informacion contractual, plazos, ubicacion y objetivos.
- La columna derecha concentra imagen referencial, croquis/documentos preparados y mapa/georreferenciacion.
- Los campos del HTML de referencia sin contrato backend quedan visibles como reservas de ampliacion y no generan persistencia paralela.
- Hotfix visual posterior: se elimina la banda informativa `Acta de constitucion operativa` para que el formulario empiece directamente en los capitulos de captura.
- Hotfix visual posterior 2: los acentos laterales gruesos de las cabeceras pasan a una linea fina `zinc`, manteniendo el color corporativo en iconografia y evitando relieves de borde.
- Hotfix visual posterior 3: los controles de despliegue pasan de chevron suelto a boton circular suave con `ChevronRight`, alineado al patron usado en el landing de proyectos.
- Hotfix visual posterior 4: el boton circular de despliegue reduce su huella visual para verse mas discreto, manteniendo el mismo patron del landing y la rotacion al expandir.
- Saneamiento posterior: se elimina la UI antigua en standby de `DatosProyecto.jsx` y la variante actual queda como unica presentacion, sin tocar contratos de datos, autosave, reportes ni georreferenciacion.
- Ajuste visual posterior: `DatosProyecto.jsx` adopta superficies suaves y microcontroles mas cercanos al landing de `Proyectos`, homogeneizando cabeceras, tarjetas laterales y botones de despliegue.
- Hotfix visual posterior 5: `SearchableSelect` mueve sus menus a un portal fijo sobre `document.body` para evitar que los combobox queden recortados por contenedores con `overflow-hidden`.
- Slice funcional posterior: la seccion `7. Objetivos, restricciones y supuestos` deja de ser placeholder. `Objetivos clave`, `Restricciones conocidas` y `Supuestos iniciales` quedan como listados incrementales persistidos en `ProyectoDetalle`.
- Ajuste de edicion: todos los memos de `DatosProyecto.jsx` quedan con corrector ortografico activo.
- Persistencia real aplicada: la migracion `fd3e4f5a6b7c` queda ejecutada en la base y la seccion 7 pasa a cumplir la universalidad del modulo al guardarse en `proyecto_detalles` por `codigo_root` y `empresa_id`.
- Ajuste funcional posterior: restricciones y supuestos reutilizan la misma funcionalidad incremental de objetivos, serializando listas sobre los campos `Text` existentes y conservando compatibilidad con contenido historico por lineas.
