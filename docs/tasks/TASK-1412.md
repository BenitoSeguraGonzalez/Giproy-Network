# TASK-1412 - Datos Proyecto Alcance y Dimensionamiento Persistido

## Modo
GIPROY CLASICO

## Estado
DONE

## Tipo
frontend + backend + documentacion

## Prioridad
media

## Objetivo
Adecuar la seccion `3. Alcance y dimensionamiento` de `Datos de Proyecto` al HTML de referencia, manteniendo el look & feel actual de GiProy y agregando persistencia real para descripcion breve, alcance detallado y mediciones dependientes del selector rector.

## Alcance autorizado
- Actualizar `frontend/src/components/projects/DatosProyecto.jsx`.
- Ampliar `ProyectoDetalle` y schemas con campos de dimensionamiento.
- Crear migracion Alembic compatible con datos existentes.
- Mantener `area_terreno`, `area_construccion` y `objeto_contrato` sin renombrar para no romper reportes ni persistencia previa.
- Incorporar selector rector `tipo_medicion` con bloques condicionales.
- Incorporar alertas suaves de consistencia y resumen tecnico automatico.
- Documentar cambios en `docs/CHANGELOG.md` y `docs/STYLE_GUIDE.md`.

## Fuera de alcance
- No modificar BIM.
- No alterar licencias.
- No cambiar reportes Word/PDF salvo compatibilidad de datos existentes.
- No eliminar campos historicos.

## Impacto licencias
- Sin impacto funcional.
- No crea nuevas capacidades ni bypass de permisos.

## Impacto UI
- Cambia la composicion visual de la seccion 3.
- El selector de medicion gobierna los campos visibles.
- Se agregan avisos informativos cuando se detectan inconsistencias tecnicas.

## No interferencia BIM
- Confirmada.
- No se tocan componentes BIM, rutas BIM ni contratos BIM.

## Porcentaje de terminacion
- Avance inicial: 0%.
- Avance final: 100%.

## Validacion ejecutada
- `python -m py_compile backend/app/models/proyecto_detalle.py backend/app/schemas/proyecto_detalle.py backend/app/services/marketplace_checkout.py`.
- `alembic upgrade aa6b7c8d9e0f`.
- `npm run build` en `frontend/`.

## Resultado
- `Datos de Proyecto > 3. Alcance y dimensionamiento` queda alineado al HTML de referencia sin recuperar su estetica antigua.
- Se implementa selector rector de medicion con bloques condicionales para area, longitud, volumen, unidades y mixto.
- Se agregan descripcion breve, alcance detallado, alertas de consistencia y resumen tecnico automatico.
- Los nuevos campos quedan persistidos en `proyecto_detalles`.
