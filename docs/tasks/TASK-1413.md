# TASK-1413 - Migracion Global De Datepickers Y Datetimepickers Animados

## Modo
GIPROY CLASICO

## Estado
DONE

## Tipo
frontend + documentacion

## Prioridad
alta

## Objetivo
Sustituir los `input[type="date"]` y `input[type="datetime-local"]` visibles de la aplicacion por un componente compartido con estetica alineada a los combobox actuales de GiProy, popover animado y variante compacta para superficies densas como `CronogramaGantt`.

## Alcance autorizado
- Crear componente UI compartido para fecha y fecha-hora.
- Mantener `framer-motion`, `createPortal` y el lenguaje visual vigente de `SearchableSelect` / `AnimatedSelect`.
- Migrar los usos detectados en `DatosProyecto`, `Cronogramas`, `GanttParetoModal`, `Proyectos`, `MarketplaceAdminDashboard`, `AdminGlobalLicencias`, `AdminGlobalComunicados`, `AdminGlobalMantenimiento` y `CronogramaGantt`.
- Adaptar la navegacion/foco del `Gantt` si dependia de inputs nativos.
- Documentar el patron en `docs/STYLE_GUIDE.md`, `docs/CHANGELOG.md`, `docs/HANDOFF.md`, `docs/project_state.json`, `docs/runtime/WORK_MODE_STATE.json` y `TODO.md`.

## Fuera de alcance
- No tocar backend.
- No alterar licencias.
- No introducir dependencias nuevas.
- No modificar BIM.

## Impacto licencias
- Sin impacto funcional ni de permisos.

## Impacto UI
- Global sobre todos los campos de fecha y fecha-hora visibles de la app clasica.
- Los triggers de fecha pasan a leerse como combobox tecnicos del sistema.
- Los paneles de calendario dejan de depender del popup nativo del navegador.

## No interferencia BIM
- Confirmada.
- El frente es exclusivo de frontend clasico y documentacion.

## Porcentaje de terminacion
- Avance inicial: 0%.
- Avance final: 100%.

## Validacion prevista
- `npm run build` en `frontend/`.
- Inventario final sin `input[type="date"]` ni `input[type="datetime-local"]` residuales en UI productiva, salvo casos tecnicos internos justificados.

## Resultado ejecutado
- Familia compartida `date/datetime` alineada a combobox.
- Animacion de apertura consistente en toda la app.
- Variante compacta operativa para `CronogramaGantt`.
- Documentacion y trazabilidad cerradas al 100%.

## Validacion ejecutada
- `npm run build` en `frontend/` correcto.
- Migracion aplicada en `DatosProyecto`, `AdminGlobalComunicados`, `AdminGlobalMantenimiento`, `AdminGlobalLicencias`, `MarketplaceAdminDashboard`, `Proyectos`, `GanttParetoModal`, `Cronogramas` y `CronogramaGantt`.

## Cierre
- Impacto en licencias: ninguno.
- Impacto UI: global sobre datepickers y datetimepickers visibles.
- No interferencia BIM: confirmada.
