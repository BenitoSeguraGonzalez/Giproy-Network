# TASK-1441 - Sincronizar CPC de recursos APU en Desagregacion

## Estado
Completada

## Modo
GIPROY CLASICO

## Objetivo
Corregir la lectura de `Desagregacion` para que los recursos de APUs implicados expongan el `CPC` vigente del recurso en la base de proyecto, evitando falsos `N/D` cuando el recurso ya tiene `cod_cpc_id` asignado.

## Alcance autorizado
- Backend comun: contrato `APUResponse` / `RecursoMinResponse`.
- Backend comun: carga explicita de `Recurso.cpc` al consultar detalle de APU.
- Frontend clasico: mantener consumo actual de `linea.recurso.cpc` sin cambiar UX.
- QA: prueba de regresion del contrato APU con CPC.
- Documentacion: `CHANGELOG`, `TODO`, `HANDOFF`, `WORK_MODE_STATE` y `project_state`.

## Fuera de alcance
- No crear UI nueva.
- No cambiar reglas de calculo VAE ya aprobadas.
- No activar ni modificar UX BIM.
- No persistir reparaciones masivas de CPC.

## Avance
- Inicio: 0%.
- Apertura documental y diagnostico confirmado: 0% -> 20%.
- Implementacion backend y contrato de tipos: 20% -> 60%.
- QA backend y build frontend: 60% -> 90%.
- Cierre documental y runtime sincronizado: 90% -> 100%.
- Avance final: 100%.

## Impacto en licencias
- Sin nuevo modulo ni nueva capacidad comercial.
- Se conserva la politica actual de acceso a `APUs`, `Recursos`, `Presupuesto` y `Desagregacion`.
- La validacion sigue del lado backend mediante tenant/empresa activa en endpoints existentes.

## Impacto en UI
- Sin cambio visual.
- La pantalla clasica de `Desagregacion` debe dejar de marcar como incompleto un APU cuyo recurso ya trae CPC desde la base activa.

## No interferencia BIM
- Confirmada.
- No se toca capa BIM, rutas BIM, dependencias BIM ni feature flags BIM.

## Validacion prevista
- `python -m pytest app/tests/test_nested_apu_integrity.py -q` desde `backend/`
- `python -m py_compile backend/app/schemas/apu.py backend/app/repositories/apu.py`
- `npm run build` en `frontend/` si el cambio de tipos o contrato afecta build.

## Validacion ejecutada
- `python -m py_compile backend/app/schemas/apu.py backend/app/repositories/apu.py` OK.
- `python -m pytest app/tests/test_nested_apu_integrity.py -q` OK: 4 passed, 1 warning existente de Pydantic `Config` en `CodCPCResponse`.
- `npm run build` en `frontend/` OK; se mantiene aviso conocido de chunk grande `CronogramaGantt`.

## Cierre
- El contrato `APUResponse` expone `cod_cpc_id` y `cpc` dentro de `RecursoMinResponse`.
- La consulta de detalle APU carga explicitamente `Recurso.cpc`.
- `Desagregacion` conserva su consumo actual de `linea.recurso.cpc`, ahora alimentado por backend.
- Se ajusta la prueba antigua de APUs anidados para declarar la unidad APU `M2` requerida por la validacion vigente del servicio.
