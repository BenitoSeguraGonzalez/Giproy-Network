# TASK-1442 - Desagregacion CPC residual y carga batch de APUs

## Estado
Completada

## Modo
GIPROY CLASICO

## Objetivo
Corregir el caso residual donde `Desagregacion` sigue mostrando recursos de APUs como `SIN CPC` y reducir la lentitud de carga causada por peticiones secuenciales y recargas repetidas del detalle de presupuesto.

## Alcance autorizado
- Backend comun: endpoint batch para detalle de APUs con recursos y CPC.
- Frontend clasico: `DesagregacionTab` debe consumir los APUs por lote y estabilizar su cache local.
- QA: validacion backend/frontend focalizada.
- Documentacion: `CHANGELOG`, `TODO`, `HANDOFF`, `WORK_MODE_STATE` y `project_state`.

## Fuera de alcance
- No cambiar UI visual.
- No cambiar reglas VAE aprobadas.
- No activar ni tocar UX BIM.
- No crear reparacion masiva de datos.

## Avance
- Inicio: 0%.
- Diagnostico confirmado por captura y revision de flujo: 0% -> 20%.
- Implementacion backend/frontend: 20% -> 60%.
- QA backend/frontend: 60% -> 90%.
- Cierre documental y runtime sincronizado: 90% -> 100%.
- Avance final: 100%.

## Implementacion aplicada
- Nuevo contrato `APUBatchDetailRequest`.
- Nuevo endpoint `POST /apus/batch/details` para resolver detalles de APUs en lote, con recursos y CPC.
- `DesagregacionTab` deja de pedir APUs de forma secuencial y consume el lote.
- La cache local `apuDetails` queda respaldada por `ref` estable para evitar que el cambio de cache regenere el callback y vuelva a cargar presupuesto.
- Al cambiar presupuesto/empresa, se limpia la cache para evitar datos residuales.

## Impacto en licencias
- Sin nuevo modulo comercial.
- Reutiliza endpoints autenticados de APUs bajo empresa activa.
- Sin bypass de permisos ni tenant.

## Impacto UI
- Sin cambio visual.
- La mejora esperada es funcional/performance: menos espera y CPC visible cuando el backend lo resuelve.

## No interferencia BIM
- Confirmada.
- No toca rutas, componentes, dependencias ni flags BIM.

## Validacion ejecutada
- `python -m py_compile backend/app/api/endpoints/apus.py backend/app/schemas/apu.py backend/app/repositories/apu.py backend/app/services/apu.py` OK.
- `python -m pytest app/tests/test_nested_apu_integrity.py -q` desde `backend/` OK: 4 passed, 1 warning existente de Pydantic en `CodCPCResponse`.
- `npm run build` en `frontend/` OK; se mantiene aviso conocido de chunk grande en `CronogramaGantt`.

## Cierre
- `Desagregacion` ya no carga los APUs implicados uno a uno.
- El cambio de `apuDetails` ya no reinyecta una recarga del presupuesto por dependencia de callback.
- La cache se limpia al cambiar presupuesto o empresa para evitar datos residuales sin CPC.
- Runtime documental devuelto a `SIN_TASK_ACTIVA_REAL`.
