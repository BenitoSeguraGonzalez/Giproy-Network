# TASK-1443 - Separar EDT y rubros APU en Desagregacion

## Estado
Completada

## Modo
GIPROY CLASICO

## Objetivo
Evitar que `Desagregacion` mezcle nodos EDT o lineas estructurales del presupuesto con los rubros APU reales del Formulario 1.

## Alcance autorizado
- Frontend clasico: `DesagregacionTab`.
- Filtrar el consolidado para que calcule y renderice solo lineas con `apu_id`.
- Mantener el flujo de CPC, VAE y desglose tecnico sobre APUs reales.
- Documentar TASK, avance e impacto.

## Fuera de alcance
- No cambiar visual global del modulo.
- No tocar calculo backend de reportes.
- No modificar EDT, Presupuesto ni APUs.
- No activar ni tocar BIM.

## Avance
- Inicio: 0%.
- Diagnostico confirmado: 0% -> 20%.
- Implementacion frontend: 20% -> 60%.
- Validacion frontend: 60% -> 90%.
- Cierre documental y runtime sincronizado: 90% -> 100%.
- Avance final: 100%.

## Implementacion aplicada
- `budgetLines` filtra solo lineas con `apu_id`.
- `flatHierarchy` deja de mezclar el arbol EDT con rubros.
- El Formulario 1 renderiza rubros APU en orden de presupuesto y con `level = 0`.
- Se retira la carga de EDT en `DesagregacionTab`, ya que no alimenta el formulario de rubros y generaba ruido visual/operativo.

## Impacto en licencias
- Sin nuevo modulo ni nueva capacidad.
- No cambia permisos ni endpoints.
- Validaciones backend existentes permanecen intactas.

## Impacto UI
- Cambio visual acotado: se retiran de la tabla de Desagregacion las filas EDT/placeholder que no son rubros APU.
- El usuario debe ver solo rubros desagregables en el consolidado.

## No interferencia BIM
- Confirmada.
- No toca capa BIM ni dependencias BIM.

## Validacion ejecutada
- `npm run build` en `frontend/` OK.
- Aviso residual conocido: chunk grande en `CronogramaGantt`.

## Cierre
- El Formulario 1 de `Desagregacion` ya no intercala nodos EDT ni placeholders sin APU.
- Los totales, integridad CPC, VAE de rubro y VAE ponderado operan sobre lineas con `apu_id`.
- No hay cambios backend, licencias ni BIM.
