# TASK-1448 - Filtro de recursos sin CPC en Precios Unitarios

## Estado
En progreso

## Modo
GIPROY CLASICO

## Objetivo
Incorporar en `Precios Unitarios > Recursos` un filtro para mostrar recursos que no tienen Codigo CPC asociado, respetando categoria/subcategoria activa y sin cambiar contratos backend.

## Alcance autorizado
- Frontend clasico: `frontend/src/pages/Recursos.jsx`.
- Filtro en el listado visible de recursos.
- Contador de recursos sin CPC en el alcance activo.
- Busqueda global coherente con el filtro cuando este activo.
- Sin cambios de API, base de datos, licencias, BIM ni arquitectura.

## Avance
- Inicio: 0%.
- Diagnostico y localizacion del modulo real: 0% -> 20%.
- Implementacion frontend conservadora: 20% -> 75%.
- Validacion tecnica con build: 75% -> 90%.
- Validacion funcional simulada del criterio CPC: 90% -> 95%.
- Avance actual: 95%.

## Impacto en licencias
- Sin modulo nuevo.
- Sin permisos nuevos.
- Sin bypass de empresa: se reutiliza la carga de recursos ya filtrada por base/empresa del modulo.

## Impacto UI
- Si, acotado a la toolbar de `Recursos`.
- Se reutiliza el estilo existente de botones compactos con icono y contador.
- No se crean componentes duplicados.

## No interferencia BIM
- Confirmada por alcance: solo capa clasica de Precios Unitarios.
- Sin UX BIM, sin rutas BIM y sin dependencia hacia BIM.

## Implementacion aplicada
- `Recursos.jsx` detecta si un recurso tiene CPC por `cod_cpc_id`, objeto `cpc` o campos planos compatibles.
- Se agrega modo `Sin CPC` con contador sobre categoria/subcategoria activa.
- `visibleRecursos` filtra solo recursos sin CPC cuando el modo esta activo.
- La busqueda global respeta el filtro activo para no proponer recursos con CPC.
- Al activar/desactivar el filtro se limpia la seleccion masiva para evitar acciones sobre recursos ocultos.

## Validacion ejecutada
- `npm run build` en `frontend/` OK; chunk generado `Recursos-BgZm0ukL.js`.
- Comprobacion estatica focalizada confirma que `Sin CPC` existe en toolbar, contador, filtro de `visibleRecursos`, filtro de busqueda global y estado vacio.
- Comprobacion funcional simulada con recursos con `cod_cpc_id`, objeto `cpc`, campo plano `cod_cpc_codigo` y recursos sin CPC confirma que solo quedan los IDs sin CPC.

## Pendiente para cierre
- QA visual en UI servida para confirmar posicion exacta del boton, contador y listado con datos reales del usuario.
