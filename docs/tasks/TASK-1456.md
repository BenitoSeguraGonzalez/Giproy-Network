# TASK-1456 - Control asignacion masiva CPC en recursos

## Estado
Completada

## Avance
100%

## Fecha
2026-04-30

## Contexto
En `Precios Unitarios > Recursos` ya existia seleccion multiple de recursos, pero no habia una accion controlada para asignar el mismo Codigo CPC a varios recursos seleccionados. La mejora debe respetar el contrato existente de CPC, no borrar codigos por payload incompleto y mantener coherencia con `Desagregacion`, que consume los recursos a traves de APUs.

## Alcance de control
- Validar que la accion masiva actua solo sobre recursos de la empresa activa.
- Validar que el CPC elegido existe antes de modificar recursos.
- Validar que `Desagregacion` ve el CPC actualizado al leer el APU.
- Validar que la UI reutiliza el patron visual de acciones existente en las filas de recursos.
- Mantener GiProy Clasico, sin activar UX BIM.

## Criterios de cierre
- TASK de implementacion asociada creada y cerrada.
- Prueba tecnica backend.
- Build frontend.
- Prueba real reversible sobre la empresa `Santiago Bermeo`.
- Documentacion sincronizada.

## Validacion
- `python -m py_compile app/schemas/recurso.py app/services/recurso.py app/api/endpoints/recursos.py app/tests/test_nested_apu_integrity.py` OK.
- `python -m pytest app/tests/test_nested_apu_integrity.py app/tests/test_project_base_reconciliation.py -q` OK: `10 passed`.
- `npm run build` OK.
- `node frontend/scripts/smoke-classic-no-bim-contamination.mjs` OK.
- Validacion real reversible `Santiago Bermeo`: recursos reales `371` y `375`, usados por APU `278` del presupuesto `13`, recibieron temporalmente el CPC `011200011`; `apu_repo.get_by_id(...)` devolvio ambos recursos con `cod_cpc_id=1` y `cod_cpc_codigo=011200011`; despues se restauro exactamente el estado original.

## Impacto en licencias
Sin licencia nueva. Se reutiliza el modulo existente de `Precios Unitarios > Recursos`; la validacion de tenant/empresa se mantiene en backend.

## Impacto UI
Si. Se agrega una accion iconografica contextual en el bloque de acciones de la fila cuando hay varios recursos seleccionados.

## No interferencia BIM
Confirmada. No se toca BIM ni sus rutas.

## Pendiente para 100%
Ninguno.
