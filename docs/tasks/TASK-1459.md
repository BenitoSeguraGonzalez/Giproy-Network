# TASK-1459 - Implementacion relaciones Gantt FF/SF y saneamiento Santiago Bermeo

## Estado
Completada

## Avance
100%

## Fecha
2026-04-30

## Contexto
La logica anterior aplicaba el inicio del proyecto como suelo silencioso dentro del calculo de dependencias. Eso evitaba fechas anteriores al proyecto, pero podia romper la semantica de `FF` y `SF/CF`: la relacion quedaba guardada, aunque el fin de la tarea ya no cumpliera la igualdad requerida.

## Implementacion backend
- `cronograma_trabajo_service._resolve_rows_dates(...)` deja de incluir el inicio del proyecto como candidato silencioso en dependencias.
- Cada dependencia calcula su fecha gobernante real.
- Si la fecha de inicio resultante queda antes del inicio operativo del proyecto, se lanza `ValueError` y la secuenciacion no se guarda.
- Se mantiene la validacion final existente contra filas antes de inicio de proyecto.

## Implementacion frontend
- `resolveDependencyTargetStart(...)` deja de aplicar un suelo silencioso al inicio del proyecto.
- El preview conserva la fecha calculada real, para detectar conflicto en vez de falsear la relacion.
- `resolveDependencyConstraintDiagnostics(...)` detecta cuando una relacion implica inicio anterior al proyecto, incluyendo `FF` y `SF/CF` por retrocalculo desde el fin.
- `persistDraftPatches(...)` bloquea el guardado antes de llamar al backend si el payload contiene una fecha anterior al inicio del proyecto.

## Saneamiento real
- Empresa: `Santiago Bermeo`.
- Presupuesto: `13`.
- Se genero backup previo: `tmp_santiago_gantt_dependency_dates_backup_20260430.json`.
- Se recalculo el Gantt con la logica corregida y se actualizaron `29` filas dependientes cuyos `start_date/end_date` persistidos estaban obsoletos respecto al calculo real.
- Verificacion posterior: `remaining_stale_dependency_rows = []`.

## Validacion real
- `FS`, `SS`, `FF` y `SF` probados de forma reversible sobre lineas reales `71 -> 73`.
- Caso invalido probado de forma reversible: `49 -> 73` con `FF`, rechazado por inicio anterior al proyecto.
- `diagnose_santiago_gantt_ff_item10.py` confirma que `1.1.7` con `FF` contra `1.1.6` queda con fin alineado al de la predecesora.
- DOM headless confirma escritura de shortcode `FF` en el Gantt real de Santiago Bermeo sin errores.

## Validacion tecnica
- `py_compile` backend OK.
- Pytest focalizado OK: `21 passed`.
- `npm run smoke:gantt-classic` OK.
- `node scripts/validate-gantt-ff-predecessor-dom.mjs` OK.
- `npm run build` OK.
- Smoke no BIM OK.

## Impacto en licencias
Ninguno.

## Impacto UI
Si, acotado a preview/conflictos del Gantt clasico.

## No interferencia BIM
Confirmada.

## Pendiente para 100%
Ninguno.
