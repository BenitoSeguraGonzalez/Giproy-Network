# TASK-1463 - Implementacion CPC de APUs en Desagregacion

## Estado
Completada

## Avance
100%

## Modo
GIPROY CLASICO

## Objetivo
Implementar la persistencia backend y UI clasica para asignar Codigo CPC a APUs en Desagregacion, con soporte de APUs anidados.

## Slice autorizado
- Modelo de asociacion proyecto raiz / APU / CPC.
- Endpoints backend para listar y guardar CPC de APU.
- Cliente frontend y selector CPC APU en Desagregacion.
- Reutilizacion del modal CPC existente.
- Reglas de VAE y conteo para APUs anidados.

## Fuera de alcance
- Cambiar CPC universal de recursos.
- Cambiar sincronizacion de base maestra salvo preservacion de no interferencia.
- Activar BIM.

## Porcentaje inicial
10% por apertura documental y alcance aprobado.

## Pendiente para 100%
- Ninguno.

## Implementacion
- Backend agrega `ProyectoApuCpc` y migracion `ad9e0f1a2b3c`.
- API APUs agrega `GET /apus/project-cpc` y `PUT /apus/{id}/project-cpc`.
- Desagregacion agrega columna `CPC` entre `Unidad` y `Cantidad` para rubros APU.
- El modal CPC existente se reutiliza para asignar CPC APU.
- APUs anidados en desglose muestran CPC en blanco, no abren asignacion y no cuentan como CPC faltante.
- `APU.vae_total` permite que un APU anidado aporte su VAE total calculado como APU normal.
- Subajuste estetico posterior: se retira la columna independiente `CPC` del grid principal y el selector CPC APU pasa a la segunda linea bajo la descripcion del rubro, como ultimo elemento, manteniendo exactamente el mismo guardado.
- Subajuste UX posterior: Desagregacion incorpora buscador navegable, no filtro, en la barra superior del grid. Busca por rubro, EDT, unidad y CPC APU, mantiene todas las filas visibles, resalta coincidencias y permite saltar con Enter o `Sgte`.

## Validacion ejecutada
- `alembic upgrade ad9e0f1a2b3c` aplicado en base local.
- `python -m py_compile app/models/apu.py app/models/proyecto_apu_cpc.py app/schemas/apu.py app/api/endpoints/apus.py app/repositories/apu.py` OK.
- `python -m pytest app/tests/test_nested_apu_integrity.py app/tests/test_project_base_reconciliation.py -q` OK: 12 passed.
- `npm run build` OK.
- `node frontend/scripts/smoke-classic-no-bim-contamination.mjs` OK.
- Validacion real reversible `Santiago Bermeo`: CPC APU temporal persistido, recurso sin contaminacion, APU anidado con VAE calculado y sin `resource_id`.
- Subajuste estetico: `npm run build` OK y smoke no BIM OK tras mover el chip CPC APU bajo la descripcion.
- Subajuste buscador: `npm run build` OK y smoke no BIM OK.

## Cierre
Avance 10% -> 100%. Sin pendientes.
