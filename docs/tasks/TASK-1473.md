# TASK-1473 - Implementacion columnas holguras Gantt

## Estado
Completada.

## Avance
100%.

## Objetivo
Implementar en el grid izquierdo del Gantt las columnas `Holg. total` y `Holg. libre`, alimentadas por la metadata CPM existente.

## Implementacion
- `CronogramaGantt.jsx` amplia las definiciones de columnas, anchos y metadatos del grid izquierdo.
- Se agregan las columnas `total_float` y `free_float` al gestor de columnas existente.
- Se crea una celda visual reutilizable para holguras con lectura de estado:
  - cero o casi cero: lectura critica/naranja.
  - negativa: alerta/rojo.
  - positiva: lectura normal/azul.
- Las celdas consumen `planningNode` preferente o `cpmDiagnostics` ya resueltos desde `metadata.cpm`.
- Las configuraciones previas de columnas en `localStorage` siguen siendo compatibles porque `GridColumnManager` normaliza y anade ids nuevos.

## Cierre
- Avance anterior: 0%.
- Avance nuevo: 100%.
- Frente Gantt holguras visibles: 100%.
- Pendiente para 100%: ninguno.
- Impacto licencias: ninguno.
- Impacto UI: columnas informativas nuevas, sin controles funcionales nuevos.
- Impacto backend: ninguno; se reutiliza contrato CPM existente.
- No interferencia BIM: confirmada; sin rutas, imports ni flags BIM.

## Validacion
- Validacion real `Santiago Bermeo`, proyecto `7`, presupuesto `13`: `cronograma_trabajo_service.get_schedule(...)` devuelve `187` filas y las `187` incluyen `metadata.cpm.total_float_days` y `metadata.cpm.free_float_days`.
- Muestras contrastadas: linea `49` `HT=0.0 / HL=0.0`, linea `102` `HT=36.1241 / HL=0.0`, linea `118` `HT=9.166 / HL=0.0`.
- `npm run smoke:gantt-classic` OK.
- `npm run build` OK en `frontend`.
