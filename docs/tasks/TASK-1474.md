# TASK-1474 - Control reparacion integral Formula Polinomica

## Estado
Completada.

## Avance
100%.

## Objetivo
Controlar la reparacion integral del modulo `Formula Polinomica` para recuperar calculos operativos al 100%, evitando que la UI muestre terminos aparentes mientras el motor queda sin monomios, coeficientes o formula general.

## Alcance
- Backend comun de Formula Polinomica.
- Contrato API `resources`, `assignments`, `regenerate`, `indices` y `get formula`.
- Frontend clasico `FormulaPolinomicaTab.jsx`.
- Datos reales de validacion en empresa/proyecto usado por el usuario.

## Politica de control
- No cerrar por compilacion.
- Auditar entrada de recursos, asignaciones, defaults, agrupacion por terminos, coeficientes, redondeo, indices, cuadrilla tipo, modos `SIN_DESGLOSE` y `CON_DESGLOSE`.
- Confirmar que los terminos visibles en UI corresponden a datos efectivos del motor.
- Confirmar que una asignacion o cambio de modo deja formula, recursos y panel derecho en un estado consistente.
- No tocar licencias ni BIM.

## Validaciones obligatorias
- Tests backend focalizados de Formula Polinomica.
- Smoke real reversible o no destructivo con datos reales.
- Build frontend.
- Evidencia concreta de monomios, suma de coeficientes, costo directo base, total de indices y formula general.

## Cierre esperado
- Avance anterior: 0%.
- Avance nuevo: 100%.
- Frente Formula Polinomica calculos: 100%.
- Pendiente para 100%: ninguno.
- Impacto licencias: ninguno previsto.
- Impacto UI: flujo de asignacion consume la formula regenerada para no rehidratar estado incompleto.
- No interferencia BIM: confirmada; no se tocan rutas, flags ni UX BIM.

## Validacion
- Auditoria real `Santiago Bermeo`, proyecto `7`, presupuesto `13`: se encontro formula con `214` recursos, `0` pendientes, `198` asignaciones, pero `0` monomios y `0` cuadrilla, por lo que `formula_general` quedaba vacia pese a estar completa por recursos.
- Regeneracion real del motor reconstruye `5` monomios y `9` items de cuadrilla con `PR = P0 (0.232B1/B0 + 0.004C1/C0 + 0.028E1/E0 + 0.005R1/R0 + 0.731X1/X0)`.
- `POST /api/v1/polinomica/13/assignments` probado de forma reversible sobre recurso `357` devuelve `200`, `0` pendientes, `is_complete=true`, `5` monomios, `9` items de cuadrilla y formula general no vacia.
