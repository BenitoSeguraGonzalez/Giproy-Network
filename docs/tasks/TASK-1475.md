# TASK-1475 - Implementacion reparacion integral Formula Polinomica

## Estado
Completada.

## Avance
100%.

## Objetivo
Implementar la reparacion conservadora del flujo de calculo de `Formula Polinomica` para que el modulo vuelva a generar monomios, coeficientes, cuadrilla, indices y formula general de forma consistente tras editar terminos o cambiar modo.

## Alcance
- Servicio backend `formula_polinomica_service`.
- Endpoint de asignaciones y regeneracion si el contrato lo requiere.
- Frontend `FormulaPolinomicaTab.jsx` solo en el flujo de carga/persistencia si participa en la regresion.
- Pruebas automatizadas y validacion real.

## Reglas
- No reintroducir columnas duplicadas `Termino` / `Asignacion`.
- Mantener la columna unica `Termino` como punto de lectura y edicion.
- No convertir un problema de calculo en un mensaje visual.
- Preservar indices manuales ya elegidos cuando se regenera.
- Preservar asignaciones manuales.
- Mantener modos `Estandar` y `Con desglose`.
- No tocar BIM ni licencias.

## Validacion requerida para 100%
- Backend: tests focalizados verdes.
- Datos reales: recursos detectados, pendientes, monomios, coeficientes y formula general contrastados.
- Frontend: build verde y, si procede, DOM/smoke de Formula Polinomica.
- Documentacion: CHANGELOG, TODO, HANDOFF y WORK_MODE_STATE sincronizados.

## Cierre
- Avance anterior: 0%.
- Avance nuevo: 100%.
- Frente Formula Polinomica calculos: 100%.
- Pendiente para 100%: ninguno.
- Impacto licencias: ninguno.
- Impacto UI: la columna unica `Termino` se mantiene; al guardar, la UI recibe formula recalculada y recarga recursos sin carrera de `loadData`.
- Impacto backend: `POST /polinomica/{presupuesto_id}/assignments` regenera y devuelve `FormulaPolinomicaResponse`.
- No interferencia BIM: confirmada.

## Implementacion
- `backend/app/api/endpoints/polinomica.py`: el endpoint de asignaciones conserva el tipo actual (`SIN_DESGLOSE` / `CON_DESGLOSE`), guarda las asignaciones y regenera la formula antes de responder.
- `frontend/src/components/projects/FormulaPolinomicaTab.jsx`: `persistAssignments(...)` consume la formula devuelta, actualiza `formulaData` y recarga solo recursos, evitando rehidratar una formula vieja o sin monomios.

## Validacion
- `python -m py_compile app/api/endpoints/polinomica.py app/services/formula_polinomica.py app/schemas/polinomica.py` OK.
- `python -m pytest app/tests/test_numeric_integrity_secondary_modules.py -q -k "formula_polinomica"` OK: `5 passed`.
- Smoke real `backend/scripts/smoke_test_formula_polinomica_real.py`: `214` recursos, `0` pendientes, formula general presente.
- API real reversible con `TestClient` sobre `Santiago Bermeo`, presupuesto `13`, recurso `357`: respuesta `200`, `5` monomios, `9` cuadrilla, formula general presente.
- `npm run build` OK en `frontend`.
- `node scripts/smoke-formula-polinomica-responsive.mjs` OK.
- `node scripts/validate-formula-responsive-dom.mjs` OK.
- `node scripts/validate-formula-scrollbar-geometry.mjs` OK.
