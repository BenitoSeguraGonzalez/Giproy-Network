 a # TASK-1485 - Implementacion ruta ortogonal de dependencias con hitos

Estado: Completada
Avance anterior: 0%
Avance nuevo: 100%
Fecha: 2026-05-02
Modo: GIPROY CLASICO

## Cambios

- `frontend/src/components/projects/cronogramasGanttDependencies.js`
  - Se elimina el atajo que convertia enlaces de hitos sin lag en una linea directa punto a punto.
  - Las relaciones con hitos pasan por el router ortogonal general del Gantt, conservando el caso de alineacion vertical exacta.

- `frontend/scripts/smoke-cronogramas-gantt-dependencies.mjs`
  - Se actualiza el contrato: tarea -> hito sin lag debe usar ruteo ortogonal, no diagonal directa.

- `frontend/scripts/smoke-cronogramas-gantt-manual-milestone-dependencies.mjs`
  - Se anade proteccion estatica para impedir que vuelva el atajo `hito sin lag -> linea directa`.

- `frontend/scripts/validate-gantt-manual-milestone-route-dom.mjs`
  - Se anade validacion DOM con datos reales de `Santiago Bermeo`, presupuesto `13`, reproduciendo un hito manual desplazado respecto a su sucesora y comprobando que el SVG renderiza una ruta ortogonal.

## Resultado validado

La reproduccion DOM sobre `Santiago Bermeo` genera la dependencia `manual-milestone:hm-mok8sj9c-j04f-49` con `route_command_count = 5` y path:

`M 149.5 166 L 179.5 166 L 191.5 166 L 191.5 106 L 12 106`

Esto confirma que ya no se emite una diagonal directa de dos puntos.

## Validaciones

- `node scripts/smoke-cronogramas-gantt-manual-milestone-dependencies.mjs` OK.
- Validacion focal del router hito -> tarea: `commandCount > 2` OK.
- `npm run smoke:gantt-classic` OK.
- `node scripts/validate-gantt-manual-milestone-route-dom.mjs` OK.
- `node scripts/validate-gantt-manual-milestone-default-time-dom.mjs` OK.
- `npm run build` OK.

## Impacto

Solo frontend Gantt clasico. No cambia calculos, relaciones persistidas, cronograma valorado, presupuesto, licencias ni BIM.

