# TASK-1489 - Implementacion bloqueo de guardado parcial de hito manual

## Estado
Completada 100%.

## Slice
Hotfix conservador en `CronogramaGantt.jsx` para impedir persistencia parcial al mover hitos manuales.

## Cambios
- El flujo de soltar hito calcula la cascada de dependientes antes de guardar `manual_milestones`.
- Si existen dependientes, primero intenta persistir los parches de la cascada.
- Si la cascada devuelve `null` o falla validacion, no se guarda el hito.
- Se agrega prueba DOM especifica `validate-gantt-manual-milestone-drag-release-dom.mjs`.

## Validacion ejecutada
- `node scripts/validate-gantt-manual-milestone-drag-release-dom.mjs` OK.
- `node scripts/validate-gantt-manual-milestone-route-dom.mjs` OK.
- `npm run smoke:gantt-classic` OK.
- `npm run build` OK.

## Resultado
El caso real de `Santiago Bermeo` ya no puede dejar el hito manual persistido si la cascada posterior queda bloqueada por la regla de inicio del proyecto. Esto evita la rehidratacion incoherente que generaba lineas visuales sin significado.

## Avance
- Anterior: 0%.
- Nuevo: 100%.

## Pendiente operativo detectado
El Gantt real de `Santiago Bermeo` mantiene una violacion previa en la cascada: una relacion intenta ubicar `1.1.7` antes del inicio del proyecto. No se saneo en esta TASK porque el alcance fue impedir el guardado parcial del hito.

## Impacto
- Gantt clasico: corregido.
- Cronograma Valorado/Caja: sin mutacion directa.
- Licencias: sin impacto.
- UI: sin cambio visual.
- BIM: sin impacto.

