# TASK-1493 - Implementación: estabilizar dependencias durante drag de hito manual

## Estado
Completada al 100%.

## Objetivo
Evitar que el preview de arrastre de un hito manual deforme el Gantt estable o genere codos/líneas fantasma antes de soltar.

## Cambios ejecutados
- `frontend/src/components/projects/CronogramaGantt.jsx`
  - El drag de hito manual deja de generar `previewPatches` de cascada visual temporal.
  - La capa de dependencias deja de recalcular sus geometrías con `activeMovePreviewDrafts`.
  - Las rutas se calculan con el estado persistido/draft real, manteniendo estable la red mientras el usuario arrastra.
- `frontend/scripts/validate-gantt-manual-milestone-drag-preview-dom.mjs`
  - Nuevo validador DOM real con datos de `Santiago Bermeo`.
  - Arrastra el hito manual más de 300 px sin soltar y confirma que la ruta de dependencia no cambia durante el preview.
- `frontend/scripts/validate-gantt-manual-milestone-route-dom.mjs`
  - Ajustado para validar el hito real/fallback alineado con la línea destino, evitando un caso artificial con fecha fija que fabricaba codos no representativos.

## Evidencia real
- Empresa: `Santiago Bermeo`.
- Proyecto: `7`.
- Presupuesto: `13`.
- Hito manual validado: `hm-mok8sj9c-j04f`.
- Dependencia validada: `manual-milestone:hm-mok8sj9c-j04f-49`.
- Durante drag DOM:
  - `bar_delta_px`: `310.86`.
  - `dependency_route_kept_stable`: `true`.
  - `route_d`: `M 414.5 166 L 414.5 106`.

## Validación ejecutada
- `node scripts/validate-gantt-manual-milestone-drag-preview-dom.mjs` OK.
- `node scripts/validate-gantt-manual-milestone-route-dom.mjs` OK.
- `node scripts/validate-gantt-santiago-route-all.mjs` OK: `30/30` rutas, `0` incidencias.
- `npm run smoke:gantt-classic` OK.
- `python tmp/audit_santiago_gantt_full.py` OK: `187` filas, `0` incidencias.
- `npm run build` OK.

## Nota de validación
`node scripts/validate-gantt-santiago-full-dom.mjs` agotó tiempo sin emitir resultado en esta sesión, por lo que no se usa como evidencia de cierre. La validación específica del bug sí reprodujo el arrastre real y confirmó que la ruta no cambia durante el preview.

## Avance
- Avance anterior: 0%.
- Avance nuevo: 100%.
- Frente Gantt clásico: 100% para este hotfix visual.
- Global relativo: ajuste puntual cerrado, sin cambio de plan rector.

## Impacto
- Licencias: sin impacto.
- UI: Gantt clásico, preview de arrastre de hito manual.
- Backend: sin cambios.
- Datos: sin cambios productivos.
- Cronograma Valorado/Presupuesto: sin impacto directo.
- BIM: sin impacto.
