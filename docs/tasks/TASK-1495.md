# TASK-1495 - Implementación: recuperar movimiento de hito manual y vertical limpia

## Estado
Completada al 100%.

## Objetivo
Recuperar el movimiento real del hito manual inicial y mantener la representación vertical limpia de su dependencia cuando el anclaje está prácticamente alineado.

## Cambios ejecutados
- `frontend/src/components/projects/CronogramaGantt.jsx`
  - Las barras e hitos del timeline se elevan sobre la capa SVG de dependencias con `z-[14]`, evitando que el hit-area transparente de una dependencia capture el `pointerdown`.
- `frontend/src/components/projects/cronogramasGanttDependencies.js`
  - La tolerancia de alineación de anclajes pasa de `2px` a `12px`.
  - En enlaces con hitos dentro de esa tolerancia se colapsa la ruta a una X única, generando línea vertical limpia en lugar de diagonal corta o micro-codo.
- `frontend/scripts/validate-gantt-manual-milestone-drag-preview-dom.mjs`
  - Valida el movimiento real del hito de `Santiago Bermeo`, incluyendo desplazamiento del elemento y estabilidad de la ruta durante preview.
- `frontend/scripts/validate-gantt-manual-milestone-route-dom.mjs`
  - Valida que la dependencia hito -> tarea se renderiza como línea vertical limpia.

## Evidencia real
- Empresa: `Santiago Bermeo`.
- Proyecto: `7`.
- Presupuesto: `13`.
- Hito manual: `hm-mok8sj9c-j04f`.
- Dependencia validada: `manual-milestone:hm-mok8sj9c-j04f-49`.
- Movimiento DOM:
  - `bar_delta_px`: `-310.86`.
  - `pointer_drag_px`: `-220`.
  - `dependency_route_kept_stable`: `true`.
  - `route_d`: `M 579 166 L 579 106`.
- Ruta DOM vertical:
  - `route_command_count`: `2`.
  - `route_d`: `M 579 166 L 579 106`.

## Validación ejecutada
- `node scripts/validate-gantt-manual-milestone-route-dom.mjs` OK.
- `node scripts/validate-gantt-manual-milestone-drag-preview-dom.mjs` OK.
- `node scripts/validate-gantt-santiago-route-all.mjs` OK: `30/30` rutas calculadas, `0` incidencias.
- `python tmp/audit_santiago_gantt_full.py` OK: `187` filas, `0` filas antes del inicio del proyecto, `0` incidencias de dependencias, `0` incidencias de hitos manuales.
- `npm run smoke:gantt-classic` OK.
- `npm run build` OK.

## Avance
- Avance anterior: 0%.
- Avance nuevo: 100%.
- Frente Gantt clásico: 100% para este hotfix funcional/visual.
- Global relativo: ajuste puntual cerrado, sin cambio de plan rector.

## Impacto
- Licencias: sin impacto.
- UI: Gantt clásico, hito manual y rutas de dependencias.
- Backend: sin cambios.
- Datos productivos: sin cambios.
- Cronograma Valorado/Presupuesto: sin impacto directo.
- BIM: sin impacto.
