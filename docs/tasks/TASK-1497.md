# TASK-1497 - Implementación: preview coherente de hito manual y cálculo de inicio

## Estado
Completada al 100%.

## Objetivo
Hacer que el arrastre de un hito manual use el mismo contrato visual y funcional antes y después de soltar: hito, sucesores y dependencias deben moverse y calcularse juntos.

## Cambios ejecutados
- `frontend/src/components/projects/CronogramaGantt.jsx`
  - El preview de drag vuelve a construir `previewPatches` también para hitos manuales.
  - La capa de dependencias usa un `dependencyDrafts` combinado con `drafts + activeMovePreviewDrafts` durante el arrastre.
  - En hitos manuales, el anclaje de dependencias prioriza la geometría visual real del hito en lugar de una geometría exacta derivada que podía quedar desfasada.
- `frontend/scripts/validate-gantt-manual-milestone-drag-preview-dom.mjs`
  - La validación DOM pasa a exigir preview coherente: se mueven hito, sucesora y ruta.

## Evidencia real
- Empresa: `Santiago Bermeo`.
- Proyecto: `7`.
- Presupuesto: `13`.
- Hito manual: `hm-mok8sj9c-j04f`.
- Sucesora: línea `49`.
- Preview DOM:
  - `bar_delta_px`: `-292.56`.
  - `target_delta_px`: `-320`.
  - `dependency_route_updated`: `true`.
- Commit DOM:
  - `target_start_before`: `2026-05-04T08:00:00`.
  - `target_start_saved`: `2026-06-01T08:00:00`.
  - `milestone_start_saved`: `2026-06-01T08:00:00`.
  - `route_command_count`: `2`.

## Validación ejecutada
- `node scripts/validate-gantt-manual-milestone-drag-preview-dom.mjs` OK.
- `node scripts/validate-gantt-manual-milestone-drag-release-dom.mjs` OK.
- `node scripts/validate-gantt-santiago-route-all.mjs` OK: `30/30` rutas calculadas, `0` incidencias.
- `python tmp/audit_santiago_gantt_full.py` OK: `187` filas, `0` filas antes del inicio del proyecto, `0` incidencias de dependencias, `0` incidencias de hitos manuales.
- `npm run smoke:gantt-classic` OK.
- `npm run build` OK.

## Avance
- Avance anterior: 0%.
- Avance nuevo: 100%.
- Frente Gantt clásico: 100% para este hotfix funcional/visual.
- Global relativo: ajuste puntual cerrado, sin cambio de plan rector.

## Impacto
- Licencias: sin impacto.
- UI: Gantt clásico, preview y commit de hitos manuales.
- Backend: sin cambios.
- Datos productivos: sin cambios.
- Cronograma Valorado/Presupuesto: sin impacto directo.
- BIM: sin impacto.
