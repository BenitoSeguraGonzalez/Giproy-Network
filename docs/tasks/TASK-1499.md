# TASK-1499 - Implementación: restaurar enlace hito -> ruta y validar Gantt real

## Estado
Completada al 100%.

## Objetivo
Sanear el Gantt real de `Santiago Bermeo` para que el hito manual inicial vuelva a estar enlazado con la primera tarea de la ruta y que las rutas de predecesoras queden coherentes en base, cálculo y presentación DOM.

## Cambios ejecutados
- Se creó backup previo:
  - `tmp/santiago_bermeo_gantt_manual_link_backup_20260505_105254.json`.
- Se restauró el hito manual:
  - `id`: `hm-mok8sj9c-j04f`.
  - `start_date`: `2026-03-24T08:00:00`.
  - `after_line_id`: `49`.
  - `display_after_line_id`: `edt-31`.
  - `successor_dependencies`: `manual-milestone:hm-mok8sj9c-j04f -> 49`, tipo `FS`, lag `0`.
- Se reconstruyó `schedule_data` con `cronograma_trabajo_service.update_schedule(...)`, usando el motor oficial.

## Resultado confirmado
- Línea `49`:
  - Código: `1.1.1`.
  - Descripción: `Replanteo y nivelación`.
  - Inicio: `2026-03-24T08:00:00`.
  - Fin: `2026-03-24T10:19:00`.
- Hito `hm-mok8sj9c-j04f`:
  - Inicio: `2026-03-24T08:00:00`.
  - Sucesora: línea `49`.
  - Relación: `FS`.
- Dependencias totales auditadas por router: `30`.
- Rutas DOM renderizadas en viewport virtual: `15`, todas válidas.

## Validación ejecutada
- `python tmp/audit_santiago_gantt_full.py` OK:
  - `187` filas.
  - `raw_mismatch_count = 0`.
  - `dependency_issue_count = 0`.
  - `manual_issue_count = 0`.
  - `cpm_missing_count = 0`.
  - `subbar_issue_count = 0`.
- `python tmp/validate_santiago_gantt_integrity.py` OK:
  - `1.1.6 -> 1.1.7` conserva relación `FF`.
  - `before_project_start_count = 0`.
  - `dependency_issue_count = 0`.
- `python tmp/audit_santiago_gantt_links.py` OK:
  - Hito manual enlazado a línea `49`.
  - Dependencias tarea-tarea legibles y coherentes.
- `node scripts/validate-gantt-santiago-route-all.mjs` OK:
  - `dependency_count = 30`.
  - `routed_count = 30`.
  - `issue_count = 0`.
- `node scripts/validate-gantt-manual-milestone-route-dom.mjs` OK:
  - `dependency_key = manual-milestone:hm-mok8sj9c-j04f-49`.
  - `route_d = "M 12 166 L 12 106"`.
  - `route_command_count = 2`.
- `node scripts/validate-gantt-santiago-full-dom.mjs` OK:
  - `expected_dependency_count = 30`.
  - `rendered_dependency_count = 15`.
  - `diagonal_route_count = 0`.
  - `direct_manual_route_count = 0`.
  - `empty_route_count = 0`.
  - Nota: el `missing_visible_count = 15` es esperable por virtualización del viewport.
- `npm run smoke:gantt-classic` OK.

## Avance
- Avance anterior: 0%.
- Avance nuevo: 100%.
- Frente Gantt clásico: 100% para saneamiento real de Santiago Bermeo.
- Global relativo: ajuste puntual cerrado, sin cambio de plan rector.

## Impacto
- Licencias: sin impacto.
- UI: sin cambio de código visual.
- Backend: sin cambio de lógica; se usó el motor oficial existente.
- Datos productivos: sí, solo `schedule_data` del Gantt de `Santiago Bermeo`.
- Cronograma Valorado/Presupuesto: sin escritura directa.
- BIM: sin impacto.
