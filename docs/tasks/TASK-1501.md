# TASK-1501 - Implementacion hotfix Gantt preview fluido y rollback de patches

Estado: Completada
Avance anterior: 0%
Avance nuevo: 100%
Fecha: 2026-05-05
Modo: GIPROY CLASICO

## Cambios

- `frontend/src/components/projects/CronogramaGantt.jsx`
  - El preview de arrastre de hito manual usa el delta real del puntero para pintar el rombo activo, manteniendo el calculo de cascada para la previsualizacion de sucesores.
  - Si un guardado optimista de drag devuelve `null` por validacion funcional, el estado local vuelve al snapshot anterior y no queda un Gantt sucio hasta refrescar.

## Validacion real

- `node scripts/validate-gantt-manual-milestone-drag-preview-dom.mjs`
  - Empresa: `Santiago Bermeo`.
  - Presupuesto: `13`.
  - Hito: `hm-mok8sj9c-j04f`.
  - Resultado: `bar_delta_px=220` y `pointer_drag_px=220`; el hito sigue exactamente al puntero.
  - La ruta se actualiza durante el preview.

- `node scripts/validate-gantt-manual-milestone-drag-release-dom.mjs`
  - Confirma bloqueo seguro cuando la cascada real violaria el inicio de proyecto.
  - El hito no se guarda solo sin sucesores validos.

- `python tmp/audit_santiago_gantt_full.py`
  - `187` filas.
  - `0` filas antes de inicio de proyecto.
  - `0` incidencias de dependencias.
  - `0` incidencias de hito manual.

- `node scripts/validate-gantt-ff-predecessor-dom.mjs`
  - Confirma que la relacion `FF` real no rehidrata fechas de febrero.

- `node scripts/validate-gantt-santiago-route-all.mjs`
  - `30/30` rutas calculadas.
  - `0` incidencias.

- `node scripts/validate-gantt-santiago-full-dom.mjs`
  - `0` diagonales.
  - `0` rutas directas indebidas.
  - `0` rutas vacias.

- `npm run smoke:gantt-classic`
  - OK.

- `npm run build`
  - OK.

## Impacto

Solo frontend Gantt clasico. No cambia motor backend, datos productivos, Presupuesto, Cronograma Valorado, licencias ni BIM.

## Cierre

Implementacion cerrada al 100%.

