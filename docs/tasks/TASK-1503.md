# TASK-1503 - Implementacion hotfix Gantt hito manual por hora y cascada completa

Estado: Completada
Avance anterior: 0%
Avance nuevo: 100%
Fecha: 2026-05-05
Modo: GIPROY CLASICO

## Cambios

- `frontend/src/components/projects/CronogramaGantt.jsx`
  - Los hitos manuales ya no calculan el movimiento solo con `Math.round(pixelDelta / pixelsPerDay)`.
  - En arrastre de hito manual, la fecha/hora se resuelve desde la posicion real del puntero sobre el timeline con `getTimelineDateFromPx(...)`.
  - El draft del hito guarda `start_date` y `end_date` con esa fecha/hora real.
  - `lastDeltaDays` pasa a reflejar la distancia laboral real entre origen y destino, permitiendo commits de horas/fracciones de jornada.
  - La persistencia de cascada de hito manual fuerza `requiresFullTrabajoRefresh: true` para evitar estados delta parciales hasta refrescar.

## Validacion real

- `node scripts/validate-gantt-manual-milestone-drag-preview-dom.mjs`
  - Empresa: `Santiago Bermeo`.
  - Presupuesto: `13`.
  - Hito: `hm-mok8sj9c-j04f`.
  - Resultado: `bar_delta_px=220`, `pointer_drag_px=220`, `target_delta_px=220`.
  - La ruta se actualiza durante el preview y el sucesor acompana al hito en la misma magnitud visual.

- `python tmp/audit_santiago_gantt_full.py`
  - `187` filas.
  - `0` filas antes de inicio de proyecto.
  - `0` incidencias de dependencias.
  - `0` incidencias de hito manual.

- `node scripts/validate-gantt-santiago-route-all.mjs`
  - `30/30` rutas calculadas.
  - `0` incidencias.

- `node scripts/validate-gantt-santiago-full-dom.mjs`
  - `0` diagonales.
  - `0` rutas directas indebidas.
  - `0` rutas vacias.

- `npm run smoke:gantt-classic`
  - OK.

- `npm run build`
  - OK.

## Impacto

Solo frontend Gantt clasico. No cambia motor backend, datos productivos, Presupuesto, Cronograma Valorado, licencias ni BIM.

## Cierre

Implementacion cerrada al 100%.

