# TASK-1507 - Implementacion Gantt persistencia optimista de hito manual sin rebote

Estado: Completada
Avance anterior: 0%
Avance nuevo: 100%
Fecha: 2026-05-05
Modo: GIPROY CLASICO

## Cambios

- `frontend/src/components/projects/CronogramaGantt.jsx`
  - El commit de movimiento de hito manual actualiza de forma optimista `configDraft.manual_milestones` con la posicion soltada antes de esperar la persistencia de la cascada.
  - Se mantiene `pendingManualMilestonesSignatureRef` para proteger la rehidratacion mientras la operacion esta en curso.
  - Si la cascada no se puede persistir, se revierte `manual_milestones` al snapshot anterior.

- `frontend/scripts/smoke-cronogramas-gantt-manual-milestone-drag.mjs`
  - Se refuerza el smoke para exigir que el hito manual aplique estado optimista y tenga rollback si falla la cascada.

## Resultado funcional

- Durante el drag, el hito sigue al raton.
- Al soltar, el hito permanece en la posicion soltada mientras se guarda.
- Si el guardado falla, se revierte al estado anterior de forma explicita.
- Si el guardado pasa, no hay salto visual intermedio hacia el origen.

## Validaciones ejecutadas

- `npm run smoke:gantt-classic`
  - OK.

- `npm run build`
  - OK.

- `python tmp/validate_santiago_gantt_integrity.py`
  - `187` filas.
  - `0` tareas antes de `2026-03-24T08:00:00`.
  - `0` incidencias de dependencias.

- `python tmp/audit_santiago_gantt_full.py`
  - `manual_milestone_count=1`.
  - `dependency_counts`: `FS=28`, `FF=1`, `SS=0`, `SF=0`.
  - `before_project_start_count=0`.
  - `dependency_issue_count=0`.
  - `manual_issue_count=0`.
  - `critical_count=28`.

- `node scripts/validate-gantt-santiago-route-all.mjs`
  - `dependency_count=30`.
  - `routed_count=30`.
  - `issue_count=0`.

## Restricciones cumplidas

- No se uso DOM, conforme a la regla indicada por el usuario.
- No se modifica backend.
- No se modifica base de datos.
- No se cambia arquitectura.
- No se cambia licenciamiento.
- No se activa ni contamina BIM.

## Cierre

Implementacion cerrada al 100%.
