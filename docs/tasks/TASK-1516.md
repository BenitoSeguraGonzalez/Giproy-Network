# TASK-1516 - Saneamiento real Gantt Santiago Bermeo

Estado: En validacion del usuario
Avance: Pendiente de confirmacion expresa
Fecha: 2026-05-06
Modo: GIPROY CLASICO
Control: TASK-1511

## Objetivo

Sanear el Gantt real de la empresa `Santiago Bermeo` despues de que el hito manual inicial quedara persistido sin sucesoras y desplazado fuera del estado base esperado.

## Causa verificada

Antes del saneamiento, el cronograma era coherente para el motor linea-linea, pero estaba roto en la relacion manual:

- hito `hm-mok8sj9c-j04f` persistido en `2026-06-02T08:00:00`;
- `successor_dependencies: []`;
- linea `49` sin gobierno real del hito manual;
- la auditoria independiente marcaba `manual_dependency_checks=0`.

Esto permitia que el hito se moviera como elemento aislado y que el commit no tuviera ruta manual real que propagar.

## Accion ejecutada

Se ejecuto el saneador limpio existente `tmp/rebuild_santiago_gantt_dependencies_clean.py`, que:

- creo backup previo: `tmp/santiago_bermeo_gantt_clean_rebuild_backup_20260506_113203.json`;
- creo snapshot de relaciones: `tmp/santiago_bermeo_gantt_relations_snapshot_20260506_113203.json`;
- restauro el hito `hm-mok8sj9c-j04f` a `2026-03-24T08:00:00`;
- restauro `successor_dependencies` con relacion `FS` hacia `target_id=49`;
- recalculo y persistio la cascada de `187` filas.

## Resultado tecnico

- Proyecto `7`, presupuesto `13`.
- Hito `hm-mok8sj9c-j04f`: `2026-03-24T08:00:00`.
- Linea `49` (`1.1.1`): `2026-03-24T08:00:00 -> 2026-03-24T10:18:43`.
- Dependencias linea-linea: `29` (`28 FS`, `1 FF`).
- Dependencia hito-linea: `1`.
- Filas antes del inicio de proyecto: `0`.
- Incidencias de dependencias: `0`.
- Incidencias de hitos manuales: `0`.

## Validacion

- `python tmp/audit_santiago_gantt_full.py` OK.
- `python tmp/validate_santiago_gantt_integrity.py` OK.
- `python tmp/validate_santiago_gantt_rules_independent.py` OK, con `manual_dependency_checks=1`.
- `python tmp/audit_santiago_gantt_links.py` OK.
- `npm run smoke:gantt-classic` OK.
- `smoke-classic-no-bim-contamination` OK dentro del smoke clasico.

## No interferencia BIM

- Saneamiento limitado a datos reales del Gantt clasico de `Santiago Bermeo`.
- Sin UX BIM, sin rutas BIM, sin dependencias BIM y sin acoplamientos nuevos.

## Cierre

No se cierra hasta confirmacion expresa del usuario en UI real.
