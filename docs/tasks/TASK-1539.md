# TASK-1539 - Rail de proyecto clasico refuerza bordes sin cambiar patron activo

Fecha: 2026-05-07

Modo: GIPROY CLASICO

## Resultado

Se descarta la version intermedia de boton suave por resultar visualmente pesada para el rail. Se conserva el patron anterior del menu vertical, se refuerzan bordes del boton y del contenedor del icono, y se compacta la caja colapsada.

## Cambios

- `frontend/src/pages/Proyectos.jsx`
  - Restaura el boton inline anterior del rail.
  - Mantiene el activo oscuro con sombra y texto/icono blanco.
  - Agrega borde al boton y al contenedor interno del icono para ganar presencia visual.
  - En rail colapsado limita el boton a una caja de `58px` para reducir la altura percibida del borde.
  - Muestra el punto de color del modulo activo tambien en estado colapsado, centrado verticalmente sobre el borde derecho.
  - Agrega `PROJECTS_SECTION_LABEL_REVEAL_MOTION` como prueba modular de animacion para desplazar y revelar el label al expandir.
  - Sustituye el `aside` del rail por `MotionAside` con animacion de ancho por `spring` para suavizar la apertura total del panel.
  - Mantiene los botones con `w-full h-[58px]` tanto en colapsado como en expandido, evitando el salto entre caja centrada fija y caja expandida.
  - Reutiliza el mismo `layoutId` del indicador activo entre colapsado y expandido para evitar redibujado brusco.
  - Conserva filtros de permisos y ocultamiento de BIM cuando no esta habilitado.

## No cambios

- Sin cambios en reportes de cronogramas.
- Sin cambios en backend, APIs, PostgreSQL, persistencia ni permisos.
- Sin activacion de UX BIM ni acoplamientos hacia BIM.
- Sin cambios en la animacion existente de cambio de modulo.
- Sin cambios en la estructura funcional de opciones del rail.

## Validacion

- `npm run build` OK.
- `npm run smoke:gantt-classic` OK, incluyendo `smoke-classic-no-bim-contamination`.
- ESLint focal no queda limpio por deuda previa en `Proyectos.jsx`.

## Diagnostico UX

La apertura del panel era fluida, pero cada boton cambiaba de `mx-auto h-[58px] w-[58px] justify-center` a `w-full justify-start px-4 py-3` en el primer frame de expansion. Ese cambio interno provocaba la percepcion de salto horizontal y recolocacion antes de que apareciera el label.

## Estado

En validacion del usuario. Pendiente confirmacion visual en UI real.
