# TASK-1543 - Reportes de Proyecto usan boton generico icon-only

Fecha: 2026-05-07

Modo: GIPROY CLASICO

## Resultado

El boton generico `ProjectSectionReportButton` pasa a ser icon-only compacto y Presupuesto deja de usar un boton propio de reporte para adoptar el mismo componente comun.

## Cambios

- `frontend/src/components/projects/ProjectSectionReportButton.jsx`
  - Reemplaza el uso de `ProjectHeaderActionButton` stacked por un boton compacto directo.
  - Conserva `FileSpreadsheet` como icono de reporte.
  - Mantiene `onClick`, `disabled`, `title`, `aria-label`, `className` y fallback informativo.
  - Elimina el label visible `Reporte`.

- `frontend/src/components/presupuestos/PresupuestoDetail.jsx`
  - Reemplaza el `ProjectHeaderActionButton` local de reporte por `ProjectSectionReportButton`.
  - Porta la funcionalidad existente: abre `setShowReportConfigModal(true)`.
  - Conserva `disabled={generatingReport}` y el titulo `Configurar y generar reporte profesional`.

## No cambios

- Sin cambios en preview/export de reportes.
- Sin cambios en backend, APIs, PostgreSQL, reporting service ni plantillas.
- Sin cambios en Gantt, cash flow, cronogramas funcionales, calculos ni persistencia.
- Sin UX BIM ni acoplamientos BIM.

## Validacion

- `npm run build` OK.
- `npm run smoke:gantt-classic` OK, incluyendo `smoke-classic-no-bim-contamination`.

## Estado

En validacion del usuario. No cerrar sin confirmacion visual expresa.
