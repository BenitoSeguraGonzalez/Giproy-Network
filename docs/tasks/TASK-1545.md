# TASK-1545 - AppHint estandariza hints claros y oscuros

Fecha: 2026-05-07

Modo: GIPROY CLASICO

## Resultado

Se crea `AppHint` como componente comun de hints visuales con variantes `light` y `dark`, y se migran las familias principales que ya usaban implementaciones locales o duplicadas.

## Cambios

- `frontend/src/components/ui/AppHint.jsx`
  - Nuevo helper visual reutilizable con portal, posicionamiento sobre cursor/trigger, cierre por scroll/resize/tecla y variantes clara/oscura.
  - Soporta contenido textual o JSX, ancho minimo/maximo, offset, `zIndex`, clase de trigger y condicion `shouldShow`.

- `frontend/src/components/projects/CronogramaGantt.jsx`
  - `GanttHeaderTooltip` queda como wrapper de `AppHint` con variante clara.
  - Se conserva la semantica de los hints del header de Gantt.

- `frontend/src/components/ui/ControlRail.jsx`
  - `ControlRailTooltip` queda como wrapper de `AppHint` con variante oscura.
  - Se conserva el skin oscuro con borde claro sutil del rail.

- `frontend/src/components/presupuestos/LineasPresupuestoTab.jsx`
  - `BudgetToolbarTooltip` deja su implementacion local antigua y adopta `AppHint` claro.

- `frontend/src/components/ui/CatalogSidebarCard.jsx`
  - El hint de truncamiento del catalogo lateral usa `AppHint` claro y conserva deteccion de overflow.

- `frontend/src/components/projects/ProjectSectionReportButton.jsx`
  - El boton generico de reportes queda con hint visual claro y `aria-label`, sin tooltip nativo visible.

## No cambios

- Sin cambios en calculos, reportes documentales, preview/export, Gantt funcional, Presupuesto funcional, backend, APIs, PostgreSQL ni persistencia.
- Sin cambios en rutas, permisos ni UX BIM.
- No se reemplazan todos los `title` nativos de la app cuando funcionan solo como fallback accesible o estan fuera del patron visual principal.

## Validacion

- `npm run build` OK.
- `npm run smoke:gantt-classic` OK, incluyendo `smoke-classic-no-bim-contamination`.

## Estado

En validacion visual del usuario. No cerrar sin confirmacion expresa.
