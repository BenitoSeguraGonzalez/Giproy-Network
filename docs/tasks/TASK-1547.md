# TASK-1547 - Cierre de hints visuales con AppHint

Fecha: 2026-05-07

Modo: GIPROY CLASICO

## Resultado

Se completa el segundo pase de homogeneidad de hints visuales. Los casos detectados fuera del estandar pasan a usar `AppHint` y se conserva `title` nativo solo como fallback accesible.

## Cambios

- `frontend/src/components/projects/Cronogramas.jsx`
  - La Curva S deja de usar estado local de tooltip y usa `AppHint` oscuro sobre puntos SVG.
  - Los chips operativos oscuros de pendientes, ajustes y reconciliacion usan `AppHint` oscuro.
  - Los chips financieros claros de inversion/caja usan `AppHint` claro.
  - El resumen compacto inferior y sus cards usan `AppHint` oscuro.

- `frontend/src/pages/BasesTrabajo.jsx`
  - El hint visual de descripcion de base pasa de hover CSS local a `AppHint` claro.

- `frontend/src/components/marketplace/MarketplaceOriginBadgeSet.jsx`
  - El modo `tooltip` deja de depender de `title` nativo y usa `AppHint` claro.

- `frontend/src/pages/MarketplaceAdminDashboard.jsx`
  - La ayuda de tramos automaticos deja su portal local y usa `AppHint` claro con contenido estructurado.

## No cambios

- Sin cambios en backend, APIs, PostgreSQL, calculos, reportes documentales, exportaciones, permisos ni persistencia.
- Sin cambios en BIM, UX BIM, rutas BIM ni acoplamientos nuevos.
- Los `title` nativos restantes quedan como accesibilidad/fallback en botones, modales, campos o celdas truncadas.

## Validacion

- `npm run build` OK.
- `npm run smoke:gantt-classic` OK, incluyendo `smoke-classic-no-bim-contamination`.

## Estado

Tema de hints visuales cerrado tecnicamente; pendiente solo confirmacion visual del usuario.
