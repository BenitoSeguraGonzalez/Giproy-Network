# TASK-1551 - Cache persistente de mapa georreferenciado de proyecto

Fecha: 2026-05-07

Modo: GIPROY CLASICO

## Resultado

Datos de Proyecto mantiene una imagen georreferenciada oficial por proyecto para los reportes, generada con las coordenadas y el zoom aceptados por el usuario.

## Cambios

- `backend/app/models/proyecto_detalle.py`
  - Agrega metadatos `georef_map_url`, `georef_map_status`, `georef_map_signature`, `georef_map_generated_at` y `georef_map_error`.
- `backend/app/schemas/proyecto_detalle.py`
  - Expone esos metadatos solo en la respuesta del detalle.
- `backend/alembic/versions/ab6c7d8e9f10_project_georef_map_cache.py`
  - Migra la persistencia de la cache georreferenciada por proyecto.
- `backend/app/services/reporting.py`
  - Genera y guarda `uploads/proyectos/{empresa_id}/{codigo_root}/georef/georreferenciacion.png`.
  - Valida la firma `latitud/longitud/zoom` para reutilizar la imagen si sigue vigente.
  - Marca `missing` sin coordenadas y `pending` si la generacion falla.
  - Usa la imagen cacheada como primera fuente del reporte.
- `backend/app/api/endpoints/proyecto_detalles.py`
  - Refresca la cache al guardar Datos de Proyecto.
  - Reintenta al leer el detalle si la cache esta pendiente o desfasada.
- `frontend/src/components/projects/DatosProyecto.jsx`
  - Muestra un indicador compacto `Mapa listo`, `Mapa pendiente` o `Sin mapa de reporte`.
  - Marca localmente la cache como pendiente cuando cambian coordenadas o zoom.

## No cambios

- Sin georreferenciacion por usuario.
- Sin georreferenciacion por revision.
- Sin cambio de proveedor de mapas.
- Sin dependencia BIM ni activacion de UX BIM.

## Validacion

- `python -m py_compile backend/app/services/reporting.py backend/app/api/endpoints/proyecto_detalles.py backend/app/models/proyecto_detalle.py backend/app/schemas/proyecto_detalle.py backend/alembic/versions/ab6c7d8e9f10_project_georef_map_cache.py` OK.
- `alembic upgrade ab6c7d8e9f10` OK sobre la rama de georreferenciacion.
- `npm run build` OK.
- `npm run smoke:gantt-classic` OK, incluyendo `smoke-classic-no-bim-contamination`.

## Estado

Implementado y pendiente de validacion visual del usuario en Datos de Proyecto.
