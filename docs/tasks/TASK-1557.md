# TASK-1557 - Datos de Proyecto geocodifica desde backend comun

Fecha: 2026-05-07

Modo: GIPROY CLASICO

## Resultado

La accion `Localizar` de Datos de Proyecto ya no consulta Nominatim desde el navegador. El frontend invoca un endpoint propio y el backend comun resuelve candidatos de direccion con identificacion HTTP controlada.

## Cambios

- `backend/app/schemas/proyecto_detalle.py`
  - Agrega `ProyectoGeocodeRequest` y `ProyectoGeocodeResponse`.
- `backend/app/api/endpoints/proyecto_detalles.py`
  - Agrega `POST /proyecto-detalles/geocode-address`.
  - Construye candidatos `exacta`, `urbana`, `mixta`, `territorial` y `regional`.
  - Consulta Nominatim desde backend usando `User-Agent` y `Referer` de GiProy.
  - Devuelve coordenadas, zoom, mensaje, consulta usada y nombre visible.
  - Restringe la accion a administradores/superadministradores.
- `frontend/src/api/proyectoDetalle.js`
  - Agrega `geocodeAddress`.
- `frontend/src/components/projects/DatosProyecto.jsx`
  - `handleGeocodeAddress` consume el endpoint comun.
  - Retira la llamada `fetch` directa a Nominatim y el helper duplicado del frontend.
  - Marca la cache del mapa como `pending` cuando la coordenada/zoom cambia por localizacion.

## Validacion

- `python -m py_compile backend/app/api/endpoints/proyecto_detalles.py backend/app/schemas/proyecto_detalle.py` OK.
- `npm run build` OK.
- `npm run smoke:gantt-classic` OK, incluyendo `smoke-classic-no-bim-contamination`.

## No interferencia BIM

Sin cambios en UX BIM, rutas BIM, modelos BIM ni acoplamientos hacia BIM.

## Estado

Implementado; pendiente de prueba visual del usuario con una direccion real.
