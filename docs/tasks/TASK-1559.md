# TASK-1559 - Geocoder prioriza intersecciones de Ecuador

Fecha: 2026-05-07

Modo: GIPROY CLASICO

## Resultado

El backend de geocodificacion de Datos de Proyecto interpreta direcciones ecuatorianas tipo `Av. 12 de Abril y Av. Loja` como interseccion y genera consultas alternativas mas utiles.

## Cambios

- `backend/app/api/endpoints/proyecto_detalles.py`
  - Agrega normalizacion de espacios para consultas geograficas.
  - Deduplica partes del contexto para evitar consultas como `Cuenca, Cuenca, Azuay, Ecuador`.
  - Detecta intersecciones ecuatorianas en el campo `direccion`.
  - Genera variantes con `y`, `&`, `con`, `/` y coma entre calles.
  - Agrega fallbacks por calle principal y calle de interseccion dentro del mismo contexto.
  - Reordena las consultas para enviar primero `direccion, ciudad/canton, provincia, pais`, que es mas natural para geocodificacion.

## Validacion

- `python -m py_compile backend/app/api/endpoints/proyecto_detalles.py backend/app/schemas/proyecto_detalle.py` OK.
- Prueba focal de candidatos para `Av. 12 de Abril y Av. Loja`, `Cuenca`, `Azuay`, `Ecuador`: genera consulta exacta, variantes de interseccion y fallbacks por calle sin duplicar `Cuenca`.
- `npm run build` OK.
- `npm run smoke:gantt-classic` OK, incluyendo `smoke-classic-no-bim-contamination`.

## No interferencia BIM

Sin cambios en UX BIM, rutas BIM, modelos BIM ni acoplamientos hacia BIM.

## Estado

Implementado; pendiente de prueba visual del usuario con una direccion real del proyecto.
