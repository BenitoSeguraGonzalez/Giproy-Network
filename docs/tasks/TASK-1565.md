# TASK-1565 - Datos Proyecto localiza intersecciones por geometria OSM

Fecha: 2026-05-07

Modo: GIPROY CLASICO

## Resultado

Datos de Proyecto ahora localiza intersecciones ecuatorianas mediante geometria OSM/Overpass antes de caer a Nominatim. El modal tambien elimina la coordenada duplicada del encabezado y da mas peso visual al titulo.

## Cambios

- `frontend/src/components/projects/DatosProyecto.jsx`
  - La cabecera del modal muestra `Geo-referenciacion` como titulo principal y deja las coordenadas solo en el panel lateral.
  - `Localizar` resuelve primero intersecciones ecuatorianas con Overpass desde el frontend, antes de aceptar una respuesta del backend activo.
  - El fallback de navegador consulta Overpass para intersecciones ecuatorianas.
  - Se calcula el punto de cruce aproximando la geometria de ambas calles.
  - Se evita aceptar resultados de Nominatim que no contengan ambas calles.
  - Se evita caer a ciudad/canton para una direccion con forma de interseccion.
- `backend/app/api/endpoints/proyecto_detalles.py`
  - El backend comun consulta Overpass antes de Nominatim para intersecciones.
  - Se agrega filtro de terminos obligatorios para no aceptar coincidencias parciales.
  - Para Cuenca/Azuay se usa bbox estable de ciudad, reduciendo ambiguedad del area administrativa.

## Validacion

- Validacion focal real Overpass: `Miguel Velez y Rafael Maria Arizaga, Cuenca, Azuay, Ecuador` devuelve `latitud=-2.8895224`, `longitud=-79.010998`, `zoom=18`, `source=overpass`.
- `python -m py_compile backend/app/api/endpoints/proyecto_detalles.py backend/app/schemas/proyecto_detalle.py` OK.
- `npm run build` OK.
- `npm run smoke:gantt-classic` OK, incluyendo `smoke-classic-no-bim-contamination`.

## No interferencia BIM

Sin UX BIM, rutas BIM, modelos BIM ni acoplamientos nuevos hacia BIM.

## Estado

Implementado y validado.
