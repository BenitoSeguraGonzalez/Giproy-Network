# TASK-1567 - Datos Proyecto corrige listados incrementales del acta

Fecha: 2026-05-07

Modo: GIPROY CLASICO

## Resultado

Los listados de Objetivos, Restricciones y Supuestos permiten escribir frases con espacios y agregar nuevas filas correctamente. El boton de alta queda reducido al simbolo `+`.

## Cambios

- `frontend/src/components/projects/DatosProyecto.jsx`
  - Agrega `parseEditableListItems` para leer listados editables sin recortar espacios ni eliminar filas vacias durante la edicion.
  - Agrega `serializeEditableListItems` para preservar filas vacias cuando hay mas de una fila en curso.
  - Los tres listados usan el parseo/serializacion editable.
  - El boton `+ Agregar` pasa a ser un boton iconico `+` con `title` y `aria-label`.

## Validacion

- Prueba focal de serializacion: `["objetivo "]` conserva el espacio; `+` genera `["objetivo ",""]`.
- `npm run build` OK.
- `npm run smoke:gantt-classic` OK, incluyendo `smoke-classic-no-bim-contamination`.

## No interferencia BIM

Sin UX BIM, rutas BIM, modelos BIM ni acoplamientos nuevos hacia BIM.

## Estado

Implementado y validado.
