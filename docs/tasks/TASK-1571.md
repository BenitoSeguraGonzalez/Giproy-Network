# TASK-1571 - Implementacion de footer visual de reportes y cache versionada

Fecha: 2026-05-07

## Resultado

Se ajusto el visor comun de reportes:

- El pie elimina el texto descriptivo largo.
- Las acciones `PDF`, `PDF Excel` y `Excel` usan botones compactos con el material visual comun.
- Cada accion queda envuelta en `AppHint` y conserva `aria-label`.
- El boton principal `Excel` mantiene acento naranja institucional.
- La cache de exportaciones incorpora una version de render para invalidar PDFs previos al ajuste formal.

## Archivos

- `frontend/src/components/reporting/CommonReportPreviewModal.jsx`
- `backend/app/services/reporting.py`

## Validacion

- `python -m py_compile backend/app/services/reporting.py`: OK.
- `python -m pytest app/tests/test_reporting_stakeholders_excel_pdf.py -q`: 2 passed.
- `npm run build`: OK.
- `node scripts/smoke-classic-no-bim-contamination.mjs`: OK.

## Impacto

Reportes clasicos y visor comun. Sin cambios en APIs, plantillas, modelos, persistencia ni BIM.
