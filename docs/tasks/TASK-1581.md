# TASK-1581 - Prueba de skin oscuro en cabeceras de Datos de Proyecto

## Estado
Implementada y validada.

## Alcance
- Modo: GIPROY CLASICO.
- Capa: frontend clasico, seccion `Datos de Proyecto`.
- Objetivo: probar el skin oscuro tipo Gantt en las cabeceras plegables de Datos de Proyecto.

## Cambios
- `frontend/src/components/projects/DatosProyecto.jsx`
  - La constante `COLLAPSIBLE_SECTION_HEADER_BUTTON` adopta fondo oscuro y borde inferior oscuro.
  - Se agregan `COLLAPSIBLE_SECTION_TITLE_CLASS` y `COLLAPSIBLE_SECTION_SUBTITLE_CLASS` para centralizar el color de titulo/subtitulo y facilitar la reversa.
  - Las cabeceras de las secciones usan esas constantes.
  - La cabecera lateral `8. Documentos e imágenes / Repositorio visual del proyecto` adopta el mismo skin oscuro para no quedar fuera de familia.
  - La cabecera lateral queda `sticky` dentro del panel derecho para mantenerse visible durante scroll con rueda o scrollbar.
  - El boton circular de despliegue (`renderSectionToggleIcon`) conserva tamano, forma y animacion, pero reduce su halo exterior para no quedar difuso sobre el header oscuro.
  - `CollapsibleSectionBody` reemplaza el montaje/desmontaje brusco por una transicion de altura y opacidad con curva suave tipo panel lateral.

## Reversibilidad
El cambio queda concentrado en constantes del propio archivo. Para volver al skin anterior basta con restaurar:
- `COLLAPSIBLE_SECTION_HEADER_BUTTON`
- `COLLAPSIBLE_SECTION_TITLE_CLASS`
- `COLLAPSIBLE_SECTION_SUBTITLE_CLASS`
- la sombra de `renderSectionToggleIcon`, si se quiere recuperar el halo anterior.

## Validacion
- `npm run build` OK desde `frontend/`.
- `node -e` de validacion JSON documental OK.
- `node scripts/smoke-classic-no-bim-contamination.mjs` OK desde `frontend/`.

## No interferencia BIM
Sin UX BIM, rutas BIM, modelos BIM, dependencias BIM ni acoplamientos nuevos.
