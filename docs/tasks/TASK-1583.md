# TASK-1583 - Adecuacion visual de Stakeholders al estilo base clasico

## Estado
Implementada y validada.

## Alcance
- Modo: GIPROY CLASICO.
- Capa: frontend clasico.
- Archivo principal: `frontend/src/components/projects/Stakeholders.jsx`.
- Tipo de cambio: visual/UX. Sin backend, persistencia, contratos API ni reportes generados.

## Cambios
- Header del modulo realineado con la cabecera clara/soft de `Datos de Proyecto`, evitando una banda negra sin justificacion.
- Se mantiene el morado como identidad visual propia en el icono y label `Stakeholders`.
- Toolbar compacta con acciones `Roles`, `Nuevo` y reporte generico existente.
- Workbench principal soft con chips de conteo total/visible y busqueda natural compacta.
- Tabla mas densa:
  - header sticky oscuro,
  - filas con menor altura,
  - acciones compactas visibles,
  - `AppHint` para nombres, instituciones y ubicaciones largas.
- Estado vacio sobrio y operativo.
- Modal de alta/edicion reorganizado en secciones:
  - Identidad,
  - Contacto,
  - Perfil profesional,
  - Ubicacion.
- Modal con header oscuro propio, footer compacto e inputs `h-11`, manteniendo el guardado existente.

## No cambios
- No se modifica backend.
- No se cambia el modelo de directorio comun por `codigo_root`.
- No se cambia la elegibilidad hacia EDO/EDT.
- No se agrega activacion por revision.
- No se modifica reporting ni plantillas.

## Validacion
- `npm run build` OK desde `frontend/`.
- `node scripts/smoke-classic-no-bim-contamination.mjs` OK desde `frontend/`.
- Busqueda focal en `Stakeholders.jsx` sin referencias BIM.

## No interferencia BIM
Sin UX BIM, rutas BIM, modelos BIM, dependencias BIM ni acoplamientos nuevos.
