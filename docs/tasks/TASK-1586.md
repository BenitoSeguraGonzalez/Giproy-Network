# TASK-1586 - Toolbar oscura del directorio Stakeholders

## Estado
Implementada y validada.

## Alcance
- Modo: GIPROY CLASICO.
- Frontend clasico: `Stakeholders`.
- Ajuste visual del header interno del listado/directorio.

## Objetivo
- Retirar los labels/chips `registrados`, `visibles` y `busqueda natural sin acentos` del header interno.
- Pasar la franja del buscador a skin oscuro.
- Colocar el buscador a la izquierda y mantener la busqueda natural sin acentos como comportamiento, no como texto visible.

## No cambios
- No cambia persistencia.
- No cambia backend.
- No cambia reporting.
- No cambia EDO/EDT.
- No se toca BIM.

## Resultado
- El header interno del listado queda limpio, oscuro y compacto.
- El buscador queda dentro del grupo derecho de acciones con fondo blanco, alineado al patron usado en Gantt.
- La cabecera reduce padding vertical y altura de controles para recuperar densidad.
- Se conserva la normalizacion de busqueda sin acentos.

## Validacion esperada
- `npm run build`.
- Smoke no contaminacion BIM.

## Validacion ejecutada
- `npm run build` OK desde `frontend/`.
- `node scripts/smoke-classic-no-bim-contamination.mjs` OK desde `frontend/`.
- `rg` confirma que los labels visibles retirados ya no aparecen en `Stakeholders.jsx`.
