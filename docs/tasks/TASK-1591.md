# TASK-1591 - Politica de borrado seguro Stakeholders

## Estado
Implementada y validada.

## Alcance
- Modo: GIPROY CLASICO.
- Backend comun: `stakeholders`, `EDO`, `EDT`.
- Frontend clasico: mensaje de eliminacion en `Stakeholders`.

## Objetivo
- Impedir que se elimine un stakeholder con rol o uso activo en EDO/EDT.
- Evitar nodos huerfanos o cascadas destructivas sobre EDO/EDT.
- Indicar al usuario que primero debe liberarse en EDO/EDT.

## No cambios
- No cambia el modelo de roles.
- No cambia sincronizacion EDO/EDT -> Stakeholders.
- No cambia reporting.
- No se toca BIM.

## Resultado
- `stakeholder_repo.delete(...)` valida usos en `EdoNode`, `EdtNode` y asignaciones con `rol_id`.
- El endpoint `DELETE /stakeholders/{id}` devuelve `409 Conflict` cuando el borrado esta bloqueado.
- El frontend muestra el detalle devuelto por backend.

## Validacion ejecutada
- `python -m py_compile app/repositories/stakeholder.py app/api/endpoints/stakeholders.py` OK.
- `python -m pytest app/tests/test_stakeholder_role_sync.py -q` OK, 4 passed.
- `npm run build` OK desde `frontend/`.
- `node scripts/smoke-classic-no-bim-contamination.mjs` OK desde `frontend/`.
