# TASK-1630 - Compactacion de plantilla Excel de indirectos

Fecha: 2026-05-13

Modo: GIPROY CLASICO

## Objetivo

Corregir la cabecera del Excel editable de `Indirectos` porque la version anterior quedo desproporcionada, con celdas demasiado estrechas, merges verticales y labels partidos que diluian la informacion del reporte.

## Alcance autorizado

- Plantilla editable `docs/reportes/001 - Porcentajes de Indirectos.xlsx`.
- Backend comun de reporting clasico solo para invalidar cache de exportacion.
- Documentacion TASK, CHANGELOG, project_state y runtime.

## Criterio de referencia

Se revisaron las plantillas existentes de reportes clasicos:

- `001 - Presupuesto.xlsx`
- `001 - EDO.xlsx`
- `001 - EDT - listado.xlsx`
- `001 - Stakeholders.xlsx`
- `001 - Analisis - APUS - General.xlsx`
- `002 - Analisis - APUS - SERCOP.xlsx`

El patron valido es: titulo compacto, metadatos en pocas filas, ancho real para descripcion/proyecto, resumen tabular denso y tabla principal visible pronto.

## Implementacion

- Se remaqueta `001 - Porcentajes de Indirectos.xlsx` con columnas A:I proporcionales al contenido.
- El bloque de identificacion queda en filas compactas sin labels partidos.
- El resumen economico queda como tabla de dos lineas de labels/valores, no como tarjetas altas.
- El listado de indirectos empieza pronto y conserva los placeholders existentes.
- El resumen final queda debajo del listado y se desplaza automaticamente con el motor generico de plantilla.
- La cache de exportacion se invalida mediante `REPORT_EXPORT_RENDER_VERSION = 2026-05-13-budget-indirects-report-v4`.

## No interferencia BIM

- Sin rutas BIM.
- Sin UX BIM.
- Sin modelos BIM.
- Sin imports BIM.
- Sin feature flags BIM.
- Sin acoplamiento hacia la capa BIM futura.

## Validacion

- `python -m py_compile app\services\reporting.py app\api\endpoints\reporting.py` OK.
- Generacion focal del Excel `Indirectos` OK: 0 placeholders pendientes, columnas A:I proporcionales, cabecera compacta y listado iniciado en la parte alta del documento.
- Verificacion de formato de porcentajes OK: los valores `21` y `15` conservan formato numerico `0.00`, evitando lectura erronea como `2100%`.
- `node scripts\smoke-classic-no-bim-contamination.mjs` OK.
- `python -m json.tool docs\project_state.json` OK.
- `python -m json.tool docs\runtime\WORK_MODE_STATE.json` OK.
