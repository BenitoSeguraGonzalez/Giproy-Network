# TASK-1632 - Hotfix normalizePreviewText en previo de indirectos

Fecha: 2026-05-13

Modo: GIPROY CLASICO

## Objetivo

Corregir el `ReferenceError: normalizeText is not defined` reportado en el previo clasico de reportes de Presupuesto al abrir/generar `Indirectos`.

## Alcance autorizado

- Frontend clasico del visor comun de reportes.
- Bundle frontend generado.
- Documentacion TASK, CHANGELOG, project_state y runtime.

## Implementacion

- `CommonReportPreviewModal` deja de invocar el helper inexistente `normalizeText`.
- La deteccion del reporte `Indirectos` reutiliza el helper existente `normalizePreviewText`.
- Se reconstruye el bundle frontend y el chunk del visor comun cambia a `blobDownload-C1Avb8vF.js`.

## No interferencia BIM

- Sin rutas BIM.
- Sin UX BIM.
- Sin modelos BIM.
- Sin imports BIM.
- Sin feature flags BIM.
- Sin acoplamiento hacia la capa BIM futura.

## Validacion

- `npm run build` OK en ruta real del repo.
- `node scripts\smoke-classic-no-bim-contamination.mjs` OK.
- `rg "normalizeText\(" frontend\src\components\reporting\CommonReportPreviewModal.jsx frontend\dist` sin coincidencias.
- `python -m json.tool docs\project_state.json` OK.
- `python -m json.tool docs\runtime\WORK_MODE_STATE.json` OK.
