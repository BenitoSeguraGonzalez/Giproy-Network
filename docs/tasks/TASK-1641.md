## TASK-1641 - Previo documental GIPROY en EDO/EDT y acento de exportacion

**Fecha:** 2026-05-14  
**Modo:** GIPROY CLASICO  
**Area:** Frontend clasico / Reportes comunes

### Objetivo

Actualizar el visor comun de reportes para que el previo de EDO y EDT use una presentacion documental alineada con el lanzador `GIPROY`, en vez de una tabla tipo Excel, y mover el acento naranja corporativo desde `Excel` hacia `GIPROY` en los botones inferiores de ejecucion.

### Alcance autorizado

- Solo capa clasica.
- Solo presentacion visual del modal comun de reportes.
- Sin cambios en handlers, payloads, contratos API, generadores backend, plantillas, persistencia ni PostgreSQL.
- Sin mostrar ni activar UX BIM.

### Implementacion

- `CommonReportPreviewModal` renderiza los previos documentales `edo_document` y `edt_document` como documento con cabecera `GIPROY`, metadatos, resumen y tarjetas de lineas.
- Se elimina del branch documental el layout de tabla con bordes negros tipo Excel.
- El boton `GIPROY` adopta `variant="accent"` y el boton `Excel` vuelve al estilo secundario.
- Ajuste posterior: las tarjetas del previo documental eliminan el lateral/sombra azul de acento y quedan con borde neutro.
- Ajuste posterior: el boton `GIPROY` gana profundidad soft para alinearse con los demas botones inferiores.
- Ajuste posterior: el boton `GIPROY` recibe un borde negro muy sutil para destacar sobre el fondo sin perder el acento naranja.
- El smoke de botones EDO/EDT ahora valida el orden `GIPROY`, `Excel`, `PDF`, el acento en `GIPROY`, y bloquea regresiones hacia tabla documental tipo Excel.

### Validacion

- `npm run build` OK.
- `node scripts\smoke-edo-edt-pdf-document-button.mjs` OK.
- `node scripts\smoke-classic-no-bim-contamination.mjs` OK.
- Busqueda focal de referencias BIM en archivos tocados OK, sin coincidencias.

### No interferencia BIM

Sin rutas BIM, UX BIM, imports BIM, modelos BIM ni acoplamientos hacia BIM.
