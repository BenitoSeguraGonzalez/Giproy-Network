## TASK-1642 - Etiqueta Nivel en card de profundidad EDO/EDT

**Fecha:** 2026-05-14  
**Modo:** GIPROY CLASICO  
**Area:** Frontend clasico / EDO y EDT grafico

### Objetivo

Cambiar solo el texto visible del card `Prof.` en las vistas graficas de EDO y EDT por `Nivel`, conservando el mismo valor numerico, tooltip y metrica de profundidad jerarquica.

### Alcance autorizado

- Solo capa clasica.
- Solo copy visual del renderer grafico jerarquico.
- Sin cambios en arboles EDO/EDT, calculos, handlers, persistencia, backend, API ni PostgreSQL.
- Sin mostrar ni activar UX BIM.

### Implementacion

- `HierarchyGraphView` usa `Nivel` como etiqueta visible de la metrica de profundidad cuando `moduleType === 'edt'` o `moduleType === 'edo'`.
- Los demas usos del renderer mantienen `Prof.`.
- El tooltip y el calculo de profundidad permanecen intactos.

### Validacion

- `npm run build` OK.
- `node scripts\smoke-classic-no-bim-contamination.mjs` OK.
- Busqueda focal confirma `Nivel` condicional para EDO/EDT y `Prof.` conservado para otros modulos.

### No interferencia BIM

Sin rutas BIM, UX BIM, imports BIM, modelos BIM ni acoplamientos hacia BIM.
