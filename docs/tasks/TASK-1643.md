## TASK-1643 - Botones neutros e iconografia de exportacion en reportes

**Fecha:** 2026-05-14  
**Modo:** GIPROY CLASICO  
**Area:** Frontend clasico / reporting comun

### Objetivo

Ajustar solo la presentacion visual de los botones inferiores del visor comun de reportes para retirar el acento naranja y usar iconos mas representativos, manteniendo tamano, labels, orden y handlers.

### Alcance autorizado

- Solo capa clasica.
- Solo botones inferiores del visor comun de reportes.
- Sin cambios en nombres visibles: `GIPROY`, `Excel`, `PDF`.
- Sin cambios en handlers, formatos, exportadores, backend, API, plantillas, persistencia ni PostgreSQL.
- Sin mostrar ni activar UX BIM.

### Implementacion

- `CommonReportPreviewModal` elimina la variante visual naranja de los botones de exportacion.
- Los tres botones usan la misma piel soft/neutra existente, sin aumentar altura ni padding.
- `GIPROY` usa el icono de la aplicacion (`LogoSoft.png`) como icono de accion.
- `Excel` mantiene icono de hoja de calculo y `PDF` mantiene icono documental.
- El smoke de EDO/EDT documental se actualiza para bloquear el regreso de `variant="accent"` y del fondo naranja en estos botones.

### Validacion

- `npm run build` OK.
- `node scripts\smoke-edo-edt-pdf-document-button.mjs` OK.
- `node scripts\smoke-classic-no-bim-contamination.mjs` OK.
- Busqueda focal confirma `LogoSoft.png` como icono de `GIPROY` y ausencia de variante naranja en el componente fuente.

### No interferencia BIM

Sin rutas BIM, UX BIM, imports BIM, modelos BIM ni acoplamientos hacia BIM.
