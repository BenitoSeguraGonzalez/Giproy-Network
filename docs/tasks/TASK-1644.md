## TASK-1644 - Boton GIPROY solo icono en reportes clasicos

**Fecha:** 2026-05-14  
**Modo:** GIPROY CLASICO  
**Area:** Frontend clasico / reporting comun

### Objetivo

Ajustar solo la presentacion visual del boton inferior `GIPROY` en el visor comun de reportes, retirando el texto visible y dejando el icono de la aplicacion con mayor presencia.

### Alcance autorizado

- Solo capa clasica.
- Solo boton inferior `GIPROY` del visor comun de reportes.
- Sin cambios en orden de acciones, handlers, formatos exportados, backend, API, plantillas, persistencia ni PostgreSQL.
- Sin mostrar ni activar UX BIM.

### Implementacion

- `CommonReportPreviewModal` permite personalizar tamano de icono y clase del label en `ReportExportButton`.
- El boton `GIPROY` mantiene `title`, `aria-label` y texto `GIPROY` como `sr-only`, pero retira el texto visible.
- El boton `GIPROY` conserva una caja equivalente a los otros botones de exportacion mediante ancho fijo `w-[5.25rem]`.
- El icono `LogoSoft.png` del boton `GIPROY` usa altura `h-6` y ancho automatico para no comprimir el wordmark dentro de una caja cuadrada y darle peso visual equivalente.
- `Excel` y `PDF` conservan su texto visible e iconografia actual.
- El smoke documental de EDO/EDT valida que el boton `GIPROY` use logo con altura normal, ancho automatico, label oculto y caja de boton estable.

### Validacion

- `npm run build` OK.
- `node scripts\smoke-edo-edt-pdf-document-button.mjs` OK.
- `node scripts\smoke-classic-no-bim-contamination.mjs` OK.
- Busqueda focal sin referencias BIM en archivos tocados.

### No interferencia BIM

Sin rutas BIM, UX BIM, imports BIM, modelos BIM ni acoplamientos hacia BIM.
