# TASK-1704 - Validador reproducible de baseline enterprise

Fecha: 2026-05-21  
Modo: GIPROY CLASICO  
Tipo: Tooling IA / validacion segura  
Estado: completada

## Objetivo

Automatizar el gate de validacion base para futuras sesiones, evitando repetir manualmente compilacion backend, import FastAPI y parseo de snapshots.

## Alcance autorizado

- Agregar script de soporte en `tools/ai_tools`.
- No tocar backend/frontend productivo.
- No ejecutar migraciones.
- No activar ni modificar BIM.

## Cambios realizados

- Se agrega `tools/ai_tools/validate_enterprise_baseline.py`.
- El script valida:
  - JSON documental clave.
  - `compileall` de `backend/app`.
  - import de `app.main`.
  - opcionalmente `npm run build` y smoke anti-BIM con `--include-frontend`.
- Se endurece la resolucion de `node`/`npm` para Windows usando `PATH`, variables `NODE_CMD`/`NPM_CMD` o rutas conocidas de Node.js.
- Se actualiza `docs/CHANGELOG.md`.
- Se actualiza `docs/runtime/WORK_MODE_STATE.json`.

## Impacto sobre GiProy Clasico

Sin impacto runtime. El cambio mejora la repetibilidad del proceso de validacion antes de cualquier micro-refactor.

## No interferencia BIM

No se toca codigo BIM ni UX BIM. El script solo puede ejecutar el smoke anti-BIM existente cuando se invoca con `--include-frontend`.

## Validaciones

- `py_compile` del script nuevo.
- Ejecucion del script en modo backend/documental.
- Ejecucion del script con `--include-frontend`.
- Parse JSON de `docs/runtime/WORK_MODE_STATE.json`.

## Rollback

Eliminar el script y revertir la documentacion de esta TASK. No hay rollback de DB ni de codigo funcional.
