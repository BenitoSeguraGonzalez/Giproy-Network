# TASK-1705 - Logging conservador en endpoints clasicos

Fecha: 2026-05-21  
Modo: GIPROY CLASICO  
Tipo: Micro-refactor backend seguro  
Estado: completada

## Objetivo

Iniciar la Fase 4 con un ajuste de codigo minimo y reversible: sustituir `print()` diagnosticos en endpoints clasicos por logging estandar, sin alterar contratos ni comportamiento funcional.

## Alcance autorizado

- `backend/app/api/endpoints/auth.py`
- `backend/app/api/endpoints/polinomica.py`

## Cambios realizados

- `auth.py`: el log de intento de login pasa de `print()` a `logger.info(...)`.
- `polinomica.py`: el log de regeneracion de formula pasa de `print()` a `logger.info(...)`.

## Cambios no realizados

- No se toca logica de autenticacion.
- No se toca JWT, passwords, sesiones ni tenant.
- No se toca calculo de formula polinomica.
- No se modifican rutas, responses ni schemas.
- No se toca BIM.

## Impacto sobre GiProy Clasico

Impacto funcional nulo esperado. Solo cambia el canal de salida diagnostica desde stdout directo a logging estandar de Python.

## No interferencia BIM

Sin cambios en codigo BIM, UX BIM, feature flags BIM ni dependencias hacia BIM.

## Validaciones

- `py_compile` focal de archivos tocados.
- Import de `app.main` mediante validador baseline.
- Build frontend y smoke anti-BIM mediante validador baseline completo.

## Rollback

Revertir las dos sustituciones de logging por `print()` si fuera necesario. No hay rollback DB ni migraciones.
