# TASK-1706 - Logging conservador en bases y proyecto

Fecha: 2026-05-21  
Modo: GIPROY CLASICO  
Tipo: Micro-refactor backend seguro  
Estado: completada

## Objetivo

Continuar la limpieza incremental de `print()` diagnosticos en backend clasico, sustituyendolos por logging estandar sin cambiar comportamiento funcional.

## Alcance autorizado

- `backend/app/api/endpoints/bases_trabajo.py`
- `backend/app/repositories/base_trabajo.py`
- `backend/app/services/proyecto.py`

## Cambios realizados

- `bases_trabajo.py`: mensajes de fallback/captura de destino pasan a `logger.warning/debug`.
- `base_trabajo.py`: mensaje de clonacion de base pasa a `logger.debug`.
- `proyecto.py`: errores no bloqueantes al inicializar/asegurar presupuesto pasan a `logger.warning`.

## Cambios no realizados

- No se toca logica de clonacion.
- No se toca tenant, empresa activa ni permisos.
- No se modifica la creacion de proyectos/revisiones.
- No se modifica presupuesto, EDT, APUs ni cronogramas.
- No se toca BIM.
- Se conservan `print()` en `email_utils.py`, `seed_licenses.py`, `debug_logger.py` y `session_trace.py` porque pueden ser salida esperada de desarrollo/script o fallback de emergencia si falla el propio sistema de trazas.

## Impacto sobre GiProy Clasico

Impacto funcional nulo esperado. Solo cambia el canal de salida diagnostica.

## No interferencia BIM

Sin cambios en codigo BIM, UX BIM, feature flags BIM ni dependencias nuevas hacia BIM.

## Validaciones

- `py_compile` focal de archivos tocados.
- Validador baseline completo con frontend y smoke anti-BIM.

## Rollback

Revertir las sustituciones de logging por `print()` si fuera necesario. No hay rollback DB ni migraciones.
