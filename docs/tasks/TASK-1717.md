# TASK-1717 - Cobertura ampliada de helpers puros core

Fecha: 2026-05-21  
Modo: GIPROY CLASICO  
Tipo: Fase 4 / backend helper puro  
Estado: completada

## Objetivo

Ampliar la cobertura focal de helpers puros antes de cualquier refactor real sobre normalizacion compartida.

## Alcance autorizado

- `backend/app/tests/test_core_text_and_unit_helpers.py`
- `docs/tasks/TASK-1717.md`
- `docs/CHANGELOG.md`
- `docs/runtime/WORK_MODE_STATE.json`

## Cambios realizados

- Se agregan pruebas para `app.core.utils.normalize_string`.
- Se agregan pruebas para `resolve_country_phone_prefix`, `is_valid_phone` y `normalize_phone`.
- Se corrige `app.core.utils.normalize_string` para colapsar espacios internos, alineando implementacion con su docstring.
- Se documenta que la duplicacion detectada en `services/apu.py` no se mueve todavia por estar dentro de un servicio critico.

## Cambios no realizados

- No se modifica frontend.
- No se toca DB, auth, tenant, licencias, sesiones, permisos, presupuestos, EDT, cronogramas ni BIM.
- No se unifica la normalizacion de `services/apu.py` en este slice.

## Impacto sobre GiProy Clasico

Impacto funcional esperado bajo y positivo: `normalize_string` ahora cumple su documentacion y colapsa espacios internos en normalizacion textual.

## No interferencia BIM

Sin cambios en codigo BIM, UX BIM, feature flags BIM ni dependencias nuevas hacia BIM.

## Validaciones

- Pytest focal del archivo de helpers.
- `py_compile` de `backend/app/core/utils.py`.
- Validador baseline completo con frontend y smoke anti-BIM.

## Rollback

Revertir la linea de retorno de `backend/app/core/utils.py`, revertir las pruebas agregadas, retirar esta TASK y revertir entradas de `docs/CHANGELOG.md` y `docs/runtime/WORK_MODE_STATE.json`.
