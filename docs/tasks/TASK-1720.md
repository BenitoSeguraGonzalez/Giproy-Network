# TASK-1720 - Cobertura focal de politica de calculo

Fecha: 2026-05-21  
Modo: GIPROY CLASICO  
Tipo: Fase 4 / backend helper puro  
Estado: completada

## Objetivo

Ampliar la cobertura focal de `app.core.calculation_policy` para congelar contratos de conversion Decimal, redondeo operativo de calculo y defaults de subtotales APU.

## Alcance autorizado

- `backend/app/tests/test_calculation_policy.py`
- `docs/tasks/TASK-1720.md`
- `docs/CHANGELOG.md`
- `docs/runtime/WORK_MODE_STATE.json`
- `docs/SAFE_REFACTOR_PROGRESS.md`

## Cambios realizados

- Se cubre `as_decimal` con `None`, cadena vacia, default custom y cadenas decimales.
- Se cubre `round_operational_calc` con precision operativa variable.
- Se cubre default conservador de `rendimiento=None` como `1` en `calculate_apu_line_subtotal`.
- Se actualiza progreso de Fase 4 a 22%.

## Cambios no realizados

- No se modifica codigo backend productivo.
- No se modifica frontend.
- No se modifica DB, auth, tenant, licencias, sesiones, permisos, presupuestos, EDT, cronogramas ni BIM.

## Impacto sobre GiProy Clasico

Impacto runtime nulo. Se agrega cobertura sobre la politica numerica central usada por presupuestos, APUs y cronogramas.

## No interferencia BIM

Sin cambios en codigo BIM, UX BIM, feature flags BIM ni dependencias nuevas hacia BIM.

## Validaciones

- Pytest focal de `test_calculation_policy.py`.
- Pytest focal de helpers core recientes.
- Validador baseline completo con frontend y smoke anti-BIM.

## Rollback

Revertir los tests agregados en `test_calculation_policy.py`, revertir `docs/SAFE_REFACTOR_PROGRESS.md`, retirar esta TASK y revertir entradas de `docs/CHANGELOG.md` y `docs/runtime/WORK_MODE_STATE.json`.
