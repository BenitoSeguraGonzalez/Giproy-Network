# TASK-1749 - Recuperacion segura de archivos locales desde copia pre-refactor

Fecha: 2026-05-22  
Modo: GIPROY CLASICO  
Tipo: Incidencia runtime / dependencias locales  
Estado: completada

## Objetivo

Restaurar las referencias locales persistidas en BD que apuntaban a archivos bajo `backend/uploads` y que estaban ausentes en el entorno activo, sin modificar codigo productivo ni contratos frontend/backend.

## Alcance autorizado

- Restauracion no destructiva de archivos faltantes desde `e:\Repositorios\GiProy Network\GiProy Network\backend\uploads`.
- Validacion de referencias locales en BD contra filesystem.
- Validacion HTTP de recursos estaticos servidos por backend.
- Ajuste de `.gitignore` para evitar que uploads runtime y la copia local pre-refactor entren al control de versiones.

## Cambios realizados

- Se copiaron solo archivos inexistentes hacia `backend/uploads`.
- Se recuperaron logos de empresas, adjuntos de Comunidad, imagenes de Proyecto y documentos PDF referenciados por BD.
- Se verifico que las 12 referencias locales detectadas en BD existen en disco.
- Se verifico respuesta `200` para recursos servidos por `http://localhost:3001/uploads/...`.
- Se excluyeron `uploads/`, `backend/uploads/` y `GiProy Network/` en `.gitignore`.

## No interferencia BIM

- No se modifica codigo BIM.
- No se activa UX BIM.
- No se agregan imports ni dependencias BIM.
- El cambio queda limitado a restauracion de archivos runtime clasicos y proteccion Git.

## Validacion

- Cruce BD vs filesystem: `OK 12`, `MISSING 0`.
- Logos de empresa: `200 image/jpeg`.
- Adjuntos de Comunidad: `200 image/svg+xml`.
- Imagen georef de Proyecto: `200 image/png`.
- Proxy API de imagen de Proyecto: `200 image/png`.

## Rollback

Eliminar de `backend/uploads` los archivos restaurados desde la copia pre-refactor y revertir esta TASK, la entrada de `CHANGELOG` y el ajuste de `.gitignore`.
