# TASK-1752 - Correccion focal de deteccion de links ofuscados Comunidad

Fecha: 2026-05-22  
Modo: GIPROY CLASICO  
Tipo: Fase 4 / backend helper puro  
Estado: completada

## Objetivo

Corregir una regresion detectada durante la validacion ampliada de Comunidad: `_detect_first_link` devolvia solo `www...` cuando el texto incluia un protocolo ofuscado previo como `hpps:\\`.

## Alcance autorizado

- Helper puro: `backend/app/api/endpoints/community.py`.
- Cobertura existente: `backend/app/tests/test_community_permissions.py`.
- Documentacion de avance: `docs/SAFE_REFACTOR_PROGRESS.md`, `docs/CHANGELOG.md`, `docs/runtime/WORK_MODE_STATE.json`.

## Cambios realizados

- Se normaliza el texto antes de decidir entre link directo, protocolo ofuscado y dominio ofuscado.
- Si el link directo detectado empieza en `www.` pero existe un protocolo ofuscado anterior, se conserva el fragmento completo normalizado.
- Se mantiene el comportamiento para links directos `http(s)://` y dominios ofuscados.
- Se actualiza progreso de Fase 4 a 82%.

## No interferencia BIM

- No se modifica codigo BIM.
- No se activa UX BIM.
- No se agregan imports ni dependencias BIM.
- El cambio queda limitado a helper puro de Comunidad.

## Validacion

- Suite completa `backend/app/tests/test_community_permissions.py`: 61 passed.
- Baseline enterprise con frontend incluido.
- Smoke anti-BIM incluido en baseline.

## Rollback

Revertir el bloque de `_detect_first_link`, retirar esta TASK y revertir las entradas de `CHANGELOG`, `SAFE_REFACTOR_PROGRESS` y `WORK_MODE_STATE`.
