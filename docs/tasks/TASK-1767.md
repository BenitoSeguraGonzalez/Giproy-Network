# TASK-1767 - Cliente global tenantless para paises

Fecha: 2026-05-24

Modo: GIPROY CLASICO

## Objetivo

Aplicar el primer micro-slice real de Fase 5 concentrando la lectura global de paises en un cliente API existente, sin alterar UI ni comportamiento.

## Alcance

- Agregar `maestrosApi.getPaises()`.
- Migrar lecturas simples de paises en `RegisterModal`.
- Migrar lecturas simples de paises en `BasesTrabajo`.
- Regenerar inventario frontend reproducible.

## Cambios realizados

- `frontend/src/api/maestros.js`: nuevo metodo tenantless `getPaises`.
- `frontend/src/components/RegisterModal.jsx`: usa `maestrosApi.getPaises`.
- `frontend/src/pages/BasesTrabajo.jsx`: usa `maestrosApi.getPaises`.
- `docs/frontend_dependency_inventory.json`: actualizado.
- Documentacion de progreso, CHANGELOG y runtime actualizados.

## Decisiones conservadoras

- No se modifico UI.
- No se cambiaron rutas.
- No se cambiaron payloads ni contratos API.
- No se tocaron `Settings`, `Community`, `MarketplaceAdminDashboard`, `AuthContext`, Gantt ni Presupuestos.
- No se modifico BIM.

## Resultado

- Llamadas directas fuera de `frontend/src/api`: 61 -> 59.

## Validacion

- `pytest backend/app/tests/test_community_permissions.py`: OK.
- `py_compile` de tooling: OK.
- Baseline enterprise con frontend incluido: OK.

## Rollback

Revertir los tres cambios frontend y regenerar `docs/frontend_dependency_inventory.json`. No hay migraciones DB ni cambios de datos.
