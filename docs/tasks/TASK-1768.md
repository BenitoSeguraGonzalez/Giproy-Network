# TASK-1768 - Cliente admin tenantless para catalogo de licencias

Fecha: 2026-05-24

Modo: GIPROY CLASICO

## Objetivo

Reducir llamadas API directas en frontend concentrando la lectura de catalogo de licencias dentro del cliente admin existente.

## Alcance

- Agregar `adminLicensesApi.getCatalog()`.
- Migrar la lectura de `/admin-licenses/catalog` en `AdminGlobalLicencias`.
- Regenerar inventario frontend reproducible.

## Cambios realizados

- `frontend/src/api/adminLicenses.js`: nuevo metodo tenantless `getCatalog`.
- `frontend/src/pages/AdminGlobalLicencias.jsx`: usa `adminLicensesApi.getCatalog`.
- `docs/frontend_dependency_inventory.json`: actualizado.
- Documentacion de progreso, CHANGELOG y runtime actualizados.

## Decisiones conservadoras

- No se modifico UI.
- No se cambiaron rutas.
- No se cambiaron payloads ni contratos API.
- No se tocaron `Settings`, `ProjectManager`, `AuthContext`, Gantt, Presupuestos, Community ni Marketplace.
- No se modifico BIM.

## Resultado

- La muestra directa de `AdminGlobalLicencias` hacia `/admin-licenses/catalog` desaparece del inventario.
- El conteo heuristico global queda en 59 por otras llamadas fuera de clientes.

## Validacion

- `npm run build`: OK.
- `smoke-classic-no-bim-contamination`: OK.

## Rollback

Revertir los dos cambios frontend y regenerar `docs/frontend_dependency_inventory.json`. No hay migraciones DB ni cambios de datos.
