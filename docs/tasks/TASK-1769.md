# TASK-1769 - Cliente admin global tenantless para gobernanza

Fecha: 2026-05-24

Modo: GIPROY CLASICO

## Objetivo

Reducir llamadas API directas en frontend concentrando lecturas globales de gobernanza dentro de un cliente admin especifico.

## Alcance

- Crear `adminGlobalApi`.
- Migrar lecturas de empresas y usuarios en `AdminGlobalGobernanza`.
- Regenerar inventario frontend reproducible.

## Cambios realizados

- `frontend/src/api/adminGlobal.js`: nuevo cliente tenantless con `getEmpresas` y `getUsuarios`.
- `frontend/src/pages/AdminGlobalGobernanza.jsx`: usa `adminGlobalApi`.
- `docs/frontend_dependency_inventory.json`: actualizado.
- Documentacion de progreso, CHANGELOG y runtime actualizados.

## Decisiones conservadoras

- No se modifico UI.
- No se cambiaron rutas.
- No se cambiaron payloads ni contratos API.
- No se tocaron `Settings`, `ProjectManager`, `AuthContext`, Gantt, Presupuestos, Community ni Marketplace.
- No se modifico BIM.

## Resultado

- Llamadas directas fuera de `frontend/src/api`: 59 -> 58.

## Validacion

- `npm run build`: OK.
- `smoke-classic-no-bim-contamination`: OK.

## Rollback

Revertir el cliente nuevo, restaurar las llamadas previas en `AdminGlobalGobernanza` y regenerar `docs/frontend_dependency_inventory.json`. No hay migraciones DB ni cambios de datos.
