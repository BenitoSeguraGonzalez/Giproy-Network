# TASK-1771 - Reutilizacion adminGlobalApi en layout clasico

Fecha: 2026-05-24

Modo: GIPROY CLASICO

## Objetivo

Reutilizar el cliente admin global tenantless en el layout clasico para reducir una llamada directa desde la shell protegida.

## Alcance

- Migrar `AppLayout` a `adminGlobalApi.getEmpresas()`.
- Regenerar inventario frontend reproducible.

## Cambios realizados

- `frontend/src/layouts/AppLayout.jsx`: usa `adminGlobalApi`.
- `docs/frontend_dependency_inventory.json`: actualizado.
- Documentacion de progreso, CHANGELOG y runtime actualizados.

## Decisiones conservadoras

- No se modifico UI.
- No se cambiaron rutas.
- No se cambiaron payloads ni contratos API.
- No se tocaron `Settings`, `ProjectManager`, `AuthContext`, Gantt, Presupuestos, Community ni Marketplace operativo.
- No se modifico BIM.

## Resultado

- Llamadas directas fuera de `frontend/src/api`: 54 -> 53.

## Validacion

- `npm run build`: OK.
- `smoke-classic-no-bim-contamination`: OK.
- Baseline enterprise con frontend incluido: OK.

## Rollback

Restaurar la lectura directa previa en `AppLayout` y regenerar `docs/frontend_dependency_inventory.json`. No hay migraciones DB ni cambios de datos.
