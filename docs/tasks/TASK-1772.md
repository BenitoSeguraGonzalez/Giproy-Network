# TASK-1772 - Cliente publico auth para recuperacion de password

Fecha: 2026-05-24

Modo: GIPROY CLASICO

## Objetivo

Concentrar las llamadas publicas de recuperacion y reset de password en un cliente API frontend, eliminando URLs absolutas locales.

## Alcance

- Crear `frontend/src/api/publicAuth.js`.
- Migrar `ForgotPassword` y `ResetPassword` al cliente publico.
- Regenerar inventario frontend reproducible.

## Cambios realizados

- `frontend/src/api/publicAuth.js`: agrega `requestPasswordRecovery` y `resetPassword`.
- `frontend/src/pages/ForgotPassword.jsx`: deja de usar `axios` directo y URL hardcoded a localhost.
- `frontend/src/pages/ResetPassword.jsx`: deja de usar `axios` directo y URL hardcoded a localhost.
- `frontend/src/pages/ResetPassword.jsx`: importa `motion`, ya usado por la vista.
- `docs/frontend_dependency_inventory.json`: actualizado.
- Documentacion de progreso, CHANGELOG y runtime actualizados.

## Decisiones conservadoras

- No se modifico `AuthContext`.
- No se modifico login, refresh, logout ni sesiones.
- No se cambiaron payloads backend.
- No se tocaron rutas visibles ni UI.
- No se modifico DB, tenant, licencias, ERP funcional ni BIM.

## Resultado

- Llamadas directas fuera de `frontend/src/api`: 53 -> 51.
- Se elimina dependencia de `http://localhost:8000` en recuperacion/reset.

## Validacion

- `npm run build`: OK.
- `smoke-classic-no-bim-contamination`: OK.
- Baseline enterprise con frontend incluido: OK.

## Rollback

Restaurar las llamadas directas previas en `ForgotPassword` y `ResetPassword`, eliminar `frontend/src/api/publicAuth.js` y regenerar `docs/frontend_dependency_inventory.json`. No hay migraciones DB ni cambios de datos.
