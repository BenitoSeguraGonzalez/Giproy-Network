# TASK-1773 - Consulta publica de cuentas por email en publicAuthApi

Fecha: 2026-05-24

Modo: GIPROY CLASICO

## Objetivo

Concentrar la consulta publica de cuentas por email del login en el cliente `publicAuthApi`, sin modificar autenticacion principal.

## Alcance

- Agregar `publicAuthApi.getAccountsByEmail()`.
- Migrar la llamada directa de `Login` a ese cliente.
- Regenerar inventario frontend reproducible.

## Cambios realizados

- `frontend/src/api/publicAuth.js`: agrega `getAccountsByEmail`.
- `frontend/src/pages/Login.jsx`: usa `publicAuthApi.getAccountsByEmail()` para el paso de seleccion de cuenta.
- `docs/frontend_dependency_inventory.json`: actualizado.
- Documentacion de progreso, CHANGELOG y runtime actualizados.

## Decisiones conservadoras

- No se modifico `AuthContext`.
- No se modifico login con password, refresh, logout ni persistencia de token.
- No se cambiaron payloads backend.
- No se tocaron rutas visibles ni UI.
- No se modifico DB, licencias, ERP funcional, tenant global ni BIM.

## Resultado

- Llamadas directas fuera de `frontend/src/api`: 51 -> 50.
- La consulta `/auth/accounts-by-email` queda marcada como tenantless desde cliente publico.

## Validacion

- `npm run build`: OK.
- `smoke-classic-no-bim-contamination`: OK.
- Baseline enterprise con frontend incluido: OK.

## Rollback

Restaurar la llamada directa previa en `Login`, retirar `getAccountsByEmail` de `publicAuthApi` y regenerar `docs/frontend_dependency_inventory.json`. No hay migraciones DB ni cambios de datos.
