# TASK-1774 - Registro publico en publicAuthApi

Fecha: 2026-05-24

Modo: GIPROY CLASICO

## Objetivo

Concentrar el registro publico de cuenta en `publicAuthApi`, manteniendo intacto el flujo visual y la entrega de token posterior al registro.

## Alcance

- Agregar `publicAuthApi.register()`.
- Migrar `RegisterModal` para usar el cliente publico.
- Regenerar inventario frontend reproducible.

## Cambios realizados

- `frontend/src/api/publicAuth.js`: agrega `register`.
- `frontend/src/components/RegisterModal.jsx`: usa `publicAuthApi.register()` y conserva `onRegisterSuccess(access_token)`.
- `docs/frontend_dependency_inventory.json`: actualizado.
- Documentacion de progreso, CHANGELOG y runtime actualizados.

## Decisiones conservadoras

- No se modifico el contrato del payload de registro.
- No se modifico `AuthContext`.
- No se modifico login, refresh, logout ni sesiones.
- No se cambiaron rutas visibles ni UI.
- No se modifico DB, tenant global, licencias, ERP funcional ni BIM.

## Resultado

- Llamadas directas fuera de `frontend/src/api`: 50 -> 49.
- `RegisterModal` deja de depender de llamada directa a `api.post('/register')`.

## Validacion

- `npm run build`: OK.
- `smoke-classic-no-bim-contamination`: OK.
- Baseline enterprise con frontend incluido: OK.

## Rollback

Restaurar la llamada directa previa en `RegisterModal`, retirar `register` de `publicAuthApi` y regenerar `docs/frontend_dependency_inventory.json`. No hay migraciones DB ni cambios de datos.
