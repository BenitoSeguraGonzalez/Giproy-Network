# TASK-1775 - Transporte API para trazas de sesion

Fecha: 2026-05-24

Modo: GIPROY CLASICO

## Objetivo

Mover el envio de trazas de sesion a un transport bajo `frontend/src/api`, preservando el comportamiento no bloqueante del flujo de autenticacion.

## Alcance

- Crear `frontend/src/api/sessionTrace.js`.
- Reutilizar el transport desde `frontend/src/utils/sessionTrace.js`.
- Regenerar inventario frontend reproducible.

## Cambios realizados

- `frontend/src/api/sessionTrace.js`: agrega `sendSessionTrace`.
- `frontend/src/utils/sessionTrace.js`: delega el envio en `sendSessionTrace`.
- `docs/frontend_dependency_inventory.json`: actualizado.
- Documentacion de progreso, CHANGELOG y runtime actualizados.

## Decisiones conservadoras

- No se modifico `AuthContext`.
- No se modificaron login, refresh, logout, expiracion ni tokens.
- Se conserva `keepalive: true`.
- Se conserva captura silenciosa para que el trazado no interfiera con el flujo principal.
- No se modifico DB, tenant, licencias, ERP funcional ni BIM.

## Resultado

- Llamadas directas fuera de `frontend/src/api`: 49 -> 48.

## Validacion

- `npm run build`: OK.
- `smoke-classic-no-bim-contamination`: OK.
- Baseline enterprise con frontend incluido: OK.

## Rollback

Restaurar el `fetch` directo previo en `frontend/src/utils/sessionTrace.js`, eliminar `frontend/src/api/sessionTrace.js` y regenerar `docs/frontend_dependency_inventory.json`. No hay migraciones DB ni cambios de datos.
