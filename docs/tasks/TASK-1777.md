# TASK-1777 - Lecturas globales de paises en Community y Marketplace

Fecha: 2026-05-24

Modo: GIPROY CLASICO

## Objetivo

Reutilizar `maestrosApi.getPaises()` en lecturas globales de paises dentro de pantallas grandes, sin tocar su logica funcional.

## Alcance

- Migrar lectura de paises en `Community`.
- Migrar lectura de paises en `MarketplaceAdminDashboard`.
- Regenerar inventario frontend reproducible.

## Cambios realizados

- `frontend/src/pages/Community.jsx`: usa `maestrosApi.getPaises()` para cargar paises del formulario.
- `frontend/src/pages/MarketplaceAdminDashboard.jsx`: usa `maestrosApi.getPaises()` para el modal de importacion portal.
- `docs/frontend_dependency_inventory.json`: actualizado.
- Documentacion de progreso, CHANGELOG y runtime actualizados.

## Decisiones conservadoras

- No se modifico logica de Community.
- No se modifico logica de Marketplace.
- No se modificaron payloads, rutas visibles, permisos, publicaciones, productos ni importadores.
- No se modifico backend, DB, auth, tenant global ni BIM.

## Resultado

- Llamadas directas fuera de `frontend/src/api`: 40 -> 38.

## Validacion

- `npm run build`: OK.
- `smoke-classic-no-bim-contamination`: OK.
- Baseline enterprise con frontend incluido: OK.

## Rollback

Restaurar las llamadas directas previas a `/paises/` en ambas pantallas y regenerar `docs/frontend_dependency_inventory.json`. No hay migraciones DB ni cambios de datos.
