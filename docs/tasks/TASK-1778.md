# TASK-1778 - Lectura global de paises en Settings

Fecha: 2026-05-24

Modo: GIPROY CLASICO

## Objetivo

Reutilizar `maestrosApi.getPaises()` en `Settings` para eliminar una llamada directa de lectura global sin tocar administracion sensible.

## Alcance

- Migrar solo la carga de paises en `Settings`.
- Regenerar inventario frontend reproducible.

## Cambios realizados

- `frontend/src/pages/Settings.jsx`: usa `maestrosApi.getPaises()` en `fetchPaises`.
- `docs/frontend_dependency_inventory.json`: actualizado.
- Documentacion de progreso, CHANGELOG y runtime actualizados.

## Decisiones conservadoras

- No se modificaron empresas.
- No se modificaron usuarios.
- No se modificaron logos, plantillas, proyectos ni licencias.
- No se modificaron payloads, rutas visibles, backend, DB, auth, tenant global ni BIM.

## Resultado

- Llamadas directas fuera de `frontend/src/api`: 38 -> 37.

## Validacion

- `npm run build`: OK.
- `smoke-classic-no-bim-contamination`: OK.
- Baseline enterprise con frontend incluido: OK.

## Rollback

Restaurar la llamada directa previa a `/paises/` en `Settings` y regenerar `docs/frontend_dependency_inventory.json`. No hay migraciones DB ni cambios de datos.
