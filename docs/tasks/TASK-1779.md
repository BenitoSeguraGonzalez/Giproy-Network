# TASK-1779 - Cliente API para ProjectManager clasico

Fecha: 2026-05-24

Modo: GIPROY CLASICO

## Objetivo

Reducir llamadas directas desde `ProjectManager` hacia endpoints de proyectos, bases de trabajo, usuarios y EDT, sin cambiar comportamiento funcional ni contratos backend.

## Alcance

- Se agregan metodos de asignacion a `proyectosApi`.
- Se agregan metodos de asignacion a `basesTrabajoApi`.
- Se crea `usuariosApi.getAll()` para lectura tenant-scoped de usuarios.
- `ProjectManager` deja de importar `axiosConfig` directamente y consume clientes de dominio.

## Fuera de alcance

- No se modifica UI.
- No se cambian endpoints, rutas ni payloads.
- No se cambia autenticacion.
- No se cambia multi-tenant global.
- No se modifica EDT, permisos, dashboard de equipo ni logica de asignacion.
- No se toca BIM.

## Validacion

- `tools/ai_tools/frontend_dependency_inventory.py`: OK.
- `npm run build`: OK.
- `frontend/scripts/smoke-classic-no-bim-contamination.mjs`: OK.
- `validate_enterprise_baseline.py --include-frontend`: OK.

## Resultado

El inventario reproducible baja de 37 a 30 llamadas directas fuera de `frontend/src/api`.

## Rollback

Revertir:

- `frontend/src/pages/ProjectManager.jsx`
- `frontend/src/api/proyectos.js`
- `frontend/src/api/basesTrabajo.js`
- `frontend/src/api/usuarios.js`
- `docs/frontend_dependency_inventory.json`
- entradas documentales de TASK/CHANGELOG/progreso/runtime.

