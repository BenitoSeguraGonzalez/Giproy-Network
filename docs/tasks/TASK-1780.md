# TASK-1780 - Cliente externo de geocodificacion clasica

Fecha: 2026-05-24

Modo: GIPROY CLASICO

## Objetivo

Centralizar las llamadas externas de geocodificacion de `DatosProyecto` sin cambiar la logica de seleccion, fallback, mensajes ni parseo.

## Alcance

- Se crea `frontend/src/api/geocoding.js`.
- `DatosProyecto` consume `geocodingApi.queryOverpass()` para Overpass.
- `DatosProyecto` consume `geocodingApi.searchNominatim()` para Nominatim.

## Fuera de alcance

- No se cambia backend.
- No se cambia DB.
- No se cambia tenant.
- No se cambia autenticacion.
- No se modifica logica de geocodificacion ni mapa.
- No se toca BIM.

## Validacion

- `tools/ai_tools/frontend_dependency_inventory.py`: OK.
- `npm run build`: OK.
- `frontend/scripts/smoke-classic-no-bim-contamination.mjs`: OK.
- `validate_enterprise_baseline.py --include-frontend`: OK.

## Resultado

El inventario reproducible baja de 30 a 28 llamadas directas fuera de `frontend/src/api`.

## Rollback

Revertir:

- `frontend/src/api/geocoding.js`
- `frontend/src/components/projects/DatosProyecto.jsx`
- `docs/frontend_dependency_inventory.json`
- entradas documentales de TASK/CHANGELOG/progreso/runtime.

