# TASK-1781 - Alta de usuario Comunidad via usuariosApi

Fecha: 2026-05-24

Modo: GIPROY CLASICO

## Objetivo

Eliminar la ultima llamada directa `axiosConfig` desde `Community` para el alta de usuarios de comunidad, manteniendo el mismo endpoint, payload y contexto de empresa.

## Alcance

- `usuariosApi` agrega `create(payload, params)`.
- `Community` usa `usuariosApi.create()` en `handleCreateCommunityUser`.
- Se elimina el import directo de `axiosConfig` en `Community`.

## Fuera de alcance

- No se cambia formulario de Comunidad.
- No se cambia rol asignado `usuario_comunidad`.
- No se cambia validacion de empresa activa.
- No se cambia backend, DB, auth ni tenant global.
- No se toca BIM.

## Validacion

- `tools/ai_tools/frontend_dependency_inventory.py`: OK.
- `npm run build`: OK.
- `frontend/scripts/smoke-classic-no-bim-contamination.mjs`: OK.
- `validate_enterprise_baseline.py --include-frontend`: OK.

## Resultado

El inventario reproducible baja de 28 a 27 llamadas directas fuera de `frontend/src/api`.

## Rollback

Revertir:

- `frontend/src/api/usuarios.js`
- `frontend/src/pages/Community.jsx`
- `docs/frontend_dependency_inventory.json`
- entradas documentales de TASK/CHANGELOG/progreso/runtime.

