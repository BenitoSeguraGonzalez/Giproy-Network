# TASK-1782 - Fixture local Gantt FF via cliente API

Fecha: 2026-05-24

Modo: GIPROY CLASICO

## Objetivo

Eliminar el `fetch` directo del harness Gantt FF sin tocar el Gantt productivo ni los scripts de validacion.

## Alcance

- Se crea `frontend/src/api/ganttFixtures.js`.
- `frontend/src/gantt-ff-harness.jsx` usa `ganttFixturesApi.getFfFixture()`.
- Se mantiene la misma ruta local `/gantt-ff-fixture.json`.

## Fuera de alcance

- No se modifica `CronogramaGantt`.
- No se cambian fixtures ni scripts Playwright.
- No se cambia backend, DB, auth, tenant ni BIM.

## Validacion

- `tools/ai_tools/frontend_dependency_inventory.py`: OK.
- `npm run build`: OK.
- `npm run smoke:gantt-classic`: OK.
- `validate_enterprise_baseline.py --include-frontend`: OK.

## Resultado

El inventario reproducible baja de 27 a 26 llamadas directas fuera de `frontend/src/api`.

## Rollback

Revertir:

- `frontend/src/api/ganttFixtures.js`
- `frontend/src/gantt-ff-harness.jsx`
- `docs/frontend_dependency_inventory.json`
- entradas documentales de TASK/CHANGELOG/progreso/runtime.

