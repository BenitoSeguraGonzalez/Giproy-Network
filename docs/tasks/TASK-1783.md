# TASK-1783 - Lecturas iniciales de Settings via clientes API

Fecha: 2026-05-24

Modo: GIPROY CLASICO

## Objetivo

Centralizar las lecturas iniciales de `Settings.fetchData` en clientes API de dominio sin tocar escrituras, logos, plantillas ni administracion destructiva.

## Alcance

- Se crea `frontend/src/api/empresas.js` con lecturas `getAll()` y `getById()`.
- `Settings.fetchData` usa `empresasApi` para leer empresas.
- `Settings.fetchData` usa `usuariosApi.getAll()` para leer usuarios.
- `Settings.fetchData` usa `proyectosApi.getAll()` para resolver el proyecto activo de plantillas.

## Fuera de alcance

- No se migran escrituras de empresas.
- No se migran escrituras de usuarios.
- No se migran uploads de logo.
- No se migran plantillas.
- No se cambia UI, auth, tenant global, backend, DB ni BIM.

## Validacion

- `tools/ai_tools/frontend_dependency_inventory.py`: OK.
- `npm run build`: OK.
- `frontend/scripts/smoke-classic-no-bim-contamination.mjs`: OK.
- `validate_enterprise_baseline.py --include-frontend`: OK.

## Resultado

El inventario reproducible baja de 26 a 21 llamadas directas fuera de `frontend/src/api`.

## Rollback

Revertir:

- `frontend/src/api/empresas.js`
- `frontend/src/pages/Settings.jsx`
- `docs/frontend_dependency_inventory.json`
- entradas documentales de TASK/CHANGELOG/progreso/runtime.

