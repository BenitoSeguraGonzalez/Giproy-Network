# TASK-1784 - Escrituras basicas de empresa en Settings via empresasApi

Fecha: 2026-05-25

Modo: GIPROY CLASICO

## Objetivo

Centralizar escrituras basicas de empresa en `Settings` sin tocar uploads de logo, plantillas ni flujos de autenticacion.

## Alcance

- `empresasApi` agrega `create`, `update` y `delete`.
- `Settings` usa `empresasApi.update()` para activar/desactivar empresas.
- `Settings` usa `empresasApi.update()` para preferencias de empresa.
- `Settings` usa `empresasApi.delete()` para eliminar empresa.
- `Settings` usa `empresasApi.create()` y `empresasApi.update()` para crear/editar datos base de empresa.

## Fuera de alcance

- No se migran uploads de logo.
- No se migran plantillas.
- No se migran usuarios.
- No se cambia UI.
- No se cambia backend, DB, auth, tenant global ni BIM.

## Validacion

- `tools/ai_tools/frontend_dependency_inventory.py`: OK.
- `npm run build`: OK.
- `validate_enterprise_baseline.py --include-frontend`: OK, incluyendo smoke no-BIM.

## Resultado

El inventario reproducible baja de 21 a 16 llamadas directas fuera de `frontend/src/api`.

## Rollback

Revertir:

- `frontend/src/api/empresas.js`
- `frontend/src/pages/Settings.jsx`
- `docs/frontend_dependency_inventory.json`
- entradas documentales de TASK/CHANGELOG/progreso/runtime.

