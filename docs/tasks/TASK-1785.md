# TASK-1785 - Escrituras de usuarios en Settings via usuariosApi

Fecha: 2026-05-25

Modo: GIPROY CLASICO

## Objetivo

Centralizar creacion, actualizacion y eliminacion de usuarios desde `Settings` en `usuariosApi`, preservando payloads y `empresa_id`.

## Alcance

- `usuariosApi` agrega `update(usuarioId, payload, params)`.
- `usuariosApi` agrega `delete(usuarioId, params)`.
- `Settings` usa `usuariosApi.create()`, `usuariosApi.update()` y `usuariosApi.delete()`.

## Fuera de alcance

- No se cambia formulario de personal.
- No se cambia rol, permisos ni validaciones.
- No se migran logos.
- No se migran plantillas.
- No se toca `AuthContext`.
- No se cambia backend, DB, tenant global ni BIM.

## Validacion

- `tools/ai_tools/frontend_dependency_inventory.py`: OK.
- `npm run build`: OK.
- `validate_enterprise_baseline.py --include-frontend`: OK, incluyendo smoke no-BIM.

## Resultado

El inventario reproducible baja de 16 a 13 llamadas directas fuera de `frontend/src/api`.

## Rollback

Revertir:

- `frontend/src/api/usuarios.js`
- `frontend/src/pages/Settings.jsx`
- `docs/frontend_dependency_inventory.json`
- entradas documentales de TASK/CHANGELOG/progreso/runtime.

