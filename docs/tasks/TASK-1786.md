# TASK-1786 - Settings sin llamadas API directas

Fecha: 2026-05-25

Modo: GIPROY CLASICO

## Objetivo

Completar la migracion conservadora de transporte API en `Settings` sin cambiar comportamiento funcional, rutas backend, payloads, UI, tenant global, base de datos ni BIM.

## Alcance

- `frontend/src/api/empresas.js`
- `frontend/src/pages/Settings.jsx`
- `docs/frontend_dependency_inventory.json`
- Documentacion de progreso y contexto IA.

## Cambios realizados

- Se agrega `empresasApi.uploadLogo(empresaId, formData)` con el mismo endpoint de upload de logo existente.
- `Settings` usa `empresasApi.uploadLogo()` para uploads de logo.
- `Settings` usa `empresasApi.update()` para plantillas/configuracion de empresa.
- `Settings` usa `proyectosApi.update()` para plantillas de proyecto.
- `Settings` usa `empresasApi.getAll()` para refrescar empresas despues de actualizar datos de empresa como superadministrador.
- Se elimina el import directo de `axiosConfig` en `Settings`.

## No cambios

- No se modifico backend.
- No se modifico DB ni migraciones.
- No se modificaron contratos API.
- No se modificaron formularios, validaciones, roles ni UI.
- No se modifico `AuthContext`.
- No se modifico codigo BIM ni se activo UX BIM.

## Validacion

- `tools/ai_tools/frontend_dependency_inventory.py`: OK.
- `npm run build`: OK.
- `tools/ai_tools/validate_enterprise_baseline.py --include-frontend`: OK.
- Smoke anti-BIM incluido en baseline: OK.

## Resultado

- Inventario frontend baja de 13 a 6 llamadas directas fuera de `frontend/src/api`.
- Las 6 llamadas restantes estan concentradas en `frontend/src/context/AuthContext.jsx`.

## Rollback

Revertir este archivo y los cambios en:

- `frontend/src/api/empresas.js`
- `frontend/src/pages/Settings.jsx`
- `docs/frontend_dependency_inventory.json`
- `docs/CHANGELOG.md`
- `docs/SAFE_REFACTOR_PROGRESS.md`
- `docs/FRONTEND_DEPENDENCY_MAP.md`
- `AI_CONTEXT.md`
- `docs/runtime/WORK_MODE_STATE.json`

No requiere rollback de DB.
