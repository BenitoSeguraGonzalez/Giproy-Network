# TASK-1798 - BasesTrabajo sin import directo de axiosConfig

Fecha: 2026-05-25

Modo: GIPROY CLASICO

## Objetivo

Cerrar el ultimo import directo de `axiosConfig` fuera de `frontend/src/api` en la capa clasica, manteniendo el comportamiento de Bases de Trabajo y dejando una guarda focal para evitar regresion.

## Alcance

- `frontend/src/pages/BasesTrabajo.jsx`
- `frontend/scripts/smoke-classic-bases-trabajo-api-boundary.mjs`
- `frontend/package.json`
- `tools/ai_tools/validate_enterprise_baseline.py`
- `docs/frontend_dependency_inventory.json`
- Documentacion de progreso y contexto IA.

## Cambios realizados

- Se elimina el import directo huérfano `api` desde `../api/axiosConfig`.
- `fetchPaises()` queda apoyado en `maestrosApi.getPaises()`, cliente ya existente y tenantless.
- Se crea smoke estatico `smoke-classic-bases-trabajo-api-boundary.mjs`.
- Se agrega script npm `smoke:classic-bases-trabajo`.
- El baseline enterprise ejecuta el smoke cuando se usa `--include-frontend`.
- Se regenera `docs/frontend_dependency_inventory.json`.
- El smoke valida que:
  - `BasesTrabajo` no importe `axiosConfig` directamente.
  - `BasesTrabajo` conserve `basesTrabajoApi`, `maestrosApi` y `proyectosApi`.
  - Listado, activacion, creacion, edicion, borrado, sincronizacion, historial y reversión mantengan sus clientes y tenant params.
  - `basesTrabajoApi` conserve endpoints criticos y fallback de compatibilidad.
  - `maestrosApi.getPaises()` conserve lectura tenantless.

## No cambios

- No se modifico UI.
- No se modificaron rutas backend, payloads, DB, auth, tenant runtime ni BIM.
- No se modifico la logica de sincronizacion de bases.

## Validacion

- `npm run smoke:classic-bases-trabajo`: OK.
- Busqueda de imports directos `axiosConfig` fuera de `frontend/src/api`: sin resultados.
- `tools/ai_tools/frontend_dependency_inventory.py`: OK.
- `npm run build`: OK.
- `tools/ai_tools/validate_enterprise_baseline.py --include-frontend`: OK.
- Smoke anti-BIM incluido en baseline: OK.

## Resultado

La capa frontend clasica queda sin imports directos de `axiosConfig` fuera de `frontend/src/api`, y el baseline protege la frontera API de Bases de Trabajo.

## Rollback

Revertir este archivo y los cambios en:

- `frontend/src/pages/BasesTrabajo.jsx`
- `frontend/scripts/smoke-classic-bases-trabajo-api-boundary.mjs`
- `frontend/package.json`
- `tools/ai_tools/validate_enterprise_baseline.py`
- `docs/frontend_dependency_inventory.json`
- `docs/CHANGELOG.md`
- `docs/SAFE_REFACTOR_PROGRESS.md`
- `AI_CONTEXT.md`
- `docs/runtime/WORK_MODE_STATE.json`

No requiere rollback de DB.
