# TASK-1801 - ApuBudgetEditor sin console.log productivos

Fecha: 2026-05-25

Modo: GIPROY CLASICO

## Objetivo

Retirar trazas `console.log` productivas del editor APU de presupuesto que exponian fallbacks de revision a base maestra en consola, sin modificar el fallback ni el flujo funcional.

## Alcance

- `frontend/src/components/presupuestos/ApuBudgetEditor.jsx`
- `frontend/scripts/smoke-classic-presupuesto-api-boundary.mjs`
- `docs/logging_inventory.json`
- Documentacion de progreso y contexto IA.

## Cambios realizados

- Se eliminan tres `console.log` del fallback de recursos, APUs y subcategorias desde revision a maestro.
- Se refuerza el smoke de Presupuestos para incluir `ApuBudgetEditor`.
- El smoke valida que `ApuBudgetEditor` no conserve `console.log` productivos y que mantenga clientes/fallbacks criticos.
- Se regenera `docs/logging_inventory.json`.

## No cambios

- No se modifico UI.
- No se modificaron calculos, lineas APU, recursos, subcategorias, fallback revision/maestro, guardados, rutas backend, payloads, DB, auth, tenant runtime ni BIM.
- No se eliminaron diagnosticos de error.

## Validacion

- Busqueda de `console.log` en `ApuBudgetEditor.jsx`: sin resultados.
- `.\.venv\Scripts\python.exe tools\ai_tools\logging_inventory.py`: OK.
- `.\.venv\Scripts\python.exe tools\ai_tools\validate_enterprise_baseline.py --include-frontend`: OK.
- Smoke de Presupuestos incluido en baseline: OK.
- Smoke anti-BIM incluido en baseline: OK.

Nota: la ejecucion aislada de `npm run smoke:classic-presupuesto` fallo por error del runner/sandbox de Windows (`CreateProcessAsUserW failed: 1312`), pero el mismo smoke paso dentro del baseline enterprise.

## Resultado

`ApuBudgetEditor` queda sin `console.log` productivos y protegido por el smoke de Presupuestos, manteniendo intacto el fallback operativo.

## Rollback

Revertir este archivo y los cambios en:

- `frontend/src/components/presupuestos/ApuBudgetEditor.jsx`
- `frontend/scripts/smoke-classic-presupuesto-api-boundary.mjs`
- `docs/logging_inventory.json`
- `docs/CHANGELOG.md`
- `docs/SAFE_REFACTOR_PROGRESS.md`
- `AI_CONTEXT.md`
- `docs/runtime/WORK_MODE_STATE.json`

No requiere rollback de DB.
