# TASK-1803 - AuthContext sin console.log productivos

Fecha: 2026-05-25

Modo: GIPROY CLASICO

## Objetivo

Retirar las ultimas trazas `console.log` productivas del frontend clasico, ubicadas en `AuthContext`, sin modificar autenticacion, refresh, expiracion de sesion, device id ni logout.

## Alcance

- `frontend/src/context/AuthContext.jsx`
- `frontend/scripts/smoke-classic-api-boundaries.mjs`
- `docs/logging_inventory.json`
- Documentacion de progreso y contexto IA.

## Cambios realizados

- Se elimina log de inicializacion de `deviceId`.
- Se elimina log de conectividad backend.
- Se elimina log de expiracion de sesion.
- El check no bloqueante `authApi.getCurrentUser()` se conserva con `catch` no-op.
- La expiracion conserva `clearLifecycleTimers()` y `clearSessionState(message)`.
- Se refuerza el smoke de fronteras API para bloquear `console.log` en `AuthContext` y verificar flujo critico.
- Se regenera `docs/logging_inventory.json`.

## No cambios

- No se modifico UI.
- No se modificaron login, refresh, logout, rehidratacion, device id, eventos de sesion, token storage, auth API, rutas backend, payloads, DB, tenant runtime ni BIM.
- No se eliminaron diagnosticos `console.warn` existentes.

## Validacion

- Busqueda de `console.log` en `frontend/src` fuera de `api`: sin resultados.
- `.\.venv\Scripts\python.exe tools\ai_tools\logging_inventory.py`: OK.
- `.\.venv\Scripts\python.exe tools\ai_tools\validate_enterprise_baseline.py --include-frontend`: OK.
- Smoke API boundaries incluido en baseline: OK.
- Smoke anti-BIM incluido en baseline: OK.

## Resultado

El frontend clasico queda sin `console.log` productivos en `frontend/src`, con `AuthContext` protegido por smoke focal.

## Rollback

Revertir este archivo y los cambios en:

- `frontend/src/context/AuthContext.jsx`
- `frontend/scripts/smoke-classic-api-boundaries.mjs`
- `docs/logging_inventory.json`
- `docs/CHANGELOG.md`
- `docs/SAFE_REFACTOR_PROGRESS.md`
- `AI_CONTEXT.md`
- `docs/runtime/WORK_MODE_STATE.json`

No requiere rollback de DB.
