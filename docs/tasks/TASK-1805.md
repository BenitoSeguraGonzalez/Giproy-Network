# TASK-1805 - Preflight Fase 6 entornos y Docker

Fecha: 2026-05-25

Modo: GIPROY CLASICO

## Objetivo

Abrir Fase 6 de forma conservadora con un preflight documental de entornos y Docker, sin crear Dockerfiles, sin modificar arranque local y sin tocar servicios Windows, DB, auth, tenant ni BIM.

## Alcance

- `docs/ENVIRONMENT_DOCKER_PREFLIGHT.md`
- Documentacion de progreso, changelog, contexto IA y estado runtime.

## Cambios realizados

- Se documenta el contrato real observado:
  - Backend FastAPI con prefijo `/api/v1`.
  - Backend operativo local alineado con proxy Vite en `3001`.
  - Frontend Vite en `3010`.
  - Proxy local `/api` hacia `http://localhost:3001`.
  - Cliente frontend principal usando `baseURL: '/api/v1'`.
  - PostgreSQL separado.
  - BIM desactivado por defecto.
- Se listan riesgos previos a Docker.
- Se define el orden seguro para Fase 6 antes de crear Dockerfiles.

## No cambios

- No se modifico codigo productivo.
- No se crearon Dockerfiles ni `docker-compose`.
- No se modificaron `.env` reales, servicios Windows, puertos, DB, auth, tenant runtime, uploads ni BIM.

## Validacion

- `.\.venv\Scripts\python.exe tools\ai_tools\validate_enterprise_baseline.py --include-frontend`: OK antes de documentar.
- Validacion final posterior a documentacion pendiente en el cierre de esta TASK.

## Resultado

Fase 6 queda abierta en modo preflight documental, con criterios para dockerizacion futura sin romper el entorno local.

## Rollback

Revertir este archivo y los cambios en:

- `docs/ENVIRONMENT_DOCKER_PREFLIGHT.md`
- `docs/CHANGELOG.md`
- `docs/SAFE_REFACTOR_PROGRESS.md`
- `AI_CONTEXT.md`
- `docs/runtime/WORK_MODE_STATE.json`

No requiere rollback de DB.
