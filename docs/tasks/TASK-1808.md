# TASK-1808 - Actualizacion de plantilla Modo Clasico con baseline seguro

Fecha: 2026-05-25

Modo: GIPROY CLASICO

## Objetivo

Actualizar `docs/plantillas prompt/Prompt Modo Clasico.txt` para que futuras sesiones clasicas conozcan y mantengan la refactorizacion segura cerrada en `TASK-1807`.

## Alcance

- Solo documentacion y plantilla de prompt.
- Sin cambios de codigo productivo.
- Sin cambios de base de datos.
- Sin Docker, Coolify, CI/CD, staging ni produccion.
- Sin cambios BIM.

## Cambios

- Se agrega estado enterprise vigente: fases 0-5 cerradas, Fase 6 en preflight y despliegue pausado.
- Se incorporan reglas de conservacion del baseline `TASK-1807`.
- Se explicitan guardas frontend: clientes API de dominio, ausencia de imports directos de `axiosConfig`, ausencia de `console.log` productivos y smokes focales.
- Se refuerzan reglas backend: no cambios destructivos DB, auth, tenant, permisos ni contratos sin TASK explicita.
- Se exige terminar resumen con porcentaje de finalizacion.

## Validacion

- `docs/runtime/WORK_MODE_STATE.json` validado como JSON.
- Referencias `TASK-1807`, `TASK-1808`, `axiosConfig`, `console.log`, Docker/Coolify y porcentaje verificadas en plantilla/contexto.
- `tools/ai_tools/validate_enterprise_baseline.py --include-frontend` OK.

Warnings conocidos no bloqueantes:

- Pydantic avisa por campos BIM `model_name` y `model_id`.
- Vite avisa por chunks grandes.

## Rollback

Revertir este archivo y las referencias asociadas en changelog/contexto si la plantilla resulta demasiado restrictiva para una sesion futura.
