# TASK-1809 - Correccion z-index selector empresa clasico

Fecha: 2026-05-25

Modo: GIPROY CLASICO

## Objetivo

Corregir el apilamiento visual del selector de empresa/auditoria del header clasico para que no quede tapado por cabeceras internas del modulo Proyectos/Cronogramas.

## Alcance

- Frontend clasico: `frontend/src/layouts/AppLayout.jsx`.
- Sin cambios de backend.
- Sin cambios de base de datos.
- Sin cambios de contratos API, auth, tenant, permisos, EDT, presupuestos ni cronogramas.
- Sin cambios BIM, UX BIM ni acoplamientos hacia BIM.
- Sin Docker, Coolify, CI/CD, staging ni produccion.

## Cambios

- Se eleva el `z-index` del `nav` persistente clasico.
- Se eleva el `z-index` del panel desplegable de empresa para quedar por encima de headers internos con capas altas.
- Se conserva la logica existente de carga, seleccion y cierre del selector.

## Validacion

- `npm run build` OK desde `frontend/`.
- `node scripts/smoke-classic-no-bim-contamination.mjs` OK desde `frontend/`.
- JSON documental OK para `docs/project_state.json`, `docs/architecture/project_map.json` y `docs/runtime/WORK_MODE_STATE.json`.
- Busqueda sin resultados de imports directos de `axiosConfig` fuera de `frontend/src/api`.
- Busqueda sin resultados de `console.log` productivos fuera de `frontend/src/api`.

Warnings conocidos no bloqueantes:

- Vite advierte chunks grandes.

## Rollback

Revertir los cambios de `z-index` en `frontend/src/layouts/AppLayout.jsx` y retirar esta TASK/changelog si el ajuste visual no corresponde.
