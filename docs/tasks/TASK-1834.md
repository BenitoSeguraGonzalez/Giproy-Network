# TASK-1834 - Recuperacion operativa Gantt por aislamiento de procesos

Fecha: 2026-05-26  
Modo: GIPROY CLASICO

## Objetivo

Recuperar una instancia verificable de Gantt clasico despues de detectar que los puertos historicos seguian ocupados por procesos protegidos y no terminables desde la sesion actual.

## Diagnostico

- `frontend/src` completo coincide con la copia `GiProy Network Bkp`.
- `frontend/dist` completo coincide con la copia `GiProy Network Bkp`.
- `backend/app` coincide con la copia salvo un test no productivo.
- La base local auditada para `Santiago Bermeo`, presupuesto `13`, contiene predecesoras/dependencias y no reproduce las fechas absurdas vistas en UI.
- Los puertos `3001`, `3010` y `5173` seguian escuchando con procesos protegidos.

## Correccion operativa

- Se levanta backend limpio en `3101`.
- Se levanta frontend limpio en `3110`.
- El frontend de recuperacion usa proxy `/api` hacia `http://localhost:3101`.
- El nuevo origen `localhost:3110` evita preferencias antiguas de `localStorage` asociadas a `3010`.

## Validacion

- `http://localhost:3110/`: responde `200`.
- `http://localhost:3110/api/v1/openapi.json`: responde `200`.
- `http://localhost:3101/api/v1/openapi.json`: responde `200`.

## Seguridad

- Sin Docker/Coolify/CI/CD.
- Sin cambios de BD.
- Sin cambios BIM.
- Sin modificar logica productiva de Gantt.

## Rollback

Detener los procesos de recuperacion en `3101` y `3110` y eliminar `frontend/vite.gantt-recovery.config.mjs`.
