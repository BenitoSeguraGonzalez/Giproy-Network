# TASK-1840 - Cliente API read-only Recursos Cronogramas

Fecha: 2026-05-26  
Modo: GIPROY CLASICO

## Objetivo

Preparar la frontera frontend clasica para la futura vista operativa `Recursos`, consumiendo el endpoint read-only mediante el cliente de dominio existente `cronogramasApi`.

## Alcance

- Agregar `cronogramasApi.getRecursos(presupuestoId, empresaId)`.
- Reforzar el smoke clasico de Cronogramas para bloquear el endpoint y metodo esperados.

## Fuera de alcance

- Sin vista `Recursos` todavia.
- Sin consumo desde `Cronogramas.jsx`.
- Sin cambios en `CronogramaGantt.jsx`.
- Sin persistencia, nivelacion automatica/manual, BIM, Docker/Coolify/CI/CD, DB, auth, tenant ni permisos.

## Validacion requerida

- `npm run smoke:classic-cronogramas`.
- `npm run build`.
- Smoke anti-BIM.
- Baseline enterprise con frontend.
- Hash de `CronogramaGantt.jsx` contra copia de seguridad.

## Rollback

Quitar `getRecursos`, revertir el smoke, eliminar esta TASK y la entrada de `CHANGELOG`.
