# TASK-1843 - Cliente API estado Recursos Cronogramas

Fecha: 2026-05-26  
Modo: GIPROY CLASICO

## Objetivo

Conectar la frontera API frontend clasica con la persistencia backend de Recursos creada en TASK-1842, sin consumirla todavia desde la vista.

## Alcance implementado

- Se agrega `cronogramasApi.getRecursosState(presupuestoId, empresaId)`.
- Se agrega `cronogramasApi.updateRecursosState(presupuestoId, data, empresaId)`.
- Ambos metodos usan `withTenantConfig`.
- El smoke clasico de Cronogramas valida metodos y endpoint `/recursos/state`.

## Politica aplicada

- Sin tocar `CronogramaGantt.jsx`.
- Sin modificar `Cronogramas.jsx` en este slice.
- Sin aplicar estado persistente a Gantt, Valorado ni Flujo.
- Sin nivelacion automatica/manual todavia.
- Sin BIM, UX BIM, Docker, Coolify, CI/CD, staging ni produccion.

## Validacion esperada

- Smoke clasico de frontera Cronogramas.
- Build frontend.
- Smoke anti-BIM.
- Hash de `CronogramaGantt.jsx` igual al backup.
- Baseline enterprise con frontend.

## Rollback

Revertir:

- `frontend/src/api/cronogramas.js`
- `frontend/scripts/smoke-classic-cronogramas-api-boundary.mjs`
- esta TASK y su entrada de `docs/CHANGELOG.md`

