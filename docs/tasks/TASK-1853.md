# TASK-1853 - Activacion local de estado colaborativo Recursos

Fecha: 2026-05-27  
Modo: GIPROY CLASICO

## Objetivo

Corregir el error runtime `No fue posible cargar el estado colaborativo de recursos` en `Cronogramas > Recursos`.

## Diagnostico

- El frontend cargaba correctamente la demanda de recursos.
- Fallaba `GET /cronogramas/valorados/{presupuesto_id}/recursos/state`.
- La tabla `cronogramas_recursos_state` no existia en la base local `giproy_erp`.
- La migracion `ae8f9a0b1c2d_add_cronograma_recursos_state.py` existia en codigo, pero no estaba aplicada en la base local.

## Correccion aplicada

- Se ejecuto la migracion Alembic focal:
  - `alembic upgrade ae8f9a0b1c2d`
- Se verifico que la tabla `cronogramas_recursos_state` existe con columnas esperadas.

## Politica aplicada

- Cambio no destructivo de PostgreSQL: solo crea la tabla planificada.
- Sin cambios de codigo productivo.
- Sin tocar `CronogramaGantt.jsx`.
- Sin cambios en contratos API, auth, tenant, permisos, Gantt, Valorado, Flujo ni datos de negocio.
- Sin BIM, UX BIM, Docker, Coolify, CI/CD, staging ni produccion.

## Validacion esperada

- Verificacion de existencia de tabla.
- Pytest focal de Recursos.
- Hash de `CronogramaGantt.jsx` igual al backup.
- Baseline enterprise con frontend.

## Rollback

Rollback documental/DB solo si se necesitara desactivar esta funcionalidad local:

- `alembic downgrade ac7d8e9f10a1` para revertir la tabla de estado de Recursos.

No ejecutar rollback si ya existen estados colaborativos reales guardados.

