# TASK-1903 - Avisos de solo lectura y recordatorios de gracia

Fecha: 2026-06-09
Modo: GIPROY CLASICO

## Objetivo

Informar de modo solo lectura y disponibilidad restante durante el periodo de gracia.

## Alcance previsto

- Mensaje de login mientras `access_mode=readonly`.
- Email el dia de entrada en solo lectura.
- Email cada 5 dias durante el periodo de gracia.
- Texto claro sobre borrado tras periodo estipulado.

## Estado

Parcial.

## Slice implementado

- `license_notifications` cola `license_readonly` cuando la licencia termino y la empresa sigue dentro de `grace_ends_at`.
- El aviso in-app usa `display_once=false` para poder reaparecer en futuros logins mientras siga pendiente.
- Email al administrador el primer dia operativo tras caducidad y cada 5 dias.
- El ultimo dia de disponibilidad cola email `license_data_deletion_today`.
- El texto advierte modo solo lectura, fecha final de disponibilidad y retirada de datos al finalizar el periodo.
- La API superadministrador `/api/v1/license-notifications/housekeeping/run` incluye estos avisos en el barrido integral manual.
- El runner integral ejecuta primero el housekeeping de licencias, por lo que una licencia activa vencida pasa a `expired/read_only` y genera `license_readonly` en la misma ejecucion.
- Validacion local PostgreSQL `dry-run` con fecha `2027-03-13`: genera avisos `license_readonly`, despacha email mock y no persiste eventos al revertir.

## Pendiente

- Programar ejecucion periodica real del runner.
- Configuracion SMTP real.
- Ejecutar la politica final de backup/cuarentena/purga cuando se autorice en TASKs de datos.
