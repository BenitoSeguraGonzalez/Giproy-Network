# TASK-1909 - Validacion visual de contrasenas en registro publico

Fecha: 2026-06-10
Modo: GIPROY CLASICO

## Objetivo

Mejorar el modal clasico de crear cuenta para que las contrasenas se puedan
revisar visualmente y la discordancia se detecte al salir de los campos.

## Alcance

- Frontend clasico de registro publico.
- Variante `publicRegister` de `PersonnelFormFields`.
- Sin cambios backend, auth, JWT, tenant, licencias, EDT, presupuestos,
  cronogramas, BIM, Docker/Coolify/CI/CD ni PostgreSQL.

## Cambios realizados

- Los campos `Contraseña` y `Confirmar Contraseña` del registro publico tienen
  boton iconografico para ver/ocultar el texto digitado.
- Al salir de cualquiera de los dos campos, si ambos tienen contenido y no
  coinciden, se muestra aviso inline y ambos campos quedan resaltados.
- La validacion final de submit en `RegisterModal` se conserva.
- Se retiran `console.error` existentes del archivo tocado para no reintroducir
  trazas productivas.

## Validacion ejecutada

- `npm run build` OK, con warning conocido de chunks grandes Vite.
- `node scripts/smoke-classic-no-bim-contamination.mjs` OK.
- `node scripts/smoke-classic-api-boundaries.mjs` OK.
- Guarda focal: sin `console.log/error/warn` en `PersonnelFormFields`,
  `RegisterModal` ni la pagina de verificacion; `axiosConfig` solo aparece en
  `frontend/src/api/publicAuth.js`, dentro de la capa permitida.

## No interferencia BIM

No se modifica codigo BIM ni se activa UX BIM.

## Rollback

Revertir `frontend/src/components/PersonnelFormFields.jsx`, esta TASK y las
entradas documentales asociadas.
