# TASK-1917 - Contrato tecnico Envios y Transferencias

Estado: Cerrada

Fecha: 2026-06-16

Modo: GIPROY CLASICO

## Objetivo

Definir e implementar la base tecnica minima del dominio clasico de `Envios y
Transferencias`: modelos, contratos, estados, version de snapshot, huella/hash,
permisos y adaptador de transferencia separado de venta/Marketplace.

## Alcance

- Backend comun clasico.
- Migraciones PostgreSQL no destructivas.
- Contrato API inicial sin UI final.
- Sin BIM, Docker, Coolify, CI/CD, staging ni produccion.

## Dependencias

- `TASK-1916`

## Aceptacion

- Modelo conceptual convertido en contrato tecnico versionado.
- Estados de envio definidos como enumeracion cerrada.
- `contract_version` obligatorio para snapshots.
- Hash/huella de payload exportable definido.
- Adaptador de transferencia separado de publicacion/venta Marketplace.
- Permisos base: solo Administrador/Superadministrador.
- Licencias bloqueadas contempladas desde contrato: `EXPRESS`, `TESTER`,
  `ACADEMIC`, `TRAINING`.
- Sin cambios destructivos en DB.

## Verificacion

- `python -m py_compile` focal backend.
- Tests de contrato/modelos si se crea codigo ejecutable.
- Validacion Alembic si hay migracion.
- Sin referencias BIM nuevas.

## Cambios realizados

- Se crea el modelo backend clasico `transferencia.py` para codigos publicos,
  destinatarios, paquetes adicionales, envios, items, requisitos Marketplace,
  auditoria, resultados de importacion y referencias de origen.
- Se crea contrato API versionado `transfer-v1` en
  `/api/v1/transferencias/contract`.
- Se fijan estados aprobados, roles autorizados, licencias bloqueadas y flags
  tecnicos de `dry-run`, snapshot hash, adaptador separado de Marketplace,
  compra completa, idempotencia y guarda de licencias.
- Se agrega migracion no destructiva
  `de1917a1b2c3_transferencias_foundation.py`.
- Se agrega test focal de contrato `test_transferencias_contract.py`.

## Validacion ejecutada

- `.\\.venv\\Scripts\\python.exe -m py_compile backend\\app\\models\\transferencia.py backend\\app\\schemas\\transferencia.py backend\\app\\api\\endpoints\\transferencias.py backend\\app\\api\\api.py backend\\alembic\\versions\\de1917a1b2c3_transferencias_foundation.py`
- `..\\.venv\\Scripts\\python.exe -m pytest app\\tests\\test_transferencias_contract.py -q`
- `.\\.venv\\Scripts\\python.exe -c "import json; ..."` para JSON documental
  principal.
- `node frontend\\scripts\\smoke-classic-no-bim-contamination.mjs`
- `.\\.venv\\Scripts\\python.exe tools\\ai_tools\\validate_enterprise_baseline.py`
- `rg` focal: sin referencias BIM en codigo nuevo; sin `axiosConfig` ni
  `console.log` fuera de `frontend/src/api`.

Warnings conocidos:

- Pydantic `model_name` / `model_id` protegido en modelos BIM.
- Deprecation warnings de Pydantic/httpx existentes en entorno de tests.

## Rollback

Revertir modelos, migracion, schemas/contratos y documentacion de esta TASK.
