# TASK-1925 - UI clasica de Envios y Transferencias

Estado: Cerrada

Fecha: 2026-06-16

Modo: GIPROY CLASICO

## Objetivo

Implementar la experiencia clasica completa en `Otros Servicios > Envios y
Transferencias`, alineada visual y funcionalmente con `Proyectos`.

## Alcance

- Frontend clasico.
- Clientes API en `frontend/src/api`.
- Bandeja, nuevo envio, preflight modal tecnico, requisitos Marketplace y
  timeline.
- Sin UX BIM ni lenguaje visual SaaS de Administracion Global.

## Dependencias

- `TASK-1918`
- `TASK-1919`
- `TASK-1920`
- `TASK-1921`
- `TASK-1922`

## Aceptacion

- Primera pantalla operativa, no landing ni placeholder comercial.
- Bandeja como lista compacta con informacion necesaria.
- Toolbar con `Nuevo envio` y `Refrescar`.
- Filtros entrada/salida, estado y rango de fechas.
- Busqueda omni con alias como etiqueta visual preferente.
- Estados con colores diferenciados.
- Preflight en modal tecnico.
- Doble confirmacion para envio con dependencias Marketplace.
- Ayuda contextual Marketplace compacta con lista, estado, precio vigente,
  compra y `Revalidar compras`.
- Timeline visible resumido por envio.
- UI reutiliza funcionamiento y componentes de `Proyectos` cuando aplique.
- Checklist visual comparativo contra `Proyectos`/`Datos de Proyecto`
  documentado antes del cierre.
- Sin imports directos de `axiosConfig` fuera de `frontend/src/api`.
- Sin `console.log` productivo.

## Verificacion

- `npm run build` OK.
- `node frontend/scripts/smoke-classic-no-bim-contamination.mjs` OK.
- `cmd /c node frontend\scripts\smoke-classic-transfer-header-signal.mjs` OK.
- `cmd /c node frontend\scripts\smoke-classic-transferencias-ui.mjs` OK.
- `tools/ai_tools/validate_enterprise_baseline.py --include-frontend` OK.
- Guarda focal por `rg`: sin `axiosConfig`, `console.log`, `<select>`,
  `window.prompt` ni `input type="date"` en la nueva pantalla.
- Warning conocido no bloqueante: chunks grandes Vite.

## Cambios realizados

- Nueva pagina clasica `frontend/src/pages/EnviosTransferencias.jsx`.
- Ruta protegida `/servicios/envios-transferencias`.
- Card `Envios y Transferencias` en `Otros Servicios` conectada a la bandeja
  real.
- Semaforo del header redirige a
  `/servicios/envios-transferencias?bandeja=entrada`.
- Bandeja compacta con filtros de entrada/salida, busqueda omni, estado y
  rango de fechas.
- Toolbar operativa con `Nuevo envio` y `Refrescar`.
- Modal tecnico de nuevo envio con destinatarios, activo, descripcion
  obligatoria, preflight, snapshot hash y confirmacion Marketplace.
- Acciones de entrada: abrir, importar, rechazar con motivo obligatorio y
  revalidar compras Marketplace.
- Timeline resumido y chip de bloqueo Marketplace.
- Smoke UI permanente
  `frontend/scripts/smoke-classic-transferencias-ui.mjs`.

## Checklist visual Proyectos / Datos de Proyecto

- Primera pantalla operativa, no hero ni landing.
- Fondo claro, superficies blancas soft, radios y bordes compatibles con la
  capa clasica.
- Toolbar compacta y botones con iconos `lucide-react`.
- Listado tipo bandeja densa, priorizando lectura diaria sobre tarjetas
  comerciales.
- Filtros con controles del sistema (`SearchableSelect` y
  `AnimatedDateInput`), sin selectores nativos.
- Modal tecnico, no pantalla comercial ni estilo SaaS global.
- Estados mediante chips sobrios y colores diferenciados.
- Sin UX BIM, sin texto BIM y sin acoplamiento hacia rutas BIM.

## Resultado

`TASK-1925` completa el slice frontend clasico de bandeja y flujo inicial de
`Envios y Transferencias`. El plan global queda en 90% y pasa a `TASK-1926`
para cierre enterprise/E2E final.

## Rollback

Revertir ruta/pagina/componentes/clientes API/smokes de transferencia.
