# TASK-1926 - Baseline enterprise Envios y Transferencias

Estado: Cerrada

Fecha: 2026-06-16

Modo: GIPROY CLASICO

## Objetivo

Cerrar la implementacion completa de `Envios y Transferencias` con validacion
enterprise transversal, pruebas focales y evidencia de no interferencia BIM.

## Alcance

- Backend/frontend integrados.
- Validacion final del modulo clasico.
- Sin Docker, Coolify, CI/CD, staging ni produccion.

## Dependencias

- `TASK-1917`
- `TASK-1918`
- `TASK-1919`
- `TASK-1920`
- `TASK-1921`
- `TASK-1922`
- `TASK-1923`
- `TASK-1924`
- `TASK-1925`

## Aceptacion

- Flujo E2E con empresa mockup emisor/receptor sin Marketplace.
- Flujo E2E con empresa mockup emisor/receptor con requisitos Marketplace.
- Guarda anti-regresion: `EXPRESS`, `TESTER`, `ACADEMIC` y `TRAINING` no
  pueden enviar, recibir, abrir ni importar.
- Importacion idempotente validada.
- Metricas minimas verificadas.
- Semaforo header validado por rol.
- UI validada contra checklist visual.
- Sin imports directos de `axiosConfig` fuera de `frontend/src/api`.
- Sin `console.log` productivos fuera de API.
- Sin dependencia BIM nueva.

## Verificacion

- `python -m py_compile` backend focal OK.
- Pytest focal backend de transferencias, Marketplace y licencias OK
  (`54 passed`).
- `npm run build` OK.
- Smokes focales:
  - `smoke-classic-transfer-header-signal.mjs` OK.
  - `smoke-classic-transferencias-ui.mjs` OK.
  - `smoke-classic-no-bim-contamination.mjs` OK.
- `tools/ai_tools/validate_enterprise_baseline.py --include-frontend` OK.
- Warnings conocidos aceptados: Pydantic BIM `model_name/model_id`,
  deprecations de Pydantic/httpx en tests y chunks grandes Vite.

## Cierre de aceptacion

- Flujo mockup emisor/receptor sin Marketplace validado por importacion de
  Proyecto y Base de Trabajo como copia nueva.
- Flujo con requisitos Marketplace validado por bloqueo de apertura/importacion,
  compra completa obligatoria, precio vigente y revalidacion.
- Guardas anti-regresion:
  - `EXPRESS`, `TESTER`, `ACADEMIC` y `TRAINING` no pueden resolver/asociar
    destinatarios.
  - Un receptor con esas licencias tampoco puede abrir ni importar envios.
- Importacion idempotente validada.
- Fallo de importacion recuperable y sin duplicado validado.
- Metricas minimas y resumen header validados.
- Semaforo header validado por rol y ruta real.
- UI validada contra checklist visual de `Proyectos` / `Datos de Proyecto`.
- Sin imports directos de `axiosConfig` fuera de `frontend/src/api`.
- Sin `console.log` productivos fuera de API.
- Sin dependencia ni UX BIM nueva.
- Sin Docker, Coolify, CI/CD, staging ni produccion.

## Cambios finales TASK-1926

- Se refuerzan tests de licencias bloqueadas en:
  - `backend/app/tests/test_transferencias_recipients.py`.
  - `backend/app/tests/test_transferencias_import.py`.
- Se cierra el plan de `Envios y Transferencias` al 100% dentro del alcance
  aprobado para GiProy Clasico.

## Porcentaje

100% del plan de adecuacion e implementacion de `Otros Servicios > Envios y
Transferencias` queda completado y validado dentro del alcance actual.

## Rollback

Rollback por TASK previa segun el slice afectado. Si el baseline falla por
integracion, bloquear release del modulo y revertir el ultimo slice funcional.
