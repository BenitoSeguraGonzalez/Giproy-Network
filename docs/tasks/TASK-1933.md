# TASK-1933 - Codigo publico de empresa Transferencias

Estado: Cerrada

Fecha: 2026-06-17

Modo: GIPROY CLASICO

## Objetivo

Corregir la identidad publica de `Otros Servicios > Envios y Transferencias`
para que el codigo `XXX - XXX` pertenezca a la empresa y no al usuario
administrador que opera la sesion.

## Alcance

- Modelo backend clasico de transferencias.
- Servicio de generacion, saneamiento y resolucion de codigos.
- Endpoints de transferencias con empresa operativa seleccionada.
- Modal clasico de empresas para comunicarse.
- Tests focales de destinatarios.
- Documentacion de plan, TASK, CHANGELOG y HANDOFF.

## Fuera de alcance

- No se modifican auth, JWT ni permisos generales.
- No se cambian contratos de proyectos, EDT, presupuestos, cronogramas,
  Marketplace ni importacion/exportacion fuera del scope de transferencias.
- No se elimina la tabla legacy de codigos por usuario.
- No UX BIM ni dependencias BIM.
- No Docker/Coolify/CI-CD/staging/produccion.

## Cambios realizados

- Se agrega `TransferCompanyPublicCode` como identidad activa por empresa.
- Se agrega migracion aditiva `de1933a1b2c3_transfer_company_public_codes.py`.
- `ensure_admin_public_code` pasa a generar/retornar codigo de empresa para
  conservar compatibilidad de llamadas existentes.
- Se mantiene `ensure_legacy_admin_public_code` para compatibilidad explicita
  con el modelo anterior.
- La resolucion de destinatarios busca primero codigo de empresa y conserva
  fallback legacy.
- Las rutas de transferencias aceptan `empresa_id` operativo y lo validan:
  superadministrador puede operar empresa seleccionada; administrador normal no.
- `/transferencias/admin/sanitize-company-codes` sanea codigos faltantes por
  empresa.
- La UI rotula el codigo como `Codigo publico de empresa`.
- Tests focales cubren codigos distintos para `Administradores Generales` y
  `Santiago Bermeo` con el mismo superadministrador.

## Aceptacion

- Dos empresas distintas no comparten codigo por estar operadas por el mismo
  superadministrador.
- El codigo mostrado en el modal corresponde a la empresa operativa.
- El codigo resuelve empresas, no usuarios.
- No se exponen datos personales del administrador receptor.
- No hay contaminacion BIM ni cambios Docker/Coolify/CI-CD.

## Validacion

- `.\\.venv\\Scripts\\python.exe -m py_compile backend\\app\\models\\transferencia.py backend\\app\\schemas\\transferencia.py backend\\app\\services\\transferencias.py backend\\app\\api\\endpoints\\transferencias.py backend\\alembic\\versions\\de1933a1b2c3_transfer_company_public_codes.py`.
- `..\\.venv\\Scripts\\python.exe -m pytest app\\tests\\test_transferencias_recipients.py -q`.
- `node frontend/scripts/smoke-classic-transferencias-ui.mjs`.
- `node frontend/scripts/smoke-classic-no-bim-contamination.mjs`.
- `npm run build`.
- `tools/ai_tools/validate_enterprise_baseline.py --include-frontend`.

## Rollback

Revertir cambios en:

- `backend/app/models/transferencia.py`.
- `backend/app/models/__init__.py`.
- `backend/app/schemas/transferencia.py`.
- `backend/app/services/transferencias.py`.
- `backend/app/api/endpoints/transferencias.py`.
- `backend/alembic/versions/de1933a1b2c3_transfer_company_public_codes.py`.
- `backend/app/tests/test_transferencias_recipients.py`.
- `frontend/src/pages/EnviosTransferencias.jsx`.
- Documentacion asociada.

## Porcentaje

100% del slice autorizado.
