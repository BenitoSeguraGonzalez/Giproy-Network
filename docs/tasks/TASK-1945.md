# TASK-1945 - Correccion de codigo publico de empresa en entorno local

Estado: cerrada
Fecha: 2026-06-18
Modo: GIPROY CLASICO

## Objetivo

Corregir el error visible `No se pudo cargar el codigo publico de empresa` en
`Otros Servicios > Envios y Transferencias`.

## Diagnostico

El backend ya estaba preparado para usar `TransferCompanyPublicCode`, pero la
base PostgreSQL local no tenia aplicada la migracion aditiva
`de1933a1b2c3_transfer_company_public_codes.py`.

Como consecuencia, el endpoint `GET /api/v1/transferencias/my-code` intentaba
consultar `transfer_company_public_codes` y fallaba porque la tabla no existia.

## Alcance

- Aplicar la migracion Alembic aditiva `de1933a1b2c3`.
- Generar codigos publicos de empresa para las empresas locales existentes.
- Validar unicidad y carga de datos.
- No modificar frontend, API, auth, tenant ni contratos funcionales.

## No alcance

- No borrar datos.
- No alterar comunicaciones existentes.
- No modificar empresas, usuarios, licencias, Marketplace ni proyectos.
- No tocar BIM, Docker, Coolify, CI/CD, staging ni produccion.

## Cambios ejecutados

- Ejecutado:
  `..\\.venv\\Scripts\\python.exe -m alembic -c alembic.ini upgrade de1933a1b2c3`
- Ejecutado saneamiento con servicio existente:
  `transferencias_service.sanitize_company_public_codes(db)`.

## Resultado de datos

- Empresas escaneadas: 3.
- Codigos creados: 3.
- Codigos existentes antes del saneamiento: 0.
- Codigos activos verificados:
  - `Administradores Generales`: `DVB - KPY`
  - `Jesus Benito Segura Gonzalez`: `QJ0 - XA9`
  - `Santiago Bermeo`: `Q4J - L36`

## Validaciones

- Tabla `transfer_company_public_codes`: existe.
- `alembic_version`: contiene `de1933a1b2c3`.
- `pytest app\\tests\\test_transferencias_recipients.py -q`: 13 passed.
- `node frontend\\scripts\\smoke-classic-no-bim-contamination.mjs`: OK.

Warnings conocidos no bloqueantes:

- Pydantic BIM `model_name` / `model_id`.
- Deprecations de Pydantic/httpx en suite de tests.

## Resultado

El entorno local vuelve a estar alineado con el contrato funcional de
`TASK-1933`: el codigo `XXX - XXX` es identidad publica de empresa y puede
cargarse desde la pantalla clasica de `Envios y Transferencias`.

## Rollback

Rollback no recomendado salvo necesidad de volver al estado defectuoso. En caso
necesario, ejecutar downgrade de la revision `de1933a1b2c3`, sabiendo que eso
volveria a romper la carga de codigo publico de empresa.
