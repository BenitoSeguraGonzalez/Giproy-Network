# TASK-1949 - Recuperacion operativa de proyecto borrado en Santiago Bermeo

Fecha: 2026-06-18
Modo: GiProy Clasico
Estado: Cerrada

## Nota de correccion posterior

La recuperacion de esta TASK correspondio a un candidato reciente, pero el
usuario confirmo despues que el Proyecto buscado tenia fecha de creacion de
meses atras. La recuperacion correcta queda documentada y ejecutada en
TASK-1950 para `Proyecto Prueba Compartir 1` (`proyecto_id=7`).

El Proyecto creado en esta TASK (`proyecto_id=30`) queda como candidato no
objetivo. No se elimina automaticamente para evitar una accion destructiva no
autorizada; su rollback debe ejecutarse solo con confirmacion explicita.

## Objetivo

Analizar y recuperar un Proyecto borrado por el administrador de la empresa
`Santiago Bermeo`, preservando la integridad clasica Proyecto/Base/APU/EDT/
Presupuesto y evitando inserciones manuales parciales.

## Alcance

- Datos operativos clasicos de Proyectos.
- Empresa receptora `Santiago Bermeo` (`empresa_id=3`).
- Motor comun `classic_asset_portability_service`.
- Auditoria estructurada `system_audit_events`.

Queda fuera:

- UX BIM, dependencias BIM o activacion BIM.
- Docker, Coolify, CI/CD, staging o produccion.
- Cambios de codigo, rutas API, auth, JWT, permisos o tenant global.
- Recuperacion por restauracion destructiva de base de datos.

## Analisis realizado

- Se confirmo que el borrado de Proyecto es fisico/destructivo en la capa
  clasica, no una papelera logica.
- La tabla `proyectos` mantenia solo los proyectos vivos `26` y `29` para
  `Santiago Bermeo`.
- La secuencia de proyectos y snapshots de comunicaciones mostraban copias
  importadas previas hacia Santiago con IDs `23`, `24` y `25`, ya ausentes.
- El candidato mas reciente recuperable era la copia importada desde
  `shipment_id=4`, con `target_entity_id=25`, originada desde el Proyecto vivo
  `22` de `Administradores Generales`.

## Cambios realizados

- Se creo backup operativo previo:
  `tmp/project_recovery_santiago_before_20260618_172159.json`.
- Se recupero el proyecto mediante clonacion transaccional desde el origen
  vivo `Proyecto 22` hacia `Santiago Bermeo`.
- Nuevo Proyecto creado:
  - `proyecto_id=30`
  - nombre: `compras publicas prueba1.pdf - recibido 4 - recuperado`
  - codigo: `Giproy-2026-000000018-TRF4REC`
  - `base_trabajo_id=55`
- Se registro auditoria estructurada:
  - modulo: `proyectos`
  - evento: `project_recovered_manual`
  - entidad: `proyecto`
  - `entity_id=30`

## Validacion ejecutada

- Integridad `classic_asset_portability_service.assert_project_budget_apu_integrity`: OK.
- Nodos EDT recuperados: 23.
- Presupuestos recuperados: 1.
- Lineas de presupuesto recuperadas: 247.
- APUs en Base recuperada: 212.
- Recursos en Base recuperada: 287.
- Lineas operativas sin APU: 0.
- APUs fuera de la Base de Proyecto recuperada: 0.

## Impacto

Impacto acotado a datos clasicos de la empresa `Santiago Bermeo`.
No se modifico codigo fuente, contratos API, frontend, BIM, Docker ni guardas
del baseline TASK-1807.

## Rollback

Rollback focal:

- Borrar el Proyecto recuperado `proyecto_id=30` desde el flujo clasico de
  borrado completo, lo que debe eliminar su Base de Proyecto clonada `55` y sus
  dependencias operativas.
- Conservar como referencia el backup previo
  `tmp/project_recovery_santiago_before_20260618_172159.json`.
- Revertir esta TASK y las entradas asociadas en `docs/CHANGELOG.md` y
  `docs/HANDOFF.md` si se requiere limpiar la trazabilidad documental.
