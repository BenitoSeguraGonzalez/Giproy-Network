# TASK-1950 - Recuperacion correcta de proyecto historico en Santiago Bermeo

Fecha: 2026-06-18
Modo: GiProy Clasico
Estado: Cerrada

## Objetivo

Recuperar el Proyecto historico correcto de la empresa `Santiago Bermeo`, tras
confirmar que el borrado fue reciente pero el Proyecto tenia fecha de creacion
de meses atras.

## Alcance

- Datos operativos clasicos de Proyectos.
- Empresa `Santiago Bermeo` (`empresa_id=3`).
- Proyecto historico `Proyecto Prueba Compartir 1` (`proyecto_id=7`).
- Base de Proyecto `base_trabajo_id=34`.
- Presupuesto `presupuesto_id=13`.

Queda fuera:

- UX BIM, dependencias BIM o activacion BIM.
- Docker, Coolify, CI/CD, staging o produccion.
- Cambios de contratos API, auth, JWT, permisos o tenant.
- Borrado automatico del candidato recuperado incorrectamente en TASK-1949.

## Analisis realizado

- La pista funcional clave fue que el borrado era reciente, pero la fecha de
  creacion del Proyecto era antigua.
- Se localizaron referencias historicas consistentes al Proyecto
  `Proyecto Prueba Compartir 1`, creado el `2026-03-16`, con codigo
  `SantiagoBermeo-2026-001`.
- `db_backup.json` contenia el grafo operativo completo del Proyecto:
  Proyecto, Base de Proyecto, EDT, presupuesto, APUs, recursos, lineas de APU,
  cronogramas, formula polinomica y snapshots de calendario.
- Se descartaron inserciones parciales y se preparo un script idempotente de
  recuperacion desde backup con ejecucion previa en modo `dry-run`.

## Cambios realizados

- Se creo el script focal:
  `backend/scripts/recover_santiago_project7_from_backup.py`.
- Se recupero desde `db_backup.json`:
  - `proyecto_id=7`
  - nombre: `Proyecto Prueba Compartir 1`
  - codigo: `SantiagoBermeo-2026-001`
  - `base_trabajo_id=34`
  - `presupuesto_id=13`
- Se restauraron dependencias operativas:
  - 15 nodos EDT
  - 201 lineas de presupuesto
  - 177 APUs
  - 866 lineas de APU
  - 216 recursos
  - cronograma de trabajo
  - cronograma valorado
  - formula polinomica
  - snapshot de calendario
- Se registro auditoria estructurada:
  `project_recovered_from_backup` para `entity_id=7`.

## Validacion ejecutada

- `py_compile` focal del script de recuperacion: OK.
- Recuperacion `--dry-run`: OK, transaccion revertida sin escribir datos.
- Recuperacion `--apply`: OK.
- Verificacion de existencia:
  - Proyecto: OK
  - Base de Proyecto: OK
  - Presupuesto: OK
- Integridad Proyecto/Base/APU:
  - lineas operativas sin APU: 0
  - APUs fuera de la Base de Proyecto `34`: 0
  - `classic_asset_portability_service.assert_project_budget_apu_integrity`: OK.
- Auditoria:
  - `project_recovered_from_backup` registrada para `proyecto_id=7`.

## Impacto

Impacto acotado a datos clasicos de la empresa `Santiago Bermeo` y a un script
operativo focal de recuperacion. No se modifica BIM, frontend, rutas API,
auth, JWT, permisos, tenant global, Docker/Coolify, CI/CD ni guardas del
baseline TASK-1807.

## Rollback

Rollback focal posible:

- Borrar `proyecto_id=7` desde el flujo clasico de borrado completo, si se
  confirma que la recuperacion no debe conservarse.
- El candidato recuperado incorrectamente en TASK-1949 (`proyecto_id=30`) no
  se elimina en esta TASK para evitar una accion destructiva no autorizada; su
  limpieza debe ejecutarse solo con autorizacion explicita.
- Reporte de aplicacion:
  `tmp/santiago_project7_recovery_apply_20260618_173219.json`.
