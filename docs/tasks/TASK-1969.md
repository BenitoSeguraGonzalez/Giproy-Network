# TASK-1969 - Ocultar derechos SaaS no operativos en Settings Empresa

## Modo

GIPROY CLASICO.

## Objetivo

Quitar de `Settings Empresa > Mi Empresa > Licencia y SaaS` las capacidades
visibles `Equipo`, `Fusion` y `Migracion` dentro de `Derechos efectivos`.

## Alcance autorizado

- Frontend clasico.
- Vista `Settings Empresa > Mi Empresa > Licencia y SaaS`.
- Smoke focal de estructura visual de Settings Empresa.
- Documentacion TASK/CHANGELOG/HANDOFF.

## Implementacion

- Se retiran del mapa visible `COMMERCIAL_CAPABILITY_LABELS` en `Settings.jsx`:
  - `equipo`;
  - `mod_fusion`;
  - `mod_migracion`.
- El backend y el resolvedor de licencias quedan intactos.
- `Equipo` sigue gobernado por el sistema de licencias, pero no se muestra como
  chip redundante en Settings.
- `Fusion` y `Migracion` no se muestran porque son capacidades obsoletas/no
  funcionales.
- Se refuerza `smoke-classic-settings-empresa-visual-structure.mjs` para
  bloquear la reaparicion de esas etiquetas en Settings.

## Fuera de alcance

- No se modifica backend.
- No se cambian endpoints ni contratos API.
- No se cambia auth/JWT, empresa activa, permisos ni multi-tenant.
- No se toca PostgreSQL.
- No se cambia el catalogo SaaS ni el resolvedor de capacidades.
- No se toca BIM, Docker/Coolify, CI/CD, staging ni produccion.
- No se reabre el baseline `TASK-1807`.

## Validacion

- `cmd /c node frontend\scripts\smoke-classic-settings-empresa-visual-structure.mjs`: OK.
- `cmd /c node frontend\scripts\smoke-classic-no-bim-contamination.mjs`: OK.
- `npm run build`: OK, con warning conocido de chunks grandes Vite.
- `.\.venv\Scripts\python.exe tools\ai_tools\validate_enterprise_baseline.py --include-frontend`: OK, con warnings conocidos Pydantic BIM `model_name/model_id` y chunks grandes Vite.

## No interferencia

- Sin cambios BIM.
- Sin UX BIM activada.
- Sin acoplamientos hacia BIM.
- Sin Docker/Coolify/CI/CD/staging/produccion.
- Sin cambios backend, DB, auth, tenant ni contratos API.

## Rollback

- Revertir los cambios en `frontend/src/pages/Settings.jsx`.
- Revertir el assert agregado en
  `frontend/scripts/smoke-classic-settings-empresa-visual-structure.mjs`.
- Revertir esta TASK y las entradas asociadas en `docs/CHANGELOG.md` y
  `docs/HANDOFF.md`.
