# TASK-1980 - Ajuste color anidado en editor light Gantt

## Modo

GIPROY CLASICO.

## Objetivo

Sustituir el color rojo/naranja aplicado al estado visual `Anidado` en el
editor light de recursos de Gantt por un color sobrio que no comunique alerta,
error ni criticidad.

## Alcance

- Cambiar solo la paleta visual asociada al origen operativo `anidado`.
- Aplicar el nuevo color a descripcion, unidad de medida y etiqueta del recurso.
- Mantener los colores de estados de conflicto o alerta, como `no_fusionado`,
  sin alterar su semantica.
- Reforzar el smoke clasico de Cronogramas para evitar que `anidado` vuelva a
  usar el color rojo/naranja de alerta.

## Fuera de alcance

- Sin cambios en explosion de APUs anidados.
- Sin cambios en consolidacion de recursos, snapshots operativos, cantidades,
  rendimientos, ruta critica, presupuesto, API, backend ni base de datos.
- Sin cambios BIM, Docker, Coolify, CI/CD, staging ni produccion.

## Validacion prevista

- `cmd /c node frontend\scripts\smoke-classic-cronogramas-api-boundary.mjs`
- `cmd /c node frontend\scripts\smoke-classic-no-bim-contamination.mjs`
- `npm run build`
- `.\.venv\Scripts\python.exe tools\ai_tools\validate_enterprise_baseline.py --include-frontend`

## Resultado

Implementado y validado.

- `CronogramaGantt` usa `#8A6A2A` para el estado visual `anidado`.
- `smoke-classic-cronogramas-api-boundary.mjs` bloquea que `anidado` vuelva al
  rojo/naranja de alerta.

## Validacion ejecutada

- `cmd /c node frontend\scripts\smoke-classic-cronogramas-api-boundary.mjs`: OK.
- `cmd /c node frontend\scripts\smoke-classic-no-bim-contamination.mjs`: OK.
- `npm run build`: OK, con warning conocido de chunks grandes Vite.
- `.\.venv\Scripts\python.exe tools\ai_tools\validate_enterprise_baseline.py --include-frontend`:
  OK, con warnings conocidos Pydantic BIM `model_name/model_id`.
