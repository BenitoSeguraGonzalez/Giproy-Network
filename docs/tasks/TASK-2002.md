# TASK-2002 - Compactacion del editor light APU en Gantt clasico

## Contexto

El editor light de APUs dentro de `Cronogramas > Gantt` quedaba parcialmente
por debajo del header de la aplicacion en ciertas resoluciones/DPI. Ademas, su
header, subheader de resumen y footer de totales consumian demasiada altura
operativa.

## Alcance

- Ajuste visual focal del modal del editor light APU en Gantt clasico.
- Corregir la prioridad de capa del modal frente al layout principal.
- Compactar header, subheader/resumen superior y footer/totales.
- No modificar calculos, persistencia, API, backend, DB, auth, tenant ni BIM.

## Cambios

- Se agrega `GANTT_RESOURCE_EDITOR_MODAL_Z_INDEX` para elevar el portal del
  editor light de recursos por encima del header/layout clasico.
- El contenedor del modal usa mas altura util (`max-h-[96vh]`) y menos padding
  exterior.
- Header, botones de accion, tarjetas de resumen y totales quedan con padding,
  radios y tipografias mas compactos.
- El modal interno de ajuste trazable queda contenido dentro del editor con
  jerarquia local estable.

## Validacion

- `npm run build` en `frontend`: OK, con warning conocido de chunks grandes
  Vite.
- `node frontend/scripts/smoke-classic-no-bim-contamination.mjs`: OK.
- `node frontend/scripts/smoke-classic-cronogramas-api-boundary.mjs`: OK.
- Busqueda de imports directos de `axiosConfig` fuera de `frontend/src/api`: sin
  resultados.
- Busqueda de `console.log` productivos en `frontend/src` fuera de `api`: sin
  resultados.

## No interferencia

- Sin cambios BIM ni UX BIM.
- Sin cambios backend, DB, auth/JWT/tenant ni contratos API.
- Sin Docker/Coolify/CI/CD/staging/produccion.
- Sin tocar guardas TASK-1779 a TASK-1807.

## Estado

Cerrada.
