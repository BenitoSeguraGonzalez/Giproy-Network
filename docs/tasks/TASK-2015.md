# TASK-2015 - Lazy-load de modales secundarios en Presupuesto clasico

Fecha: 2026-07-08

Modo: GIPROY CLASICO

## Objetivo

Continuar la optimizacion de velocidad de presentacion en Presupuesto sin
modificar contratos API, datos, tenant, auth ni reglas de negocio.

## Alcance

- Diferir modales secundarios de `PresupuestoDetail`.
- Mantener carga inicial de lineas y catalogo APU sin cambios funcionales.
- Reforzar smoke focal de Presupuesto para evitar regresion a imports eager.

## Cambios

- `IndirectosModal` pasa de import estatico a `React.lazy(...)`.
- `ParetoModal` pasa de import estatico a `React.lazy(...)`.
- `NotasGeneralesModal` pasa de import estatico a `React.lazy(...)`.
- `CommonReportPreviewModal` pasa de import estatico a `React.lazy(...)`.
- `ReportGenerationModal` pasa de import estatico a `React.lazy(...)`.
- Cada modal se monta bajo `React.Suspense fallback={null}` solo cuando su
  estado de apertura esta activo.
- El smoke `smoke-classic-presupuesto-api-boundary.mjs` valida que estos
  modales no vuelvan a importarse eager.

## Resultado de rendimiento

- Antes del slice: `PresupuestoDetail` ~198,6 KB.
- Despues del slice: `PresupuestoDetail` ~153,8 KB, gzip ~42,5 KB.
- Chunks diferidos generados:
  - `IndirectosModal` ~27,9 KB, gzip ~7,6 KB.
  - `CommonReportPreviewModal` ~30,2 KB, gzip ~8,0 KB.
  - `ParetoModal` ~13,6 KB, gzip ~4,1 KB.
  - `NotasGeneralesModal` ~8,8 KB, gzip ~3,3 KB.
  - `ReportGenerationModal` ~1,6 KB, gzip ~0,8 KB.

## Validacion

- `node frontend/scripts/smoke-classic-presupuesto-api-boundary.mjs`: OK.
- `node frontend/scripts/smoke-classic-no-bim-contamination.mjs`: OK.
- `npm run build`: OK, con warning conocido por chunk `CronogramaGantt`.
- `python tools/ai_tools/validate_enterprise_baseline.py --include-frontend`:
  OK.
- Despliegue beta: frontend reconstruido y recreado; `giproy-beta-frontend`,
  `giproy-beta-backend` y `giproy-beta-postgres` healthy.

## No interferencia

- Sin cambios backend.
- Sin migraciones DB.
- Sin cambios en auth, JWT, tenant, permisos, rutas ni contratos API.
- Sin activar UX BIM ni crear dependencia hacia BIM.
- Sin tocar guardas TASK-1807.
- Sin crear Dockerfiles, compose, pipelines ni configuracion Coolify.

## Rollback

Restaurar los imports estaticos previos en `PresupuestoDetail.jsx`, volver a
renderizar los modales siempre montados y retirar las aserciones lazy del smoke
focal.

En servidor beta existen backups con sufijo `bak-task2015-20260708-122309`.
