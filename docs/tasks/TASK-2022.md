# TASK-2022 - Puerta controlada Proyectos <-> BIM

Fecha: 2026-07-13

Modo: INTEGRACION CONTROLADA CLASICO <-> BIM

Estado: DONE

Tipo: integracion

Prioridad: high

## Objetivo

Iniciar Gate D exponiendo el workspace BIM dentro del proyecto seleccionado
solo para empresas y usuarios autorizados por los feature flags BIM existentes,
sin modificar contratos, permisos ni datos de GiProy Clasico.

## Alcance autorizado

- Un unico punto de insercion en `Proyectos`.
- Carga diferida de `BimTab`; el bundle BIM no se monta sin acceso positivo.
- Navegacion desde un vinculo BIM hacia EDT, Presupuesto o el APU solicitado.
- Smokes focales con BIM apagado y encendido.
- Sin cambios backend, DB, auth/JWT, tenant, EDT, APUs o Presupuesto.

## Criterios verificables

- Con BIM apagado, `CLASSIC_SECTIONS` conserva las mismas secciones y orden.
- `?tab=bim` no monta BIM sin autorizacion y vuelve a Datos del proyecto.
- Con acceso positivo, aparece una unica seccion BIM y monta `BimTab` de forma
  diferida dentro del proyecto activo.
- Los links `edt` y `presupuesto` vuelven a sus secciones del mismo proyecto;
  `apu` abre `/apus?apu_id=<id>` usando el contrato clasico ya soportado.
- No se toca TASK-1807 ni sus guardas.

## Continuidad cerrada

- `TASK-2023`, `TASK-2024` y `TASK-2025` completan foco EDT, foco Presupuesto y
  retorno APU reutilizando contratos clasicos existentes.
- Gate E queda en prearranque; el piloto real no empieza hasta registrar
  empresa, usuarios, fechas y dos revisiones reales de modelo.

## Archivos funcionales

- `frontend/src/pages/Proyectos.jsx`
- `frontend/scripts/smoke-classic-no-bim-contamination.mjs`
- `frontend/scripts/smoke-bim-workspace-positive.mjs`

## Validacion

- Smokes focales BIM apagado/encendido: OK.
- `npm run build`: OK; `BimTab` queda en chunk diferido independiente. Solo se
  conserva el warning conocido de chunks grandes.
- Guardas de Proyectos, API y logging: OK.
- `tools/ai_tools/validate_enterprise_baseline.py --include-frontend`: OK,
  incluido el smoke anti-BIM y la compilacion/import del backend comun.

## Cierre porcentual

- Slice `TASK-2022`: 100%.
- Gate D: 100% local tras TASK-2023/2024/2025.
- Gate E: prearranque documentado; piloto real pendiente.
- Programa BIM estimado para liberacion: 99%; solo falta el piloto real Gate E.

## Rollback

Retirar exclusivamente el hook, la seccion `BIM_SECTION`, el lazy `BimTab` y
el callback de navegacion en `Proyectos.jsx`; restaurar las aserciones previas
de ambos smokes. No existe rollback de datos ni migraciones.
