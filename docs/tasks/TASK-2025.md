# TASK-2025 - Retorno APU hacia BIM

Fecha: 2026-07-13

Modo: INTEGRACION CONTROLADA CLASICO <-> BIM

Estado: DONE

## Objetivo

Abrir el APU vinculado desde BIM y permitir volver al mismo proyecto y pestaña
BIM reutilizando el flujo contextual seguro existente en APUs.

## Implementación

- La ida utiliza `apu_id`, `entry=project`, `return_to=project`, `project_id` y
  `return_tab=bim`.
- APUs selecciona y desplaza el APU solicitado con su lógica existente.
- El retorno reutiliza `handleReturnToProjectModule`, incluida la confirmación
  de guardado o abandono cuando hay cambios sin guardar.
- No se modifica `APUs.jsx`.

## Validación

- `smoke-bim-classic-navigation-gate.mjs`: OK.
- Smokes BIM flag off/on, build y baseline enterprise: OK.

## Rollback

Restaurar la construcción anterior de URL en `handleBimNavigateTarget`. No hay
persistencia nueva.
