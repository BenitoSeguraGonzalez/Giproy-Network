# TASK-2031 - Simplificacion de costos en semaforos APU

Fecha: 2026-07-21

Modo: GIPROY CLASICO

Estado: DONE - desplegada en beta

Tipo: correccion UX focal

## Objetivo

Eliminar informacion redundante del panel `Cronogramas > Gantt > Semaforos APU`
y presentar el costo unitario con la secuencia operativa solicitada.

## Alcance

- Sustituir `Directo exacto` y `Directo plan` por `Costo directo`.
- Mostrar `% indirecto`, `Costo indirecto` y `Precio plan` en ese orden.
- Retirar el bloque visual `Validaciones`, redundante para la lectura operativa.
- Mantener sin cambios el motor de calculo, API, backend y datos.

## Cambios realizados

- `Costo directo` usa el costo directo unitario planificado ya calculado.
- `Costo indirecto` muestra la diferencia entre precio plan y costo directo,
  equivalente al porcentaje indirecto aplicado.
- El panel deja de renderizar el detalle de validaciones.
- El smoke focal comprueba etiquetas, orden y ausencia de elementos retirados.

## No interferencia

- Sin cambios en BIM ni activacion de UX BIM.
- Sin cambios de contratos API, auth, tenant, PostgreSQL, APUs o Presupuestos.
- TASK-1807 y sus guardas permanecen intactos.

## Despliegue beta

- Frontend `sha256:76badc449cacdb18881ddc45c642aab119a51f43d4d70606fd2ddac96e9bf9ce`.
- Frontend, backend y PostgreSQL saludables; dominio publico `200`.
- Bundle publicado contiene `Costo directo` y conserva `Primavera P6 XER`.
- Sin reconstruccion de backend, cambios de PostgreSQL ni migraciones.
- Rollback remoto en `deploy/backups/classic-task2031-2033-20260721-102222`.

## Rollback

Restaurar las filas anteriores de `Costo unitario`, el bloque visual de
validaciones y retirar las aserciones focales agregadas al smoke.
