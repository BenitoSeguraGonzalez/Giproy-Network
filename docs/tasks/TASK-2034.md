# TASK-2034 - Reconstruccion y sincronizacion del remoto Gitea

Fecha: 2026-07-21

Modo: INFRAESTRUCTURA COMPARTIDA

Estado: DONE

Tipo: recuperacion operativa

## Objetivo

Restablecer el remoto interno Gitea tras la desaparicion del servidor historico
`192.168.18.114` y sincronizar el repositorio GiProy con GitHub.

## Diagnostico

- La direccion historica no respondia por red.
- El contenedor disponible en `192.168.18.106` apuntaba a PostgreSQL en una
  direccion sin servicio y no conservaba repositorios ni metadatos historicos.
- Gitea tenia `INSTALL_LOCK=false`, por lo que rechazaba su CLI administrativa.

## Cambios realizados

- Se respaldo `docker-compose.yml` y `app.ini` antes de reconstruir.
- Se creo una base PostgreSQL dedicada `gitea` sobre el PostgreSQL saludable del
  servidor beta y se conecto el servicio mediante la red Docker `backend`.
- Se corrigio `INSTALL_LOCK=true` y Gitea inicio con ORM operativo.
- Se creo la cuenta administradora `BenitoSegura` y se registro autenticacion
  mediante clave SSH.
- Se creo `BenitoSegura/Giproy-Netword` y se restauro la rama `main` desde el
  repositorio local ya sincronizado con GitHub.
- El remoto local `gitea` usa ahora
  `ssh://git@192.168.18.106:2222/BenitoSegura/Giproy-Netword.git`.

## Validacion

- Gitea mediante Traefik: HTTP `200`.
- Autenticacion SSH: OK.
- `origin/main`: `827388d061cc7f05696bc8a56dbd31b3865eec1a`.
- `gitea/main`: `827388d061cc7f05696bc8a56dbd31b3865eec1a`.
- Cuenta administradora activa: OK.
- Errores recientes de Gitea: ninguno.

## Credenciales y seguridad

- La clave privada no se copio ni se versiono.
- La credencial inicial queda fuera del repositorio en
  `/home/benito/.gitea-bootstrap-credentials`, con permisos restringidos.
- No se recuperaron usuarios, incidencias, tokens ni metadatos del servidor
  historico porque su base no estaba disponible.

## Rollback

- Compose: `/opt/ci-cd/gitea/docker-compose.yml.bak-rebuild-20260721-1040`.
- Configuracion: `/opt/ci-cd/gitea/data/gitea/conf/app.ini.bak-rebuild-20260721-1040`.
- Configuracion previa adicional:
  `/opt/ci-cd/gitea/data/gitea/conf/app.ini.bak-codex-20260721-103454`.
