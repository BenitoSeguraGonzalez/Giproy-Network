# TASK-2036 - Recuperacion de imagen referencial de proyecto

Fecha: 2026-07-22

Modo: GIPROY CLASICO con validacion de frontera BIM

Estado: DONE local; archivo recuperado en beta

## Objetivo

Recuperar la imagen referencial visible en `Proyectos > Datos de proyecto` y
evitar que una ausencia futura del archivo se presente como un icono de URL
rota.

## Diagnostico

- PostgreSQL referencia `/uploads/proyectos/59fdd407-5bf0-4e22-9162-dff554cd1ead.png`.
- El archivo local es un PNG valido de 79.975 bytes.
- El volumen persistente de beta no contiene el archivo y el endpoint estable
  `/api/v1/proyecto-detalles/media/image-proxy` responde `404` para esa ruta.
- El contrato frontend y la ruta backend de `TASK-0939` funcionan localmente y
  responden `200 image/png`; la causa es divergencia entre DB y storage.

## Alcance autorizado

- Restaurar exclusivamente el archivo faltante en el volumen persistente.
- Mantener el contrato de URL y backend existentes.
- Presentar un estado recuperable si el medio vuelve a faltar.
- Agregar smoke focal de regresion.
- No tocar proyecto, revision, EDT, APUs, Presupuesto, backend, DB ni BIM.

## Rollback

- Retirar el estado `referentialImageFailed` y el smoke focal.
- El archivo restaurado no sustituye otro archivo: el destino estaba ausente.
  Puede retirarse por su nombre exacto si se revierte la recuperacion de storage.

## Implementacion

- Se restauro el PNG exacto en
  `/app/backend/uploads/proyectos/59fdd407-5bf0-4e22-9162-dff554cd1ead.png`.
- El SHA-256 local y remoto coincide:
  `6497c46a2dfcd4cb0ddf9585bc757398233bbd5bb2d62b3f8f54ac97d94cfb0f`.
- `DatosProyecto` controla el error de carga, oculta el acceso al visor cuando
  el medio falla y presenta un estado legible con la accion de carga existente.

## Validacion

- Endpoint de media beta: `200`, `image/png`, 79.975 bytes.
- `npm run smoke:classic-project-image`: OK.
- `smoke-classic-no-bim-contamination.mjs` con BIM apagado: OK.
- `npm run smoke:bim-workspace-v2` con BIM habilitado: OK.
- `npm run build`: OK; solo warnings conocidos de chunks grandes.
- Backend, DB, API, tenant, flags y datos funcionales: sin cambios.
