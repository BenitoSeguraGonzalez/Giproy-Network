# TASK-2044 - Estudio programado y tuning controlado del WAF

Fecha: 2026-08-10

Modo: infraestructura controlada de GiProy Clásico con BIM aislado

Estado: OBSERVACIÓN ACTIVA HASTA 2026-08-17

## Objetivo

Programar una observación de siete días sobre OWASP CRS en `DetectionOnly`,
obtener evidencia agregada de falsos positivos y preparar los puntos oficiales
de reglas 900/999 para una futura protección bloqueante segura.

## Alcance

- Muestreo horario con deduplicación y retención agregada.
- Rutas normalizadas sin queries, IP, cabeceras ni cuerpos.
- Informe de reglas CRS por método/ruta y candidatos a revisión.
- Gate sin promoción automática a bloqueo.
- Archivos 900/999 vacíos, montados read-only y trazables.

## No alcance

- Activar `MODSEC_RULE_ENGINE=On` sin completar la ventana.
- Crear exclusiones globales o basadas en conjeturas.
- Generar tráfico autenticado sintético o usar credenciales productivas.
- Modificar Traefik, cloudflared, frontend, backend, auth, tenant o BIM.

## Criterios de aceptación

- Tests del normalizador y privacidad en verde.
- Compose local/remoto válido y WAF saludable con reglas montadas.
- Cron horario instalado sin privilegios globales y primera ejecución correcta.
- `latest.json` no contiene query strings, IP, cabeceras ni cuerpos.
- Gate exige siete días, 500 requests, 200 API y revisión humana.

## Rollback

Retirar la entrada marcada `TASK-2044` del crontab, eliminar únicamente
`deploy/runtime/waf-study`, restaurar el Compose TASK-2043 y recrear solo WAF.

## Checklist

- [x] Diseño de observación y gate
- [x] Analizador y tests
- [x] Archivos CRS 900/999 preparados
- [x] Validación Compose
- [x] Instalación remota y primera ejecución
- [x] Validación de privacidad, cron y rollback
- [ ] Completar siete días y volumen mínimo de tráfico
- [ ] Clasificar falsos positivos con flujos humanos reales
- [ ] Crear exclusiones focales respaldadas por regresión, si aparecen
- [ ] Autorizar en TASK separada la promoción eventual a bloqueo

## Programación activa

- Cron: minuto 17 de cada hora, bajo el usuario `benito` y con `flock`.
- Inicio verificable: 2026-08-10 11:20 America/Bogota.
- Primera revisión del gate: 2026-08-17 después de las 11:20 America/Bogota.
- Reporte: `/home/benito/docker/apps/giproy-beta/deploy/runtime/waf-study/latest.json`.
- El cron continúa observando si falta volumen; no activa bloqueo automáticamente.

## Validación inicial

- `2/2` tests unitarios y `py_compile`: OK.
- Compose local y remoto: OK.
- WAF: `healthy`, `0` reinicios, sin OOM; Nginx carga `852` reglas locales.
- Archivos 900/999 montados `ro` (`RW=false`).
- Primera muestra controlada: `6` requests, `2` API y `2` sondas CRS.
- Segunda ejecución sobre el mismo intervalo: `0` requests y `0` detecciones
  nuevas, confirmando deduplicación.
- El reporte reduce ambas sondas a ruta `/` y no conserva query, IP, headers ni
  bodies. Las dos detecciones iniciales son smokes conocidos, no candidatos a
  exclusión y quedan por debajo del umbral de repetición (`3`).
- Backup Compose:
  `docker-compose.beta.yml.bak-task2044-20260810-112000`.
