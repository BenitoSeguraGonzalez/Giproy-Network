# TASK-2049 - Corrección del diagnóstico y ejecución Alembic beta

Fecha: 2026-08-11

Estado: CERRADA Y VERIFICADA EN BETA

## Causa raíz

La revisión `001122334455` no faltaba: estaba versionada, desplegada y aplicada.
Una comprobación ejecutó el perfil `migrate` sin `GIPROY_IMAGE_TAG`, por lo que
Compose seleccionó una imagen predeterminada antigua que no contenía esa
revisión.

## Corrección y evidencia

- El script de deploy resuelve y exporta explícitamente `GIPROY_IMAGE_TAG` antes
  de cualquier llamada a Compose.
- Backup previo: `giproy-beta-20260811-135545.sql.gz`.
- `upgrade heads` con `task2048-20260811`: correcto.
- `alembic current` reconoce `001122334455 (head)` y los otros ocho heads.
- No se estampó ni alteró manualmente `alembic_version`.

## Rollback

Revertir la exportación explícita del tag. No hubo cambios de esquema ni datos.
