# TASK-2051 - Hardening transversal de módulos clásicos no BIM

Fecha: 2026-08-11

Modo: GIPROY CLÁSICO

Estado: COMPLETADA, VALIDADA Y DESPLEGADA EN BETA

## Objetivo

Continuar el code review de los módulos clásicos no BIM para eliminar fallos
runtime y optimizar rutas de interfaz antes de pruebas públicas, respetando los
contratos, guardas y límites de TASK-1807.

## Cambios

- Cronograma Gantt deja de leer `periodToken` fuera de alcance al reagrupar
  subbarras operativas y usa la identidad estable del padre.
- Cronogramas declara en el componente propietario los símbolos requeridos por
  el resumen temporal y estabiliza los cálculos memorizados de capacidad.
- EDT valorada declara todos sus hooks antes del retorno de modal cerrado,
  evitando cambios ilegales en el orden de hooks al abrir y cerrar.
- Marketplace usa el helper de metadatos de licencia realmente importado en el
  checkout, evitando un `ReferenceError`.
- Dashboard estabiliza su catálogo de módulos y observador, elimina una
  resincronización redundante y ofrece navegación semántica por teclado.
- El estado público de revisión RUC ignora respuestas tardías después del
  desmontaje y representa enlaces sin token sin una actualización redundante.
- El smoke de símbolos runtime protege los fallos corregidos y queda integrado
  al baseline enterprise.
- El smoke de seguridad de despliegue y la documentación adaptativa reconocen
  la versión bloqueada vigente de `react-router-dom` 7.18.2.

## Invariantes

- Sin cambios de API, base de datos, auth, JWT, tenant, permisos o contratos.
- Sin dependencias nuevas, Docker, Coolify, CI/CD ni despliegue.
- Sin incorporación de UX BIM a módulos clásicos.
- Sin reabrir la modularización cerrada en TASK-1807.

## Validación

- Smoke runtime clásico: correcto.
- Smokes no BIM registrados: correctos.
- ESLint focal de los archivos de autenticación y dashboard: correcto.
- Build Vite: correcto, 2.508 módulos.
- Baseline enterprise con frontend: correcto, incluido el nuevo smoke runtime.
- Suite backend completa heredada del mismo ciclo de hardening: `915 passed`,
  `2 skipped`, `0 failed`; no se modificó backend en TASK-2051.
- Warnings no bloqueantes: namespaces Pydantic heredados y chunks grandes
  existentes de BIM 3D/CronogramaGantt.

## Evidencia de despliegue beta

- Commit fuente: `7ad2b23`.
- Tag de imágenes: `task2051-20260811`.
- Backend: `sha256:9867986b7019b6ed2dbc7f6263f20828fcea5f8c23c6cac06daa738601817589`.
- Frontend: `sha256:131243327e98a726f2875c5c6d8577b51094dd68328396510ebbff4071faf7c6`.
- Artefacto fuente: SHA-256
  `1234eda276bfb088102f863adaef0e8aa931ad06a9d27a9f4ee9eda8320ab5e9`.
- Backup PostgreSQL: `deploy/backups/giproy-beta-20260811-154835.sql.gz`,
  555 MB, SHA-256
  `15c74a48ad9e869804816fedd754b6c69f8ea41019c753a3bed088c2d1fd887c`.
- PostgreSQL, backend, frontend y WAF: `healthy`.
- Los nueve heads Alembic, incluido `001122334455`, están aplicados.
- Home, login, OpenAPI interno y OpenAPI público: HTTP 200; identidad
  `GIPROY | Engineering ERP` verificada.
- Logs posteriores: sin `ERROR`, `Traceback` o `CRITICAL` nuevos en backend y
  sin alertas críticas nuevas del WAF.

El launcher combinado intentó resolver inicialmente una imagen transitoria
para el perfil `migrate` y Alembic rechazó la ejecución antes de reemplazar los
servicios. Se comprobó dentro de la imagen final que la revisión existe y que
la base ya estaba en los nueve heads; al no existir cambios backend ni de
esquema en TASK-2051, se publicaron explícitamente backend/frontend con el tag
fijado y se recreó el WAF. No se estampó ni modificó manualmente la base.

## Rollback

Recrear backend y frontend con las imágenes
`task2051-predeploy-20260811` y recrear después el WAF. El backup PostgreSQL se
conserva como contingencia, aunque TASK-2051 no aplicó migraciones ni modificó
datos. Para código local, revertir únicamente el commit `7ad2b23` y este cierre
documental.
