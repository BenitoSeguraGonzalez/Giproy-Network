# TASK-2063

Estado
IN_PROGRESS

Tipo
integration

Prioridad
high

Descripcion

Evitar que la apertura del cargador IFC en Modelo BIM quede bloqueada por la
carga automatica y la conversion de una version activa en el hilo principal
del navegador.

Relacionado

`BIM-TASK-0197`

Alcance

- Exigir seleccion explicita de version antes de montar el visor pesado.
- Desmontar el viewport 3D mientras el panel Importar IFC esta abierto.
- Mantener 2D y 3D sin datos fallback cuando no existe una seleccion.
- No modificar API, autenticacion, tenant, datos ni flujo clasico.

Verificacion

- [x] Smoke DOM: sin viewport pesado y dialogo Nueva carga IFC visible.
- [x] Smoke de contrato BIM.
- [x] Build Vite.
- [x] ESLint focal y `git diff --check`.
- [ ] Validacion visual autenticada en beta.
- [ ] Commit, push y deploy.

Rollback

Revertir unicamente los cambios de `BimFlowWorkspace`, su harness y el smoke
asociado; no requiere migracion ni cambios de infraestructura.
