import assert from'node:assert/strict';import{spawn,spawnSync}from'node:child_process';import{chromium}from'playwright';const port=4264,url=`http://127.0.0.1:${port}`,v=spawn('cmd.exe',['/c','npm','run','dev','--','--host','127.0.0.1','--port',String(port)],{cwd:process.cwd(),stdio:'ignore',windowsHide:true}),kill=()=>spawnSync('taskkill',['/pid',String(v.pid),'/T','/F'],{stdio:'ignore'});let b;try{for(let i=0;i<120;i++){try{if((await fetch(url)).ok)break;}catch{}await new Promise(r=>setTimeout(r,250));}b=await chromium.launch({headless:true,executablePath:'C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe'});for(const viewport of[{width:1920,height:900},{width:2560,height:1300}]){const p=await b.newPage({viewport});await p.goto(`${url}/bim-cost-forecast-harness.html`);await p.locator('[data-bim-cost-forecast]').waitFor();assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth),false);if(viewport.width===1920){await p.getByLabel('Estimado por completar').fill('8000');await p.getByLabel('Justificación forecast').fill('Hipótesis aprobable de cierre');await p.getByRole('button',{name:'Crear forecast'}).click();await p.getByLabel('Motivo de decisión forecast').fill('Aprobado por dirección');await p.getByRole('button',{name:'Aprobar'}).click();await p.waitForFunction(()=>document.body.textContent.includes('approved'));}await p.close();}console.log('validate-bim-cost-forecast-dom: ok');}finally{await b?.close();kill();}
