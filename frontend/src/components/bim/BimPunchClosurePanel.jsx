import { useCallback, useEffect, useMemo, useState } from 'react';
import { Check, ClipboardCheck, RefreshCw, ShieldCheck, X } from 'lucide-react';
import { bimModelsApi } from '../../api/bimModels';

export default function BimPunchClosurePanel({ projectId, empresaId, api = bimModelsApi }) {
    const [items, setItems] = useState([]); const [form, setForm] = useState({ revision: '', criteria: 'Sin hallazgos abiertos\nEvidencias verificadas', verification_notes: '' }); const [selectedId, setSelectedId] = useState(null); const [reason, setReason] = useState(''); const [busy, setBusy] = useState(false); const [error, setError] = useState(''); const [createOpen, setCreateOpen] = useState(false);
    const selected = useMemo(() => items.find((item) => item.id === selectedId) || items[0], [items, selectedId]);
    const load = useCallback(async () => { if (!projectId) return; try { setError(''); const rows = await api.listPunchClosures(projectId, empresaId); setItems(rows); setSelectedId((current) => current || rows[0]?.id || null); } catch (requestError) { setError(requestError?.response?.data?.detail || 'No se pudieron cargar los cierres punch.'); } }, [api, empresaId, projectId]);
    useEffect(() => { load(); }, [load]); useEffect(() => { if (!createOpen) return undefined; const close = (event) => { if (event.key === 'Escape') setCreateOpen(false); }; window.addEventListener('keydown', close); return () => window.removeEventListener('keydown', close); }, [createOpen]);
    const create = async (event) => { event.preventDefault(); try { setBusy(true); setError(''); const value = await api.createPunchClosure(projectId, { revision: form.revision, closure_criteria: form.criteria.split('\n').map((item) => item.trim()).filter(Boolean), verification_notes: form.verification_notes }, empresaId); setItems((current) => [value, ...current]); setSelectedId(value.id); setForm({ ...form, revision: '', verification_notes: '' }); setCreateOpen(false); } catch (requestError) { setError(requestError?.response?.data?.detail || 'No se pudo presentar el cierre punch.'); } finally { setBusy(false); } };
    const decide = async (decision) => { if (!selected) return; try { setBusy(true); setError(''); const value = await api.decidePunchClosure(projectId, selected.id, { decision, reason, expected_lock_version: selected.lock_version }, empresaId); setItems((current) => current.map((item) => item.id === value.id ? value : item)); setReason(''); } catch (requestError) { setError(requestError?.response?.data?.detail || 'No se pudo decidir el cierre punch.'); } finally { setBusy(false); } };
    return <section className="relative flex h-full min-h-0 flex-col overflow-hidden border border-zinc-200 bg-white" data-bim-punch-closure><header className="flex min-h-12 shrink-0 items-center justify-between border-b border-zinc-200 px-4"><div className="flex items-center gap-2"><ShieldCheck className="size-4 text-orange-600"/><div><h3 className="text-xs font-semibold text-zinc-950">Cierre de punch list</h3><p className="text-[10px] text-zinc-600">As-built vigente · inventario congelado · decisión auditable</p></div></div><div className="flex items-center gap-2"><button type="button" onClick={() => setCreateOpen(true)} className="inline-flex h-8 items-center gap-1 bg-orange-600 px-3 text-[10px] font-semibold text-white"><ClipboardCheck size={13}/>Nuevo cierre</button><button type="button" onClick={load} disabled={busy} aria-label="Actualizar cierres punch" className="inline-flex size-8 items-center justify-center border border-zinc-300"><RefreshCw size={14}/></button></div></header>{error ? <p role="alert" className="shrink-0 border-b border-rose-200 bg-rose-50 px-4 py-2 text-xs text-rose-800">{error}</p> : null}<main className="min-h-0 flex-1 overflow-auto" data-bim-punch-closure-ledger><table className="w-full min-w-[58rem] border-collapse text-left text-xs"><thead className="sticky top-0 z-10 bg-white text-[10px] font-semibold text-zinc-600"><tr className="border-b border-zinc-200"><th className="px-4 py-2.5">Revisión</th><th className="px-3 py-2.5">Estado</th><th className="px-3 py-2.5">Hallazgos</th><th className="px-3 py-2.5">Críticos</th><th className="px-3 py-2.5">Huella punch</th><th className="px-3 py-2.5">As-built</th></tr></thead><tbody className="divide-y divide-zinc-200">{items.map((item) => <tr key={item.id} onClick={() => setSelectedId(item.id)} className={`cursor-pointer ${selected?.id === item.id ? 'bg-orange-50' : 'hover:bg-zinc-50'}`}><td className="px-4 py-3 font-semibold">{item.revision}</td><td className="px-3 py-3"><span className="rounded bg-zinc-100 px-2 py-1 text-[10px] font-semibold">{item.status}</span></td><td className="px-3 py-3 font-semibold">{item.total_items}</td><td className="px-3 py-3 font-semibold text-rose-700">{item.critical_items}</td><td className="px-3 py-3"><code className="block max-w-[20rem] truncate text-[10px]">{item.punch_snapshot_sha256}</code></td><td className="px-3 py-3">#{item.as_built_acceptance_id}</td></tr>)}</tbody></table>{!items.length ? <p className="p-8 text-center text-xs text-zinc-500">Sin cierres punch presentados.</p> : null}</main>{selected?.status === 'submitted' ? <footer className="grid shrink-0 grid-cols-[minmax(0,1fr)_7rem_7rem] gap-2 border-t border-zinc-200 p-3"><input aria-label="Motivo de decisión de cierre punch" value={reason} onChange={(event) => setReason(event.target.value)} placeholder="Motivo verificable" className="h-9 rounded-md border border-zinc-300 px-3 text-xs"/><button type="button" onClick={() => decide('accepted')} disabled={busy || reason.length < 5} className="inline-flex h-9 items-center justify-center gap-1 rounded-md bg-emerald-700 text-xs font-semibold text-white disabled:opacity-40"><Check size={13}/>Aceptar</button><button type="button" onClick={() => decide('rejected')} disabled={busy || reason.length < 5} className="inline-flex h-9 items-center justify-center gap-1 rounded-md border border-rose-200 text-xs text-rose-700"><X size={13}/>Rechazar</button></footer> : null}{createOpen ? <div className="absolute inset-0 z-30 grid place-items-center bg-zinc-950/35 p-6"><form onSubmit={create} className="w-full max-w-xl overflow-hidden rounded-xl bg-white shadow-2xl" role="dialog" aria-modal="true" aria-labelledby="punch-create-title"><header className="flex min-h-14 items-center border-b border-zinc-200 px-5"><div><h3 id="punch-create-title" className="text-sm font-semibold">Nuevo cierre punch</h3><p className="text-[11px] text-zinc-600">Declara criterios y evidencias de cierre.</p></div><button type="button" onClick={() => setCreateOpen(false)} aria-label="Cerrar nuevo cierre punch" className="ml-auto inline-flex size-8 items-center justify-center text-zinc-500"><X size={16}/></button></header><div className="space-y-4 p-5"><input required aria-label="Revisión de cierre punch" placeholder="Revisión" value={form.revision} onChange={(event) => setForm({ ...form, revision: event.target.value })} className="h-9 w-full rounded-md border border-zinc-300 px-3 text-xs"/><textarea required aria-label="Criterios de cierre punch" value={form.criteria} onChange={(event) => setForm({ ...form, criteria: event.target.value })} className="h-24 w-full resize-none rounded-md border border-zinc-300 p-3 text-xs"/><textarea required minLength={5} aria-label="Verificación de cierre punch" placeholder="Recorrido, evidencias y excepciones" value={form.verification_notes} onChange={(event) => setForm({ ...form, verification_notes: event.target.value })} className="h-32 w-full resize-none rounded-md border border-zinc-300 p-3 text-xs"/></div><footer className="flex min-h-14 items-center justify-end gap-2 border-t border-zinc-200 px-5"><button type="button" onClick={() => setCreateOpen(false)} className="h-9 px-3 text-xs">Cancelar</button><button disabled={busy} className="h-9 rounded-md bg-orange-600 px-4 text-xs font-semibold text-white">Presentar cierre</button></footer></form></div> : null}</section>;
}
